// dprint-ignore-file
// DO NOT MODIFY DIRECTLY, modify src/dsl/generator.ts to make changes

import { Adsr_default_attack_audio_decay_audio_sustain_audio_release_audio_exponent_audio_trig_audio, Adsr_default_attack_audio_decay_audio_sustain_audio_release_audio_exponent_audio_trig_scalar, Adsr_default_attack_audio_decay_audio_sustain_audio_release_audio_exponent_scalar_trig_audio, Adsr_default_attack_audio_decay_audio_sustain_audio_release_audio_exponent_scalar_trig_scalar, Adsr_default_attack_audio_decay_audio_sustain_audio_release_scalar_exponent_audio_trig_audio, Adsr_default_attack_audio_decay_audio_sustain_audio_release_scalar_exponent_audio_trig_scalar, Adsr_default_attack_audio_decay_audio_sustain_audio_release_scalar_exponent_scalar_trig_audio, Adsr_default_attack_audio_decay_audio_sustain_audio_release_scalar_exponent_scalar_trig_scalar, Adsr_default_attack_audio_decay_audio_sustain_scalar_release_audio_exponent_audio_trig_audio, Adsr_default_attack_audio_decay_audio_sustain_scalar_release_audio_exponent_audio_trig_scalar, Adsr_default_attack_audio_decay_audio_sustain_scalar_release_audio_exponent_scalar_trig_audio, Adsr_default_attack_audio_decay_audio_sustain_scalar_release_audio_exponent_scalar_trig_scalar, Adsr_default_attack_audio_decay_audio_sustain_scalar_release_scalar_exponent_audio_trig_audio, Adsr_default_attack_audio_decay_audio_sustain_scalar_release_scalar_exponent_audio_trig_scalar, Adsr_default_attack_audio_decay_audio_sustain_scalar_release_scalar_exponent_scalar_trig_audio, Adsr_default_attack_audio_decay_audio_sustain_scalar_release_scalar_exponent_scalar_trig_scalar, Adsr_default_attack_audio_decay_scalar_sustain_audio_release_audio_exponent_audio_trig_audio, Adsr_default_attack_audio_decay_scalar_sustain_audio_release_audio_exponent_audio_trig_scalar, Adsr_default_attack_audio_decay_scalar_sustain_audio_release_audio_exponent_scalar_trig_audio, Adsr_default_attack_audio_decay_scalar_sustain_audio_release_audio_exponent_scalar_trig_scalar, Adsr_default_attack_audio_decay_scalar_sustain_audio_release_scalar_exponent_audio_trig_audio, Adsr_default_attack_audio_decay_scalar_sustain_audio_release_scalar_exponent_audio_trig_scalar, Adsr_default_attack_audio_decay_scalar_sustain_audio_release_scalar_exponent_scalar_trig_audio, Adsr_default_attack_audio_decay_scalar_sustain_audio_release_scalar_exponent_scalar_trig_scalar, Adsr_default_attack_audio_decay_scalar_sustain_scalar_release_audio_exponent_audio_trig_audio, Adsr_default_attack_audio_decay_scalar_sustain_scalar_release_audio_exponent_audio_trig_scalar, Adsr_default_attack_audio_decay_scalar_sustain_scalar_release_audio_exponent_scalar_trig_audio, Adsr_default_attack_audio_decay_scalar_sustain_scalar_release_audio_exponent_scalar_trig_scalar, Adsr_default_attack_audio_decay_scalar_sustain_scalar_release_scalar_exponent_audio_trig_audio, Adsr_default_attack_audio_decay_scalar_sustain_scalar_release_scalar_exponent_audio_trig_scalar, Adsr_default_attack_audio_decay_scalar_sustain_scalar_release_scalar_exponent_scalar_trig_audio, Adsr_default_attack_audio_decay_scalar_sustain_scalar_release_scalar_exponent_scalar_trig_scalar, Adsr_default_attack_scalar_decay_audio_sustain_audio_release_audio_exponent_audio_trig_audio, Adsr_default_attack_scalar_decay_audio_sustain_audio_release_audio_exponent_audio_trig_scalar, Adsr_default_attack_scalar_decay_audio_sustain_audio_release_audio_exponent_scalar_trig_audio, Adsr_default_attack_scalar_decay_audio_sustain_audio_release_audio_exponent_scalar_trig_scalar, Adsr_default_attack_scalar_decay_audio_sustain_audio_release_scalar_exponent_audio_trig_audio, Adsr_default_attack_scalar_decay_audio_sustain_audio_release_scalar_exponent_audio_trig_scalar, Adsr_default_attack_scalar_decay_audio_sustain_audio_release_scalar_exponent_scalar_trig_audio, Adsr_default_attack_scalar_decay_audio_sustain_audio_release_scalar_exponent_scalar_trig_scalar, Adsr_default_attack_scalar_decay_audio_sustain_scalar_release_audio_exponent_audio_trig_audio, Adsr_default_attack_scalar_decay_audio_sustain_scalar_release_audio_exponent_audio_trig_scalar, Adsr_default_attack_scalar_decay_audio_sustain_scalar_release_audio_exponent_scalar_trig_audio, Adsr_default_attack_scalar_decay_audio_sustain_scalar_release_audio_exponent_scalar_trig_scalar, Adsr_default_attack_scalar_decay_audio_sustain_scalar_release_scalar_exponent_audio_trig_audio, Adsr_default_attack_scalar_decay_audio_sustain_scalar_release_scalar_exponent_audio_trig_scalar, Adsr_default_attack_scalar_decay_audio_sustain_scalar_release_scalar_exponent_scalar_trig_audio, Adsr_default_attack_scalar_decay_audio_sustain_scalar_release_scalar_exponent_scalar_trig_scalar, Adsr_default_attack_scalar_decay_scalar_sustain_audio_release_audio_exponent_audio_trig_audio, Adsr_default_attack_scalar_decay_scalar_sustain_audio_release_audio_exponent_audio_trig_scalar, Adsr_default_attack_scalar_decay_scalar_sustain_audio_release_audio_exponent_scalar_trig_audio, Adsr_default_attack_scalar_decay_scalar_sustain_audio_release_audio_exponent_scalar_trig_scalar, Adsr_default_attack_scalar_decay_scalar_sustain_audio_release_scalar_exponent_audio_trig_audio, Adsr_default_attack_scalar_decay_scalar_sustain_audio_release_scalar_exponent_audio_trig_scalar, Adsr_default_attack_scalar_decay_scalar_sustain_audio_release_scalar_exponent_scalar_trig_audio, Adsr_default_attack_scalar_decay_scalar_sustain_audio_release_scalar_exponent_scalar_trig_scalar, Adsr_default_attack_scalar_decay_scalar_sustain_scalar_release_audio_exponent_audio_trig_audio, Adsr_default_attack_scalar_decay_scalar_sustain_scalar_release_audio_exponent_audio_trig_scalar, Adsr_default_attack_scalar_decay_scalar_sustain_scalar_release_audio_exponent_scalar_trig_audio, Adsr_default_attack_scalar_decay_scalar_sustain_scalar_release_audio_exponent_scalar_trig_scalar, Adsr_default_attack_scalar_decay_scalar_sustain_scalar_release_scalar_exponent_audio_trig_audio, Adsr_default_attack_scalar_decay_scalar_sustain_scalar_release_scalar_exponent_audio_trig_scalar, Adsr_default_attack_scalar_decay_scalar_sustain_scalar_release_scalar_exponent_scalar_trig_audio, Adsr_default_attack_scalar_decay_scalar_sustain_scalar_release_scalar_exponent_scalar_trig_scalar } from '../../gen/adsr'
import { VmState, push, downsample, upsample } from '../runner'
import * as genOpHelpers from '../gen-op-helpers'
import * as heap from '../heap'
import * as vmOpsVars from '../vm-ops-vars'
import { AudioVmOp } from '../vm-op'
import { decodeAudio, decodeArray, decodeCellRef, decodeScalar, encodeAudio, encodeArray, encodeScalar, isArray, isAudio, isCellRef, isScalar, WAVEFORM_CHUNK_SAMPLES, WAVEFORM_RING_MASK } from '../constants'
import { GenSlot } from '../gen-history'
import { GenPool } from '../gen-pool'
import { RunParams } from '../run-params'

export function initGenPools_Adsr(vm: VmState): void {
  vm.genPools.push(new GenPool(() => new Adsr_default_attack_scalar_decay_scalar_sustain_scalar_release_scalar_exponent_scalar_trig_scalar(), 441, 8, vm.genPoolManager, (dst: Object, src: Object) => {
    changetype<Adsr_default_attack_scalar_decay_scalar_sustain_scalar_release_scalar_exponent_scalar_trig_scalar>(dst).copyFrom(changetype<Adsr_default_attack_scalar_decay_scalar_sustain_scalar_release_scalar_exponent_scalar_trig_scalar>(src))
  }, (dst: Object) => { changetype<Adsr_default_attack_scalar_decay_scalar_sustain_scalar_release_scalar_exponent_scalar_trig_scalar>(dst).reset() }))
  vm.genPools.push(new GenPool(() => new Adsr_default_attack_scalar_decay_scalar_sustain_scalar_release_scalar_exponent_scalar_trig_audio(), 442, 8, vm.genPoolManager, (dst: Object, src: Object) => {
    changetype<Adsr_default_attack_scalar_decay_scalar_sustain_scalar_release_scalar_exponent_scalar_trig_audio>(dst).copyFrom(changetype<Adsr_default_attack_scalar_decay_scalar_sustain_scalar_release_scalar_exponent_scalar_trig_audio>(src))
  }, (dst: Object) => { changetype<Adsr_default_attack_scalar_decay_scalar_sustain_scalar_release_scalar_exponent_scalar_trig_audio>(dst).reset() }))
  vm.genPools.push(new GenPool(() => new Adsr_default_attack_scalar_decay_scalar_sustain_scalar_release_scalar_exponent_audio_trig_scalar(), 443, 8, vm.genPoolManager, (dst: Object, src: Object) => {
    changetype<Adsr_default_attack_scalar_decay_scalar_sustain_scalar_release_scalar_exponent_audio_trig_scalar>(dst).copyFrom(changetype<Adsr_default_attack_scalar_decay_scalar_sustain_scalar_release_scalar_exponent_audio_trig_scalar>(src))
  }, (dst: Object) => { changetype<Adsr_default_attack_scalar_decay_scalar_sustain_scalar_release_scalar_exponent_audio_trig_scalar>(dst).reset() }))
  vm.genPools.push(new GenPool(() => new Adsr_default_attack_scalar_decay_scalar_sustain_scalar_release_scalar_exponent_audio_trig_audio(), 444, 8, vm.genPoolManager, (dst: Object, src: Object) => {
    changetype<Adsr_default_attack_scalar_decay_scalar_sustain_scalar_release_scalar_exponent_audio_trig_audio>(dst).copyFrom(changetype<Adsr_default_attack_scalar_decay_scalar_sustain_scalar_release_scalar_exponent_audio_trig_audio>(src))
  }, (dst: Object) => { changetype<Adsr_default_attack_scalar_decay_scalar_sustain_scalar_release_scalar_exponent_audio_trig_audio>(dst).reset() }))
  vm.genPools.push(new GenPool(() => new Adsr_default_attack_scalar_decay_scalar_sustain_scalar_release_audio_exponent_scalar_trig_scalar(), 445, 8, vm.genPoolManager, (dst: Object, src: Object) => {
    changetype<Adsr_default_attack_scalar_decay_scalar_sustain_scalar_release_audio_exponent_scalar_trig_scalar>(dst).copyFrom(changetype<Adsr_default_attack_scalar_decay_scalar_sustain_scalar_release_audio_exponent_scalar_trig_scalar>(src))
  }, (dst: Object) => { changetype<Adsr_default_attack_scalar_decay_scalar_sustain_scalar_release_audio_exponent_scalar_trig_scalar>(dst).reset() }))
  vm.genPools.push(new GenPool(() => new Adsr_default_attack_scalar_decay_scalar_sustain_scalar_release_audio_exponent_scalar_trig_audio(), 446, 8, vm.genPoolManager, (dst: Object, src: Object) => {
    changetype<Adsr_default_attack_scalar_decay_scalar_sustain_scalar_release_audio_exponent_scalar_trig_audio>(dst).copyFrom(changetype<Adsr_default_attack_scalar_decay_scalar_sustain_scalar_release_audio_exponent_scalar_trig_audio>(src))
  }, (dst: Object) => { changetype<Adsr_default_attack_scalar_decay_scalar_sustain_scalar_release_audio_exponent_scalar_trig_audio>(dst).reset() }))
  vm.genPools.push(new GenPool(() => new Adsr_default_attack_scalar_decay_scalar_sustain_scalar_release_audio_exponent_audio_trig_scalar(), 447, 8, vm.genPoolManager, (dst: Object, src: Object) => {
    changetype<Adsr_default_attack_scalar_decay_scalar_sustain_scalar_release_audio_exponent_audio_trig_scalar>(dst).copyFrom(changetype<Adsr_default_attack_scalar_decay_scalar_sustain_scalar_release_audio_exponent_audio_trig_scalar>(src))
  }, (dst: Object) => { changetype<Adsr_default_attack_scalar_decay_scalar_sustain_scalar_release_audio_exponent_audio_trig_scalar>(dst).reset() }))
  vm.genPools.push(new GenPool(() => new Adsr_default_attack_scalar_decay_scalar_sustain_scalar_release_audio_exponent_audio_trig_audio(), 448, 8, vm.genPoolManager, (dst: Object, src: Object) => {
    changetype<Adsr_default_attack_scalar_decay_scalar_sustain_scalar_release_audio_exponent_audio_trig_audio>(dst).copyFrom(changetype<Adsr_default_attack_scalar_decay_scalar_sustain_scalar_release_audio_exponent_audio_trig_audio>(src))
  }, (dst: Object) => { changetype<Adsr_default_attack_scalar_decay_scalar_sustain_scalar_release_audio_exponent_audio_trig_audio>(dst).reset() }))
  vm.genPools.push(new GenPool(() => new Adsr_default_attack_scalar_decay_scalar_sustain_audio_release_scalar_exponent_scalar_trig_scalar(), 449, 8, vm.genPoolManager, (dst: Object, src: Object) => {
    changetype<Adsr_default_attack_scalar_decay_scalar_sustain_audio_release_scalar_exponent_scalar_trig_scalar>(dst).copyFrom(changetype<Adsr_default_attack_scalar_decay_scalar_sustain_audio_release_scalar_exponent_scalar_trig_scalar>(src))
  }, (dst: Object) => { changetype<Adsr_default_attack_scalar_decay_scalar_sustain_audio_release_scalar_exponent_scalar_trig_scalar>(dst).reset() }))
  vm.genPools.push(new GenPool(() => new Adsr_default_attack_scalar_decay_scalar_sustain_audio_release_scalar_exponent_scalar_trig_audio(), 450, 8, vm.genPoolManager, (dst: Object, src: Object) => {
    changetype<Adsr_default_attack_scalar_decay_scalar_sustain_audio_release_scalar_exponent_scalar_trig_audio>(dst).copyFrom(changetype<Adsr_default_attack_scalar_decay_scalar_sustain_audio_release_scalar_exponent_scalar_trig_audio>(src))
  }, (dst: Object) => { changetype<Adsr_default_attack_scalar_decay_scalar_sustain_audio_release_scalar_exponent_scalar_trig_audio>(dst).reset() }))
  vm.genPools.push(new GenPool(() => new Adsr_default_attack_scalar_decay_scalar_sustain_audio_release_scalar_exponent_audio_trig_scalar(), 451, 8, vm.genPoolManager, (dst: Object, src: Object) => {
    changetype<Adsr_default_attack_scalar_decay_scalar_sustain_audio_release_scalar_exponent_audio_trig_scalar>(dst).copyFrom(changetype<Adsr_default_attack_scalar_decay_scalar_sustain_audio_release_scalar_exponent_audio_trig_scalar>(src))
  }, (dst: Object) => { changetype<Adsr_default_attack_scalar_decay_scalar_sustain_audio_release_scalar_exponent_audio_trig_scalar>(dst).reset() }))
  vm.genPools.push(new GenPool(() => new Adsr_default_attack_scalar_decay_scalar_sustain_audio_release_scalar_exponent_audio_trig_audio(), 452, 8, vm.genPoolManager, (dst: Object, src: Object) => {
    changetype<Adsr_default_attack_scalar_decay_scalar_sustain_audio_release_scalar_exponent_audio_trig_audio>(dst).copyFrom(changetype<Adsr_default_attack_scalar_decay_scalar_sustain_audio_release_scalar_exponent_audio_trig_audio>(src))
  }, (dst: Object) => { changetype<Adsr_default_attack_scalar_decay_scalar_sustain_audio_release_scalar_exponent_audio_trig_audio>(dst).reset() }))
  vm.genPools.push(new GenPool(() => new Adsr_default_attack_scalar_decay_scalar_sustain_audio_release_audio_exponent_scalar_trig_scalar(), 453, 8, vm.genPoolManager, (dst: Object, src: Object) => {
    changetype<Adsr_default_attack_scalar_decay_scalar_sustain_audio_release_audio_exponent_scalar_trig_scalar>(dst).copyFrom(changetype<Adsr_default_attack_scalar_decay_scalar_sustain_audio_release_audio_exponent_scalar_trig_scalar>(src))
  }, (dst: Object) => { changetype<Adsr_default_attack_scalar_decay_scalar_sustain_audio_release_audio_exponent_scalar_trig_scalar>(dst).reset() }))
  vm.genPools.push(new GenPool(() => new Adsr_default_attack_scalar_decay_scalar_sustain_audio_release_audio_exponent_scalar_trig_audio(), 454, 8, vm.genPoolManager, (dst: Object, src: Object) => {
    changetype<Adsr_default_attack_scalar_decay_scalar_sustain_audio_release_audio_exponent_scalar_trig_audio>(dst).copyFrom(changetype<Adsr_default_attack_scalar_decay_scalar_sustain_audio_release_audio_exponent_scalar_trig_audio>(src))
  }, (dst: Object) => { changetype<Adsr_default_attack_scalar_decay_scalar_sustain_audio_release_audio_exponent_scalar_trig_audio>(dst).reset() }))
  vm.genPools.push(new GenPool(() => new Adsr_default_attack_scalar_decay_scalar_sustain_audio_release_audio_exponent_audio_trig_scalar(), 455, 8, vm.genPoolManager, (dst: Object, src: Object) => {
    changetype<Adsr_default_attack_scalar_decay_scalar_sustain_audio_release_audio_exponent_audio_trig_scalar>(dst).copyFrom(changetype<Adsr_default_attack_scalar_decay_scalar_sustain_audio_release_audio_exponent_audio_trig_scalar>(src))
  }, (dst: Object) => { changetype<Adsr_default_attack_scalar_decay_scalar_sustain_audio_release_audio_exponent_audio_trig_scalar>(dst).reset() }))
  vm.genPools.push(new GenPool(() => new Adsr_default_attack_scalar_decay_scalar_sustain_audio_release_audio_exponent_audio_trig_audio(), 456, 8, vm.genPoolManager, (dst: Object, src: Object) => {
    changetype<Adsr_default_attack_scalar_decay_scalar_sustain_audio_release_audio_exponent_audio_trig_audio>(dst).copyFrom(changetype<Adsr_default_attack_scalar_decay_scalar_sustain_audio_release_audio_exponent_audio_trig_audio>(src))
  }, (dst: Object) => { changetype<Adsr_default_attack_scalar_decay_scalar_sustain_audio_release_audio_exponent_audio_trig_audio>(dst).reset() }))
  vm.genPools.push(new GenPool(() => new Adsr_default_attack_scalar_decay_audio_sustain_scalar_release_scalar_exponent_scalar_trig_scalar(), 457, 8, vm.genPoolManager, (dst: Object, src: Object) => {
    changetype<Adsr_default_attack_scalar_decay_audio_sustain_scalar_release_scalar_exponent_scalar_trig_scalar>(dst).copyFrom(changetype<Adsr_default_attack_scalar_decay_audio_sustain_scalar_release_scalar_exponent_scalar_trig_scalar>(src))
  }, (dst: Object) => { changetype<Adsr_default_attack_scalar_decay_audio_sustain_scalar_release_scalar_exponent_scalar_trig_scalar>(dst).reset() }))
  vm.genPools.push(new GenPool(() => new Adsr_default_attack_scalar_decay_audio_sustain_scalar_release_scalar_exponent_scalar_trig_audio(), 458, 8, vm.genPoolManager, (dst: Object, src: Object) => {
    changetype<Adsr_default_attack_scalar_decay_audio_sustain_scalar_release_scalar_exponent_scalar_trig_audio>(dst).copyFrom(changetype<Adsr_default_attack_scalar_decay_audio_sustain_scalar_release_scalar_exponent_scalar_trig_audio>(src))
  }, (dst: Object) => { changetype<Adsr_default_attack_scalar_decay_audio_sustain_scalar_release_scalar_exponent_scalar_trig_audio>(dst).reset() }))
  vm.genPools.push(new GenPool(() => new Adsr_default_attack_scalar_decay_audio_sustain_scalar_release_scalar_exponent_audio_trig_scalar(), 459, 8, vm.genPoolManager, (dst: Object, src: Object) => {
    changetype<Adsr_default_attack_scalar_decay_audio_sustain_scalar_release_scalar_exponent_audio_trig_scalar>(dst).copyFrom(changetype<Adsr_default_attack_scalar_decay_audio_sustain_scalar_release_scalar_exponent_audio_trig_scalar>(src))
  }, (dst: Object) => { changetype<Adsr_default_attack_scalar_decay_audio_sustain_scalar_release_scalar_exponent_audio_trig_scalar>(dst).reset() }))
  vm.genPools.push(new GenPool(() => new Adsr_default_attack_scalar_decay_audio_sustain_scalar_release_scalar_exponent_audio_trig_audio(), 460, 8, vm.genPoolManager, (dst: Object, src: Object) => {
    changetype<Adsr_default_attack_scalar_decay_audio_sustain_scalar_release_scalar_exponent_audio_trig_audio>(dst).copyFrom(changetype<Adsr_default_attack_scalar_decay_audio_sustain_scalar_release_scalar_exponent_audio_trig_audio>(src))
  }, (dst: Object) => { changetype<Adsr_default_attack_scalar_decay_audio_sustain_scalar_release_scalar_exponent_audio_trig_audio>(dst).reset() }))
  vm.genPools.push(new GenPool(() => new Adsr_default_attack_scalar_decay_audio_sustain_scalar_release_audio_exponent_scalar_trig_scalar(), 461, 8, vm.genPoolManager, (dst: Object, src: Object) => {
    changetype<Adsr_default_attack_scalar_decay_audio_sustain_scalar_release_audio_exponent_scalar_trig_scalar>(dst).copyFrom(changetype<Adsr_default_attack_scalar_decay_audio_sustain_scalar_release_audio_exponent_scalar_trig_scalar>(src))
  }, (dst: Object) => { changetype<Adsr_default_attack_scalar_decay_audio_sustain_scalar_release_audio_exponent_scalar_trig_scalar>(dst).reset() }))
  vm.genPools.push(new GenPool(() => new Adsr_default_attack_scalar_decay_audio_sustain_scalar_release_audio_exponent_scalar_trig_audio(), 462, 8, vm.genPoolManager, (dst: Object, src: Object) => {
    changetype<Adsr_default_attack_scalar_decay_audio_sustain_scalar_release_audio_exponent_scalar_trig_audio>(dst).copyFrom(changetype<Adsr_default_attack_scalar_decay_audio_sustain_scalar_release_audio_exponent_scalar_trig_audio>(src))
  }, (dst: Object) => { changetype<Adsr_default_attack_scalar_decay_audio_sustain_scalar_release_audio_exponent_scalar_trig_audio>(dst).reset() }))
  vm.genPools.push(new GenPool(() => new Adsr_default_attack_scalar_decay_audio_sustain_scalar_release_audio_exponent_audio_trig_scalar(), 463, 8, vm.genPoolManager, (dst: Object, src: Object) => {
    changetype<Adsr_default_attack_scalar_decay_audio_sustain_scalar_release_audio_exponent_audio_trig_scalar>(dst).copyFrom(changetype<Adsr_default_attack_scalar_decay_audio_sustain_scalar_release_audio_exponent_audio_trig_scalar>(src))
  }, (dst: Object) => { changetype<Adsr_default_attack_scalar_decay_audio_sustain_scalar_release_audio_exponent_audio_trig_scalar>(dst).reset() }))
  vm.genPools.push(new GenPool(() => new Adsr_default_attack_scalar_decay_audio_sustain_scalar_release_audio_exponent_audio_trig_audio(), 464, 8, vm.genPoolManager, (dst: Object, src: Object) => {
    changetype<Adsr_default_attack_scalar_decay_audio_sustain_scalar_release_audio_exponent_audio_trig_audio>(dst).copyFrom(changetype<Adsr_default_attack_scalar_decay_audio_sustain_scalar_release_audio_exponent_audio_trig_audio>(src))
  }, (dst: Object) => { changetype<Adsr_default_attack_scalar_decay_audio_sustain_scalar_release_audio_exponent_audio_trig_audio>(dst).reset() }))
  vm.genPools.push(new GenPool(() => new Adsr_default_attack_scalar_decay_audio_sustain_audio_release_scalar_exponent_scalar_trig_scalar(), 465, 8, vm.genPoolManager, (dst: Object, src: Object) => {
    changetype<Adsr_default_attack_scalar_decay_audio_sustain_audio_release_scalar_exponent_scalar_trig_scalar>(dst).copyFrom(changetype<Adsr_default_attack_scalar_decay_audio_sustain_audio_release_scalar_exponent_scalar_trig_scalar>(src))
  }, (dst: Object) => { changetype<Adsr_default_attack_scalar_decay_audio_sustain_audio_release_scalar_exponent_scalar_trig_scalar>(dst).reset() }))
  vm.genPools.push(new GenPool(() => new Adsr_default_attack_scalar_decay_audio_sustain_audio_release_scalar_exponent_scalar_trig_audio(), 466, 8, vm.genPoolManager, (dst: Object, src: Object) => {
    changetype<Adsr_default_attack_scalar_decay_audio_sustain_audio_release_scalar_exponent_scalar_trig_audio>(dst).copyFrom(changetype<Adsr_default_attack_scalar_decay_audio_sustain_audio_release_scalar_exponent_scalar_trig_audio>(src))
  }, (dst: Object) => { changetype<Adsr_default_attack_scalar_decay_audio_sustain_audio_release_scalar_exponent_scalar_trig_audio>(dst).reset() }))
  vm.genPools.push(new GenPool(() => new Adsr_default_attack_scalar_decay_audio_sustain_audio_release_scalar_exponent_audio_trig_scalar(), 467, 8, vm.genPoolManager, (dst: Object, src: Object) => {
    changetype<Adsr_default_attack_scalar_decay_audio_sustain_audio_release_scalar_exponent_audio_trig_scalar>(dst).copyFrom(changetype<Adsr_default_attack_scalar_decay_audio_sustain_audio_release_scalar_exponent_audio_trig_scalar>(src))
  }, (dst: Object) => { changetype<Adsr_default_attack_scalar_decay_audio_sustain_audio_release_scalar_exponent_audio_trig_scalar>(dst).reset() }))
  vm.genPools.push(new GenPool(() => new Adsr_default_attack_scalar_decay_audio_sustain_audio_release_scalar_exponent_audio_trig_audio(), 468, 8, vm.genPoolManager, (dst: Object, src: Object) => {
    changetype<Adsr_default_attack_scalar_decay_audio_sustain_audio_release_scalar_exponent_audio_trig_audio>(dst).copyFrom(changetype<Adsr_default_attack_scalar_decay_audio_sustain_audio_release_scalar_exponent_audio_trig_audio>(src))
  }, (dst: Object) => { changetype<Adsr_default_attack_scalar_decay_audio_sustain_audio_release_scalar_exponent_audio_trig_audio>(dst).reset() }))
  vm.genPools.push(new GenPool(() => new Adsr_default_attack_scalar_decay_audio_sustain_audio_release_audio_exponent_scalar_trig_scalar(), 469, 8, vm.genPoolManager, (dst: Object, src: Object) => {
    changetype<Adsr_default_attack_scalar_decay_audio_sustain_audio_release_audio_exponent_scalar_trig_scalar>(dst).copyFrom(changetype<Adsr_default_attack_scalar_decay_audio_sustain_audio_release_audio_exponent_scalar_trig_scalar>(src))
  }, (dst: Object) => { changetype<Adsr_default_attack_scalar_decay_audio_sustain_audio_release_audio_exponent_scalar_trig_scalar>(dst).reset() }))
  vm.genPools.push(new GenPool(() => new Adsr_default_attack_scalar_decay_audio_sustain_audio_release_audio_exponent_scalar_trig_audio(), 470, 8, vm.genPoolManager, (dst: Object, src: Object) => {
    changetype<Adsr_default_attack_scalar_decay_audio_sustain_audio_release_audio_exponent_scalar_trig_audio>(dst).copyFrom(changetype<Adsr_default_attack_scalar_decay_audio_sustain_audio_release_audio_exponent_scalar_trig_audio>(src))
  }, (dst: Object) => { changetype<Adsr_default_attack_scalar_decay_audio_sustain_audio_release_audio_exponent_scalar_trig_audio>(dst).reset() }))
  vm.genPools.push(new GenPool(() => new Adsr_default_attack_scalar_decay_audio_sustain_audio_release_audio_exponent_audio_trig_scalar(), 471, 8, vm.genPoolManager, (dst: Object, src: Object) => {
    changetype<Adsr_default_attack_scalar_decay_audio_sustain_audio_release_audio_exponent_audio_trig_scalar>(dst).copyFrom(changetype<Adsr_default_attack_scalar_decay_audio_sustain_audio_release_audio_exponent_audio_trig_scalar>(src))
  }, (dst: Object) => { changetype<Adsr_default_attack_scalar_decay_audio_sustain_audio_release_audio_exponent_audio_trig_scalar>(dst).reset() }))
  vm.genPools.push(new GenPool(() => new Adsr_default_attack_scalar_decay_audio_sustain_audio_release_audio_exponent_audio_trig_audio(), 472, 8, vm.genPoolManager, (dst: Object, src: Object) => {
    changetype<Adsr_default_attack_scalar_decay_audio_sustain_audio_release_audio_exponent_audio_trig_audio>(dst).copyFrom(changetype<Adsr_default_attack_scalar_decay_audio_sustain_audio_release_audio_exponent_audio_trig_audio>(src))
  }, (dst: Object) => { changetype<Adsr_default_attack_scalar_decay_audio_sustain_audio_release_audio_exponent_audio_trig_audio>(dst).reset() }))
  vm.genPools.push(new GenPool(() => new Adsr_default_attack_audio_decay_scalar_sustain_scalar_release_scalar_exponent_scalar_trig_scalar(), 473, 8, vm.genPoolManager, (dst: Object, src: Object) => {
    changetype<Adsr_default_attack_audio_decay_scalar_sustain_scalar_release_scalar_exponent_scalar_trig_scalar>(dst).copyFrom(changetype<Adsr_default_attack_audio_decay_scalar_sustain_scalar_release_scalar_exponent_scalar_trig_scalar>(src))
  }, (dst: Object) => { changetype<Adsr_default_attack_audio_decay_scalar_sustain_scalar_release_scalar_exponent_scalar_trig_scalar>(dst).reset() }))
  vm.genPools.push(new GenPool(() => new Adsr_default_attack_audio_decay_scalar_sustain_scalar_release_scalar_exponent_scalar_trig_audio(), 474, 8, vm.genPoolManager, (dst: Object, src: Object) => {
    changetype<Adsr_default_attack_audio_decay_scalar_sustain_scalar_release_scalar_exponent_scalar_trig_audio>(dst).copyFrom(changetype<Adsr_default_attack_audio_decay_scalar_sustain_scalar_release_scalar_exponent_scalar_trig_audio>(src))
  }, (dst: Object) => { changetype<Adsr_default_attack_audio_decay_scalar_sustain_scalar_release_scalar_exponent_scalar_trig_audio>(dst).reset() }))
  vm.genPools.push(new GenPool(() => new Adsr_default_attack_audio_decay_scalar_sustain_scalar_release_scalar_exponent_audio_trig_scalar(), 475, 8, vm.genPoolManager, (dst: Object, src: Object) => {
    changetype<Adsr_default_attack_audio_decay_scalar_sustain_scalar_release_scalar_exponent_audio_trig_scalar>(dst).copyFrom(changetype<Adsr_default_attack_audio_decay_scalar_sustain_scalar_release_scalar_exponent_audio_trig_scalar>(src))
  }, (dst: Object) => { changetype<Adsr_default_attack_audio_decay_scalar_sustain_scalar_release_scalar_exponent_audio_trig_scalar>(dst).reset() }))
  vm.genPools.push(new GenPool(() => new Adsr_default_attack_audio_decay_scalar_sustain_scalar_release_scalar_exponent_audio_trig_audio(), 476, 8, vm.genPoolManager, (dst: Object, src: Object) => {
    changetype<Adsr_default_attack_audio_decay_scalar_sustain_scalar_release_scalar_exponent_audio_trig_audio>(dst).copyFrom(changetype<Adsr_default_attack_audio_decay_scalar_sustain_scalar_release_scalar_exponent_audio_trig_audio>(src))
  }, (dst: Object) => { changetype<Adsr_default_attack_audio_decay_scalar_sustain_scalar_release_scalar_exponent_audio_trig_audio>(dst).reset() }))
  vm.genPools.push(new GenPool(() => new Adsr_default_attack_audio_decay_scalar_sustain_scalar_release_audio_exponent_scalar_trig_scalar(), 477, 8, vm.genPoolManager, (dst: Object, src: Object) => {
    changetype<Adsr_default_attack_audio_decay_scalar_sustain_scalar_release_audio_exponent_scalar_trig_scalar>(dst).copyFrom(changetype<Adsr_default_attack_audio_decay_scalar_sustain_scalar_release_audio_exponent_scalar_trig_scalar>(src))
  }, (dst: Object) => { changetype<Adsr_default_attack_audio_decay_scalar_sustain_scalar_release_audio_exponent_scalar_trig_scalar>(dst).reset() }))
  vm.genPools.push(new GenPool(() => new Adsr_default_attack_audio_decay_scalar_sustain_scalar_release_audio_exponent_scalar_trig_audio(), 478, 8, vm.genPoolManager, (dst: Object, src: Object) => {
    changetype<Adsr_default_attack_audio_decay_scalar_sustain_scalar_release_audio_exponent_scalar_trig_audio>(dst).copyFrom(changetype<Adsr_default_attack_audio_decay_scalar_sustain_scalar_release_audio_exponent_scalar_trig_audio>(src))
  }, (dst: Object) => { changetype<Adsr_default_attack_audio_decay_scalar_sustain_scalar_release_audio_exponent_scalar_trig_audio>(dst).reset() }))
  vm.genPools.push(new GenPool(() => new Adsr_default_attack_audio_decay_scalar_sustain_scalar_release_audio_exponent_audio_trig_scalar(), 479, 8, vm.genPoolManager, (dst: Object, src: Object) => {
    changetype<Adsr_default_attack_audio_decay_scalar_sustain_scalar_release_audio_exponent_audio_trig_scalar>(dst).copyFrom(changetype<Adsr_default_attack_audio_decay_scalar_sustain_scalar_release_audio_exponent_audio_trig_scalar>(src))
  }, (dst: Object) => { changetype<Adsr_default_attack_audio_decay_scalar_sustain_scalar_release_audio_exponent_audio_trig_scalar>(dst).reset() }))
  vm.genPools.push(new GenPool(() => new Adsr_default_attack_audio_decay_scalar_sustain_scalar_release_audio_exponent_audio_trig_audio(), 480, 8, vm.genPoolManager, (dst: Object, src: Object) => {
    changetype<Adsr_default_attack_audio_decay_scalar_sustain_scalar_release_audio_exponent_audio_trig_audio>(dst).copyFrom(changetype<Adsr_default_attack_audio_decay_scalar_sustain_scalar_release_audio_exponent_audio_trig_audio>(src))
  }, (dst: Object) => { changetype<Adsr_default_attack_audio_decay_scalar_sustain_scalar_release_audio_exponent_audio_trig_audio>(dst).reset() }))
  vm.genPools.push(new GenPool(() => new Adsr_default_attack_audio_decay_scalar_sustain_audio_release_scalar_exponent_scalar_trig_scalar(), 481, 8, vm.genPoolManager, (dst: Object, src: Object) => {
    changetype<Adsr_default_attack_audio_decay_scalar_sustain_audio_release_scalar_exponent_scalar_trig_scalar>(dst).copyFrom(changetype<Adsr_default_attack_audio_decay_scalar_sustain_audio_release_scalar_exponent_scalar_trig_scalar>(src))
  }, (dst: Object) => { changetype<Adsr_default_attack_audio_decay_scalar_sustain_audio_release_scalar_exponent_scalar_trig_scalar>(dst).reset() }))
  vm.genPools.push(new GenPool(() => new Adsr_default_attack_audio_decay_scalar_sustain_audio_release_scalar_exponent_scalar_trig_audio(), 482, 8, vm.genPoolManager, (dst: Object, src: Object) => {
    changetype<Adsr_default_attack_audio_decay_scalar_sustain_audio_release_scalar_exponent_scalar_trig_audio>(dst).copyFrom(changetype<Adsr_default_attack_audio_decay_scalar_sustain_audio_release_scalar_exponent_scalar_trig_audio>(src))
  }, (dst: Object) => { changetype<Adsr_default_attack_audio_decay_scalar_sustain_audio_release_scalar_exponent_scalar_trig_audio>(dst).reset() }))
  vm.genPools.push(new GenPool(() => new Adsr_default_attack_audio_decay_scalar_sustain_audio_release_scalar_exponent_audio_trig_scalar(), 483, 8, vm.genPoolManager, (dst: Object, src: Object) => {
    changetype<Adsr_default_attack_audio_decay_scalar_sustain_audio_release_scalar_exponent_audio_trig_scalar>(dst).copyFrom(changetype<Adsr_default_attack_audio_decay_scalar_sustain_audio_release_scalar_exponent_audio_trig_scalar>(src))
  }, (dst: Object) => { changetype<Adsr_default_attack_audio_decay_scalar_sustain_audio_release_scalar_exponent_audio_trig_scalar>(dst).reset() }))
  vm.genPools.push(new GenPool(() => new Adsr_default_attack_audio_decay_scalar_sustain_audio_release_scalar_exponent_audio_trig_audio(), 484, 8, vm.genPoolManager, (dst: Object, src: Object) => {
    changetype<Adsr_default_attack_audio_decay_scalar_sustain_audio_release_scalar_exponent_audio_trig_audio>(dst).copyFrom(changetype<Adsr_default_attack_audio_decay_scalar_sustain_audio_release_scalar_exponent_audio_trig_audio>(src))
  }, (dst: Object) => { changetype<Adsr_default_attack_audio_decay_scalar_sustain_audio_release_scalar_exponent_audio_trig_audio>(dst).reset() }))
  vm.genPools.push(new GenPool(() => new Adsr_default_attack_audio_decay_scalar_sustain_audio_release_audio_exponent_scalar_trig_scalar(), 485, 8, vm.genPoolManager, (dst: Object, src: Object) => {
    changetype<Adsr_default_attack_audio_decay_scalar_sustain_audio_release_audio_exponent_scalar_trig_scalar>(dst).copyFrom(changetype<Adsr_default_attack_audio_decay_scalar_sustain_audio_release_audio_exponent_scalar_trig_scalar>(src))
  }, (dst: Object) => { changetype<Adsr_default_attack_audio_decay_scalar_sustain_audio_release_audio_exponent_scalar_trig_scalar>(dst).reset() }))
  vm.genPools.push(new GenPool(() => new Adsr_default_attack_audio_decay_scalar_sustain_audio_release_audio_exponent_scalar_trig_audio(), 486, 8, vm.genPoolManager, (dst: Object, src: Object) => {
    changetype<Adsr_default_attack_audio_decay_scalar_sustain_audio_release_audio_exponent_scalar_trig_audio>(dst).copyFrom(changetype<Adsr_default_attack_audio_decay_scalar_sustain_audio_release_audio_exponent_scalar_trig_audio>(src))
  }, (dst: Object) => { changetype<Adsr_default_attack_audio_decay_scalar_sustain_audio_release_audio_exponent_scalar_trig_audio>(dst).reset() }))
  vm.genPools.push(new GenPool(() => new Adsr_default_attack_audio_decay_scalar_sustain_audio_release_audio_exponent_audio_trig_scalar(), 487, 8, vm.genPoolManager, (dst: Object, src: Object) => {
    changetype<Adsr_default_attack_audio_decay_scalar_sustain_audio_release_audio_exponent_audio_trig_scalar>(dst).copyFrom(changetype<Adsr_default_attack_audio_decay_scalar_sustain_audio_release_audio_exponent_audio_trig_scalar>(src))
  }, (dst: Object) => { changetype<Adsr_default_attack_audio_decay_scalar_sustain_audio_release_audio_exponent_audio_trig_scalar>(dst).reset() }))
  vm.genPools.push(new GenPool(() => new Adsr_default_attack_audio_decay_scalar_sustain_audio_release_audio_exponent_audio_trig_audio(), 488, 8, vm.genPoolManager, (dst: Object, src: Object) => {
    changetype<Adsr_default_attack_audio_decay_scalar_sustain_audio_release_audio_exponent_audio_trig_audio>(dst).copyFrom(changetype<Adsr_default_attack_audio_decay_scalar_sustain_audio_release_audio_exponent_audio_trig_audio>(src))
  }, (dst: Object) => { changetype<Adsr_default_attack_audio_decay_scalar_sustain_audio_release_audio_exponent_audio_trig_audio>(dst).reset() }))
  vm.genPools.push(new GenPool(() => new Adsr_default_attack_audio_decay_audio_sustain_scalar_release_scalar_exponent_scalar_trig_scalar(), 489, 8, vm.genPoolManager, (dst: Object, src: Object) => {
    changetype<Adsr_default_attack_audio_decay_audio_sustain_scalar_release_scalar_exponent_scalar_trig_scalar>(dst).copyFrom(changetype<Adsr_default_attack_audio_decay_audio_sustain_scalar_release_scalar_exponent_scalar_trig_scalar>(src))
  }, (dst: Object) => { changetype<Adsr_default_attack_audio_decay_audio_sustain_scalar_release_scalar_exponent_scalar_trig_scalar>(dst).reset() }))
  vm.genPools.push(new GenPool(() => new Adsr_default_attack_audio_decay_audio_sustain_scalar_release_scalar_exponent_scalar_trig_audio(), 490, 8, vm.genPoolManager, (dst: Object, src: Object) => {
    changetype<Adsr_default_attack_audio_decay_audio_sustain_scalar_release_scalar_exponent_scalar_trig_audio>(dst).copyFrom(changetype<Adsr_default_attack_audio_decay_audio_sustain_scalar_release_scalar_exponent_scalar_trig_audio>(src))
  }, (dst: Object) => { changetype<Adsr_default_attack_audio_decay_audio_sustain_scalar_release_scalar_exponent_scalar_trig_audio>(dst).reset() }))
  vm.genPools.push(new GenPool(() => new Adsr_default_attack_audio_decay_audio_sustain_scalar_release_scalar_exponent_audio_trig_scalar(), 491, 8, vm.genPoolManager, (dst: Object, src: Object) => {
    changetype<Adsr_default_attack_audio_decay_audio_sustain_scalar_release_scalar_exponent_audio_trig_scalar>(dst).copyFrom(changetype<Adsr_default_attack_audio_decay_audio_sustain_scalar_release_scalar_exponent_audio_trig_scalar>(src))
  }, (dst: Object) => { changetype<Adsr_default_attack_audio_decay_audio_sustain_scalar_release_scalar_exponent_audio_trig_scalar>(dst).reset() }))
  vm.genPools.push(new GenPool(() => new Adsr_default_attack_audio_decay_audio_sustain_scalar_release_scalar_exponent_audio_trig_audio(), 492, 8, vm.genPoolManager, (dst: Object, src: Object) => {
    changetype<Adsr_default_attack_audio_decay_audio_sustain_scalar_release_scalar_exponent_audio_trig_audio>(dst).copyFrom(changetype<Adsr_default_attack_audio_decay_audio_sustain_scalar_release_scalar_exponent_audio_trig_audio>(src))
  }, (dst: Object) => { changetype<Adsr_default_attack_audio_decay_audio_sustain_scalar_release_scalar_exponent_audio_trig_audio>(dst).reset() }))
  vm.genPools.push(new GenPool(() => new Adsr_default_attack_audio_decay_audio_sustain_scalar_release_audio_exponent_scalar_trig_scalar(), 493, 8, vm.genPoolManager, (dst: Object, src: Object) => {
    changetype<Adsr_default_attack_audio_decay_audio_sustain_scalar_release_audio_exponent_scalar_trig_scalar>(dst).copyFrom(changetype<Adsr_default_attack_audio_decay_audio_sustain_scalar_release_audio_exponent_scalar_trig_scalar>(src))
  }, (dst: Object) => { changetype<Adsr_default_attack_audio_decay_audio_sustain_scalar_release_audio_exponent_scalar_trig_scalar>(dst).reset() }))
  vm.genPools.push(new GenPool(() => new Adsr_default_attack_audio_decay_audio_sustain_scalar_release_audio_exponent_scalar_trig_audio(), 494, 8, vm.genPoolManager, (dst: Object, src: Object) => {
    changetype<Adsr_default_attack_audio_decay_audio_sustain_scalar_release_audio_exponent_scalar_trig_audio>(dst).copyFrom(changetype<Adsr_default_attack_audio_decay_audio_sustain_scalar_release_audio_exponent_scalar_trig_audio>(src))
  }, (dst: Object) => { changetype<Adsr_default_attack_audio_decay_audio_sustain_scalar_release_audio_exponent_scalar_trig_audio>(dst).reset() }))
  vm.genPools.push(new GenPool(() => new Adsr_default_attack_audio_decay_audio_sustain_scalar_release_audio_exponent_audio_trig_scalar(), 495, 8, vm.genPoolManager, (dst: Object, src: Object) => {
    changetype<Adsr_default_attack_audio_decay_audio_sustain_scalar_release_audio_exponent_audio_trig_scalar>(dst).copyFrom(changetype<Adsr_default_attack_audio_decay_audio_sustain_scalar_release_audio_exponent_audio_trig_scalar>(src))
  }, (dst: Object) => { changetype<Adsr_default_attack_audio_decay_audio_sustain_scalar_release_audio_exponent_audio_trig_scalar>(dst).reset() }))
  vm.genPools.push(new GenPool(() => new Adsr_default_attack_audio_decay_audio_sustain_scalar_release_audio_exponent_audio_trig_audio(), 496, 8, vm.genPoolManager, (dst: Object, src: Object) => {
    changetype<Adsr_default_attack_audio_decay_audio_sustain_scalar_release_audio_exponent_audio_trig_audio>(dst).copyFrom(changetype<Adsr_default_attack_audio_decay_audio_sustain_scalar_release_audio_exponent_audio_trig_audio>(src))
  }, (dst: Object) => { changetype<Adsr_default_attack_audio_decay_audio_sustain_scalar_release_audio_exponent_audio_trig_audio>(dst).reset() }))
  vm.genPools.push(new GenPool(() => new Adsr_default_attack_audio_decay_audio_sustain_audio_release_scalar_exponent_scalar_trig_scalar(), 497, 8, vm.genPoolManager, (dst: Object, src: Object) => {
    changetype<Adsr_default_attack_audio_decay_audio_sustain_audio_release_scalar_exponent_scalar_trig_scalar>(dst).copyFrom(changetype<Adsr_default_attack_audio_decay_audio_sustain_audio_release_scalar_exponent_scalar_trig_scalar>(src))
  }, (dst: Object) => { changetype<Adsr_default_attack_audio_decay_audio_sustain_audio_release_scalar_exponent_scalar_trig_scalar>(dst).reset() }))
  vm.genPools.push(new GenPool(() => new Adsr_default_attack_audio_decay_audio_sustain_audio_release_scalar_exponent_scalar_trig_audio(), 498, 8, vm.genPoolManager, (dst: Object, src: Object) => {
    changetype<Adsr_default_attack_audio_decay_audio_sustain_audio_release_scalar_exponent_scalar_trig_audio>(dst).copyFrom(changetype<Adsr_default_attack_audio_decay_audio_sustain_audio_release_scalar_exponent_scalar_trig_audio>(src))
  }, (dst: Object) => { changetype<Adsr_default_attack_audio_decay_audio_sustain_audio_release_scalar_exponent_scalar_trig_audio>(dst).reset() }))
  vm.genPools.push(new GenPool(() => new Adsr_default_attack_audio_decay_audio_sustain_audio_release_scalar_exponent_audio_trig_scalar(), 499, 8, vm.genPoolManager, (dst: Object, src: Object) => {
    changetype<Adsr_default_attack_audio_decay_audio_sustain_audio_release_scalar_exponent_audio_trig_scalar>(dst).copyFrom(changetype<Adsr_default_attack_audio_decay_audio_sustain_audio_release_scalar_exponent_audio_trig_scalar>(src))
  }, (dst: Object) => { changetype<Adsr_default_attack_audio_decay_audio_sustain_audio_release_scalar_exponent_audio_trig_scalar>(dst).reset() }))
  vm.genPools.push(new GenPool(() => new Adsr_default_attack_audio_decay_audio_sustain_audio_release_scalar_exponent_audio_trig_audio(), 500, 8, vm.genPoolManager, (dst: Object, src: Object) => {
    changetype<Adsr_default_attack_audio_decay_audio_sustain_audio_release_scalar_exponent_audio_trig_audio>(dst).copyFrom(changetype<Adsr_default_attack_audio_decay_audio_sustain_audio_release_scalar_exponent_audio_trig_audio>(src))
  }, (dst: Object) => { changetype<Adsr_default_attack_audio_decay_audio_sustain_audio_release_scalar_exponent_audio_trig_audio>(dst).reset() }))
  vm.genPools.push(new GenPool(() => new Adsr_default_attack_audio_decay_audio_sustain_audio_release_audio_exponent_scalar_trig_scalar(), 501, 8, vm.genPoolManager, (dst: Object, src: Object) => {
    changetype<Adsr_default_attack_audio_decay_audio_sustain_audio_release_audio_exponent_scalar_trig_scalar>(dst).copyFrom(changetype<Adsr_default_attack_audio_decay_audio_sustain_audio_release_audio_exponent_scalar_trig_scalar>(src))
  }, (dst: Object) => { changetype<Adsr_default_attack_audio_decay_audio_sustain_audio_release_audio_exponent_scalar_trig_scalar>(dst).reset() }))
  vm.genPools.push(new GenPool(() => new Adsr_default_attack_audio_decay_audio_sustain_audio_release_audio_exponent_scalar_trig_audio(), 502, 8, vm.genPoolManager, (dst: Object, src: Object) => {
    changetype<Adsr_default_attack_audio_decay_audio_sustain_audio_release_audio_exponent_scalar_trig_audio>(dst).copyFrom(changetype<Adsr_default_attack_audio_decay_audio_sustain_audio_release_audio_exponent_scalar_trig_audio>(src))
  }, (dst: Object) => { changetype<Adsr_default_attack_audio_decay_audio_sustain_audio_release_audio_exponent_scalar_trig_audio>(dst).reset() }))
  vm.genPools.push(new GenPool(() => new Adsr_default_attack_audio_decay_audio_sustain_audio_release_audio_exponent_audio_trig_scalar(), 503, 8, vm.genPoolManager, (dst: Object, src: Object) => {
    changetype<Adsr_default_attack_audio_decay_audio_sustain_audio_release_audio_exponent_audio_trig_scalar>(dst).copyFrom(changetype<Adsr_default_attack_audio_decay_audio_sustain_audio_release_audio_exponent_audio_trig_scalar>(src))
  }, (dst: Object) => { changetype<Adsr_default_attack_audio_decay_audio_sustain_audio_release_audio_exponent_audio_trig_scalar>(dst).reset() }))
  vm.genPools.push(new GenPool(() => new Adsr_default_attack_audio_decay_audio_sustain_audio_release_audio_exponent_audio_trig_audio(), 504, 8, vm.genPoolManager, (dst: Object, src: Object) => {
    changetype<Adsr_default_attack_audio_decay_audio_sustain_audio_release_audio_exponent_audio_trig_audio>(dst).copyFrom(changetype<Adsr_default_attack_audio_decay_audio_sustain_audio_release_audio_exponent_audio_trig_audio>(src))
  }, (dst: Object) => { changetype<Adsr_default_attack_audio_decay_audio_sustain_audio_release_audio_exponent_audio_trig_audio>(dst).reset() }))
}

export function handleGenOp_Adsr(vm: VmState, op: AudioVmOp, pc: i32, opsPtr: usize, params: RunParams): i32 {
  switch (op) {
case AudioVmOp.GenAdsr_default: {
  genOpHelpers.resolveAndPushAbsolutePC(vm, pc)
  const trigTagged: f64 = vm.stack[--vm.stackTop]
  const exponentTagged: f64 = vm.stack[--vm.stackTop]
  const releaseTagged: f64 = vm.stack[--vm.stackTop]
  const sustainTagged: f64 = vm.stack[--vm.stackTop]
  const decayTagged: f64 = vm.stack[--vm.stackTop]
  const attackTagged: f64 = vm.stack[--vm.stackTop]
  let output: Float32Array = changetype<Float32Array>(0)
  let modeMask: i32 = 0
  if (isAudio(attackTagged) || genOpHelpers.isStereoAudioArray(vm, attackTagged)) {
    modeMask |= 1
  }
  if (isAudio(decayTagged) || genOpHelpers.isStereoAudioArray(vm, decayTagged)) {
    modeMask |= 2
  }
  if (isAudio(sustainTagged) || genOpHelpers.isStereoAudioArray(vm, sustainTagged)) {
    modeMask |= 4
  }
  if (isAudio(releaseTagged) || genOpHelpers.isStereoAudioArray(vm, releaseTagged)) {
    modeMask |= 8
  }
  if (isAudio(exponentTagged) || genOpHelpers.isStereoAudioArray(vm, exponentTagged)) {
    modeMask |= 16
  }
  if (isAudio(trigTagged) || genOpHelpers.isStereoAudioArray(vm, trigTagged)) {
    modeMask |= 32
  }
  const attackValue: f32 = genOpHelpers.scalarOrFirstSample(vm, attackTagged)
  vm.paramScratch[0] = attackValue
  const decayValue: f32 = genOpHelpers.scalarOrFirstSample(vm, decayTagged)
  vm.paramScratch[1] = decayValue
  const sustainValue: f32 = genOpHelpers.scalarOrFirstSample(vm, sustainTagged)
  vm.paramScratch[2] = sustainValue
  const releaseValue: f32 = genOpHelpers.scalarOrFirstSample(vm, releaseTagged)
  vm.paramScratch[3] = releaseValue
  const exponentValue: f32 = genOpHelpers.scalarOrFirstSample(vm, exponentTagged)
  vm.paramScratch[4] = exponentValue
  const trigValue: f32 = genOpHelpers.scalarOrFirstSample(vm, trigTagged)
  vm.paramScratch[5] = trigValue
  switch (modeMask) {
    case 0: {
      const slot: GenSlot = vm.genPools[441].get()
      genOpHelpers.writeCallStackMetaToSlot(vm, slot)
      const procLen: i32 = genOpHelpers.alignedProcLength(params.bufferLength)
      genOpHelpers.writeInputToHistoryRingZero(slot.history)
      const inputPtr: usize = 0
      output = vm.arena.get(procLen)
      const outputPtr: usize = output.dataStart
      const instance: Adsr_default_attack_scalar_decay_scalar_sustain_scalar_release_scalar_exponent_scalar_trig_scalar = changetype<Adsr_default_attack_scalar_decay_scalar_sustain_scalar_release_scalar_exponent_scalar_trig_scalar>(slot.instance)
      vm.paramScratch[6] = instance.stage
      vm.paramScratch[7] = instance.env
      const osFactor: i32 = genOpHelpers.getOversampleFactor(vm)
      if (osFactor > 0) {
        const baseLen: i32 = params.bufferLength / osFactor
        const baseSampleRate: f32 = params.sampleRate / f32(osFactor)
        const baseNyquist: f32 = baseSampleRate * 0.5
        const basePiOverNyquist: f32 = f32(Mathf.PI) / baseNyquist
        const baseSampleCount: f32 = f32(params.sampleCount) / f32(osFactor)
        const baseProcLen: i32 = (baseLen + 15) & ~15
        const baseOut: Float32Array = vm.arena.get(baseProcLen)
        const baseOutPtr: usize = baseOut.dataStart
        let baseInputPtr: usize = inputPtr
        slot.history.write(params.sampleCount / osFactor, vm.paramScratch)
        instance.process(baseLen, params.sampleCount / osFactor, baseSampleRate, baseNyquist, basePiOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, baseInputPtr, baseOutPtr, attackValue, decayValue, sustainValue, releaseValue, exponentValue, trigValue)
        upsample(vm, baseOutPtr, outputPtr, baseLen, osFactor)
        genOpHelpers.extendBufferWithLastSample(outputPtr, params.bufferLength, genOpHelpers.alignedProcLength(params.bufferLength))
        vm.arena.release(baseOut)
      } else {
        slot.history.write(params.sampleCount, vm.paramScratch)
        instance.process(params.bufferLength, params.sampleCount, params.sampleRate, params.nyquist, params.piOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, inputPtr, outputPtr, attackValue, decayValue, sustainValue, releaseValue, exponentValue, trigValue)
      }
      genOpHelpers.writeOutputToHistoryRing(slot.history, outputPtr, params.bufferLength)
      break
    }
    case 32: {
      const slot: GenSlot = vm.genPools[442].get()
      genOpHelpers.writeCallStackMetaToSlot(vm, slot)
      const procLen: i32 = genOpHelpers.alignedProcLength(params.bufferLength)
      genOpHelpers.writeInputToHistoryRingZero(slot.history)
      const inputPtr: usize = 0
      output = vm.arena.get(procLen)
      const outputPtr: usize = output.dataStart
      const instance: Adsr_default_attack_scalar_decay_scalar_sustain_scalar_release_scalar_exponent_scalar_trig_audio = changetype<Adsr_default_attack_scalar_decay_scalar_sustain_scalar_release_scalar_exponent_scalar_trig_audio>(slot.instance)
      vm.paramScratch[6] = instance.stage
      vm.paramScratch[7] = instance.env
      const trigAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, trigTagged, procLen)
      const osFactor: i32 = genOpHelpers.getOversampleFactor(vm)
      if (osFactor > 0) {
        const baseLen: i32 = params.bufferLength / osFactor
        const baseSampleRate: f32 = params.sampleRate / f32(osFactor)
        const baseNyquist: f32 = baseSampleRate * 0.5
        const basePiOverNyquist: f32 = f32(Mathf.PI) / baseNyquist
        const baseSampleCount: f32 = f32(params.sampleCount) / f32(osFactor)
        const baseProcLen: i32 = (baseLen + 15) & ~15
        const baseOut: Float32Array = vm.arena.get(baseProcLen)
        const baseOutPtr: usize = baseOut.dataStart
        let baseInputPtr: usize = inputPtr
        const trigSrc: usize = trigAudioResult.ptr
        const trigBuf: Float32Array = vm.arena.get(baseProcLen)
        const trigPtr: usize = trigBuf.dataStart
        downsample(vm, trigSrc, trigPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(trigPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(trigPtr + (usize(k) << 2), last)
          }
        }
        slot.history.write(params.sampleCount / osFactor, vm.paramScratch)
        instance.process(baseLen, params.sampleCount / osFactor, baseSampleRate, baseNyquist, basePiOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, baseInputPtr, baseOutPtr, attackValue, decayValue, sustainValue, releaseValue, exponentValue, trigPtr)
        upsample(vm, baseOutPtr, outputPtr, baseLen, osFactor)
        genOpHelpers.extendBufferWithLastSample(outputPtr, params.bufferLength, genOpHelpers.alignedProcLength(params.bufferLength))
        vm.arena.release(baseOut)
        vm.arena.release(trigBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      } else {
        slot.history.write(params.sampleCount, vm.paramScratch)
        instance.process(params.bufferLength, params.sampleCount, params.sampleRate, params.nyquist, params.piOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, inputPtr, outputPtr, attackValue, decayValue, sustainValue, releaseValue, exponentValue, trigAudioResult.ptr)
        genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      }
      genOpHelpers.writeOutputToHistoryRing(slot.history, outputPtr, params.bufferLength)
      genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      break
    }
    case 16: {
      const slot: GenSlot = vm.genPools[443].get()
      genOpHelpers.writeCallStackMetaToSlot(vm, slot)
      const procLen: i32 = genOpHelpers.alignedProcLength(params.bufferLength)
      genOpHelpers.writeInputToHistoryRingZero(slot.history)
      const inputPtr: usize = 0
      output = vm.arena.get(procLen)
      const outputPtr: usize = output.dataStart
      const instance: Adsr_default_attack_scalar_decay_scalar_sustain_scalar_release_scalar_exponent_audio_trig_scalar = changetype<Adsr_default_attack_scalar_decay_scalar_sustain_scalar_release_scalar_exponent_audio_trig_scalar>(slot.instance)
      vm.paramScratch[6] = instance.stage
      vm.paramScratch[7] = instance.env
      const exponentAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, exponentTagged, procLen)
      const osFactor: i32 = genOpHelpers.getOversampleFactor(vm)
      if (osFactor > 0) {
        const baseLen: i32 = params.bufferLength / osFactor
        const baseSampleRate: f32 = params.sampleRate / f32(osFactor)
        const baseNyquist: f32 = baseSampleRate * 0.5
        const basePiOverNyquist: f32 = f32(Mathf.PI) / baseNyquist
        const baseSampleCount: f32 = f32(params.sampleCount) / f32(osFactor)
        const baseProcLen: i32 = (baseLen + 15) & ~15
        const baseOut: Float32Array = vm.arena.get(baseProcLen)
        const baseOutPtr: usize = baseOut.dataStart
        let baseInputPtr: usize = inputPtr
        const exponentSrc: usize = exponentAudioResult.ptr
        const exponentBuf: Float32Array = vm.arena.get(baseProcLen)
        const exponentPtr: usize = exponentBuf.dataStart
        downsample(vm, exponentSrc, exponentPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(exponentPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(exponentPtr + (usize(k) << 2), last)
          }
        }
        slot.history.write(params.sampleCount / osFactor, vm.paramScratch)
        instance.process(baseLen, params.sampleCount / osFactor, baseSampleRate, baseNyquist, basePiOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, baseInputPtr, baseOutPtr, attackValue, decayValue, sustainValue, releaseValue, trigValue, exponentPtr)
        upsample(vm, baseOutPtr, outputPtr, baseLen, osFactor)
        genOpHelpers.extendBufferWithLastSample(outputPtr, params.bufferLength, genOpHelpers.alignedProcLength(params.bufferLength))
        vm.arena.release(baseOut)
        vm.arena.release(exponentBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
      } else {
        slot.history.write(params.sampleCount, vm.paramScratch)
        instance.process(params.bufferLength, params.sampleCount, params.sampleRate, params.nyquist, params.piOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, inputPtr, outputPtr, attackValue, decayValue, sustainValue, releaseValue, trigValue, exponentAudioResult.ptr)
        genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
      }
      genOpHelpers.writeOutputToHistoryRing(slot.history, outputPtr, params.bufferLength)
      genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
      break
    }
    case 48: {
      const slot: GenSlot = vm.genPools[444].get()
      genOpHelpers.writeCallStackMetaToSlot(vm, slot)
      const procLen: i32 = genOpHelpers.alignedProcLength(params.bufferLength)
      genOpHelpers.writeInputToHistoryRingZero(slot.history)
      const inputPtr: usize = 0
      output = vm.arena.get(procLen)
      const outputPtr: usize = output.dataStart
      const instance: Adsr_default_attack_scalar_decay_scalar_sustain_scalar_release_scalar_exponent_audio_trig_audio = changetype<Adsr_default_attack_scalar_decay_scalar_sustain_scalar_release_scalar_exponent_audio_trig_audio>(slot.instance)
      vm.paramScratch[6] = instance.stage
      vm.paramScratch[7] = instance.env
      const exponentAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, exponentTagged, procLen)
      const trigAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, trigTagged, procLen)
      const osFactor: i32 = genOpHelpers.getOversampleFactor(vm)
      if (osFactor > 0) {
        const baseLen: i32 = params.bufferLength / osFactor
        const baseSampleRate: f32 = params.sampleRate / f32(osFactor)
        const baseNyquist: f32 = baseSampleRate * 0.5
        const basePiOverNyquist: f32 = f32(Mathf.PI) / baseNyquist
        const baseSampleCount: f32 = f32(params.sampleCount) / f32(osFactor)
        const baseProcLen: i32 = (baseLen + 15) & ~15
        const baseOut: Float32Array = vm.arena.get(baseProcLen)
        const baseOutPtr: usize = baseOut.dataStart
        let baseInputPtr: usize = inputPtr
        const exponentSrc: usize = exponentAudioResult.ptr
        const exponentBuf: Float32Array = vm.arena.get(baseProcLen)
        const exponentPtr: usize = exponentBuf.dataStart
        downsample(vm, exponentSrc, exponentPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(exponentPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(exponentPtr + (usize(k) << 2), last)
          }
        }
        const trigSrc: usize = trigAudioResult.ptr
        const trigBuf: Float32Array = vm.arena.get(baseProcLen)
        const trigPtr: usize = trigBuf.dataStart
        downsample(vm, trigSrc, trigPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(trigPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(trigPtr + (usize(k) << 2), last)
          }
        }
        slot.history.write(params.sampleCount / osFactor, vm.paramScratch)
        instance.process(baseLen, params.sampleCount / osFactor, baseSampleRate, baseNyquist, basePiOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, baseInputPtr, baseOutPtr, attackValue, decayValue, sustainValue, releaseValue, exponentPtr, trigPtr)
        upsample(vm, baseOutPtr, outputPtr, baseLen, osFactor)
        genOpHelpers.extendBufferWithLastSample(outputPtr, params.bufferLength, genOpHelpers.alignedProcLength(params.bufferLength))
        vm.arena.release(baseOut)
        vm.arena.release(exponentBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
        vm.arena.release(trigBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      } else {
        slot.history.write(params.sampleCount, vm.paramScratch)
        instance.process(params.bufferLength, params.sampleCount, params.sampleRate, params.nyquist, params.piOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, inputPtr, outputPtr, attackValue, decayValue, sustainValue, releaseValue, exponentAudioResult.ptr, trigAudioResult.ptr)
        genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      }
      genOpHelpers.writeOutputToHistoryRing(slot.history, outputPtr, params.bufferLength)
      genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      break
    }
    case 8: {
      const slot: GenSlot = vm.genPools[445].get()
      genOpHelpers.writeCallStackMetaToSlot(vm, slot)
      const procLen: i32 = genOpHelpers.alignedProcLength(params.bufferLength)
      genOpHelpers.writeInputToHistoryRingZero(slot.history)
      const inputPtr: usize = 0
      output = vm.arena.get(procLen)
      const outputPtr: usize = output.dataStart
      const instance: Adsr_default_attack_scalar_decay_scalar_sustain_scalar_release_audio_exponent_scalar_trig_scalar = changetype<Adsr_default_attack_scalar_decay_scalar_sustain_scalar_release_audio_exponent_scalar_trig_scalar>(slot.instance)
      vm.paramScratch[6] = instance.stage
      vm.paramScratch[7] = instance.env
      const releaseAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, releaseTagged, procLen)
      const osFactor: i32 = genOpHelpers.getOversampleFactor(vm)
      if (osFactor > 0) {
        const baseLen: i32 = params.bufferLength / osFactor
        const baseSampleRate: f32 = params.sampleRate / f32(osFactor)
        const baseNyquist: f32 = baseSampleRate * 0.5
        const basePiOverNyquist: f32 = f32(Mathf.PI) / baseNyquist
        const baseSampleCount: f32 = f32(params.sampleCount) / f32(osFactor)
        const baseProcLen: i32 = (baseLen + 15) & ~15
        const baseOut: Float32Array = vm.arena.get(baseProcLen)
        const baseOutPtr: usize = baseOut.dataStart
        let baseInputPtr: usize = inputPtr
        const releaseSrc: usize = releaseAudioResult.ptr
        const releaseBuf: Float32Array = vm.arena.get(baseProcLen)
        const releasePtr: usize = releaseBuf.dataStart
        downsample(vm, releaseSrc, releasePtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(releasePtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(releasePtr + (usize(k) << 2), last)
          }
        }
        slot.history.write(params.sampleCount / osFactor, vm.paramScratch)
        instance.process(baseLen, params.sampleCount / osFactor, baseSampleRate, baseNyquist, basePiOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, baseInputPtr, baseOutPtr, attackValue, decayValue, sustainValue, exponentValue, trigValue, releasePtr)
        upsample(vm, baseOutPtr, outputPtr, baseLen, osFactor)
        genOpHelpers.extendBufferWithLastSample(outputPtr, params.bufferLength, genOpHelpers.alignedProcLength(params.bufferLength))
        vm.arena.release(baseOut)
        vm.arena.release(releaseBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
      } else {
        slot.history.write(params.sampleCount, vm.paramScratch)
        instance.process(params.bufferLength, params.sampleCount, params.sampleRate, params.nyquist, params.piOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, inputPtr, outputPtr, attackValue, decayValue, sustainValue, exponentValue, trigValue, releaseAudioResult.ptr)
        genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
      }
      genOpHelpers.writeOutputToHistoryRing(slot.history, outputPtr, params.bufferLength)
      genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
      break
    }
    case 40: {
      const slot: GenSlot = vm.genPools[446].get()
      genOpHelpers.writeCallStackMetaToSlot(vm, slot)
      const procLen: i32 = genOpHelpers.alignedProcLength(params.bufferLength)
      genOpHelpers.writeInputToHistoryRingZero(slot.history)
      const inputPtr: usize = 0
      output = vm.arena.get(procLen)
      const outputPtr: usize = output.dataStart
      const instance: Adsr_default_attack_scalar_decay_scalar_sustain_scalar_release_audio_exponent_scalar_trig_audio = changetype<Adsr_default_attack_scalar_decay_scalar_sustain_scalar_release_audio_exponent_scalar_trig_audio>(slot.instance)
      vm.paramScratch[6] = instance.stage
      vm.paramScratch[7] = instance.env
      const releaseAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, releaseTagged, procLen)
      const trigAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, trigTagged, procLen)
      const osFactor: i32 = genOpHelpers.getOversampleFactor(vm)
      if (osFactor > 0) {
        const baseLen: i32 = params.bufferLength / osFactor
        const baseSampleRate: f32 = params.sampleRate / f32(osFactor)
        const baseNyquist: f32 = baseSampleRate * 0.5
        const basePiOverNyquist: f32 = f32(Mathf.PI) / baseNyquist
        const baseSampleCount: f32 = f32(params.sampleCount) / f32(osFactor)
        const baseProcLen: i32 = (baseLen + 15) & ~15
        const baseOut: Float32Array = vm.arena.get(baseProcLen)
        const baseOutPtr: usize = baseOut.dataStart
        let baseInputPtr: usize = inputPtr
        const releaseSrc: usize = releaseAudioResult.ptr
        const releaseBuf: Float32Array = vm.arena.get(baseProcLen)
        const releasePtr: usize = releaseBuf.dataStart
        downsample(vm, releaseSrc, releasePtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(releasePtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(releasePtr + (usize(k) << 2), last)
          }
        }
        const trigSrc: usize = trigAudioResult.ptr
        const trigBuf: Float32Array = vm.arena.get(baseProcLen)
        const trigPtr: usize = trigBuf.dataStart
        downsample(vm, trigSrc, trigPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(trigPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(trigPtr + (usize(k) << 2), last)
          }
        }
        slot.history.write(params.sampleCount / osFactor, vm.paramScratch)
        instance.process(baseLen, params.sampleCount / osFactor, baseSampleRate, baseNyquist, basePiOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, baseInputPtr, baseOutPtr, attackValue, decayValue, sustainValue, exponentValue, releasePtr, trigPtr)
        upsample(vm, baseOutPtr, outputPtr, baseLen, osFactor)
        genOpHelpers.extendBufferWithLastSample(outputPtr, params.bufferLength, genOpHelpers.alignedProcLength(params.bufferLength))
        vm.arena.release(baseOut)
        vm.arena.release(releaseBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
        vm.arena.release(trigBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      } else {
        slot.history.write(params.sampleCount, vm.paramScratch)
        instance.process(params.bufferLength, params.sampleCount, params.sampleRate, params.nyquist, params.piOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, inputPtr, outputPtr, attackValue, decayValue, sustainValue, exponentValue, releaseAudioResult.ptr, trigAudioResult.ptr)
        genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      }
      genOpHelpers.writeOutputToHistoryRing(slot.history, outputPtr, params.bufferLength)
      genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      break
    }
    case 24: {
      const slot: GenSlot = vm.genPools[447].get()
      genOpHelpers.writeCallStackMetaToSlot(vm, slot)
      const procLen: i32 = genOpHelpers.alignedProcLength(params.bufferLength)
      genOpHelpers.writeInputToHistoryRingZero(slot.history)
      const inputPtr: usize = 0
      output = vm.arena.get(procLen)
      const outputPtr: usize = output.dataStart
      const instance: Adsr_default_attack_scalar_decay_scalar_sustain_scalar_release_audio_exponent_audio_trig_scalar = changetype<Adsr_default_attack_scalar_decay_scalar_sustain_scalar_release_audio_exponent_audio_trig_scalar>(slot.instance)
      vm.paramScratch[6] = instance.stage
      vm.paramScratch[7] = instance.env
      const releaseAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, releaseTagged, procLen)
      const exponentAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, exponentTagged, procLen)
      const osFactor: i32 = genOpHelpers.getOversampleFactor(vm)
      if (osFactor > 0) {
        const baseLen: i32 = params.bufferLength / osFactor
        const baseSampleRate: f32 = params.sampleRate / f32(osFactor)
        const baseNyquist: f32 = baseSampleRate * 0.5
        const basePiOverNyquist: f32 = f32(Mathf.PI) / baseNyquist
        const baseSampleCount: f32 = f32(params.sampleCount) / f32(osFactor)
        const baseProcLen: i32 = (baseLen + 15) & ~15
        const baseOut: Float32Array = vm.arena.get(baseProcLen)
        const baseOutPtr: usize = baseOut.dataStart
        let baseInputPtr: usize = inputPtr
        const releaseSrc: usize = releaseAudioResult.ptr
        const releaseBuf: Float32Array = vm.arena.get(baseProcLen)
        const releasePtr: usize = releaseBuf.dataStart
        downsample(vm, releaseSrc, releasePtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(releasePtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(releasePtr + (usize(k) << 2), last)
          }
        }
        const exponentSrc: usize = exponentAudioResult.ptr
        const exponentBuf: Float32Array = vm.arena.get(baseProcLen)
        const exponentPtr: usize = exponentBuf.dataStart
        downsample(vm, exponentSrc, exponentPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(exponentPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(exponentPtr + (usize(k) << 2), last)
          }
        }
        slot.history.write(params.sampleCount / osFactor, vm.paramScratch)
        instance.process(baseLen, params.sampleCount / osFactor, baseSampleRate, baseNyquist, basePiOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, baseInputPtr, baseOutPtr, attackValue, decayValue, sustainValue, trigValue, releasePtr, exponentPtr)
        upsample(vm, baseOutPtr, outputPtr, baseLen, osFactor)
        genOpHelpers.extendBufferWithLastSample(outputPtr, params.bufferLength, genOpHelpers.alignedProcLength(params.bufferLength))
        vm.arena.release(baseOut)
        vm.arena.release(releaseBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
        vm.arena.release(exponentBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
      } else {
        slot.history.write(params.sampleCount, vm.paramScratch)
        instance.process(params.bufferLength, params.sampleCount, params.sampleRate, params.nyquist, params.piOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, inputPtr, outputPtr, attackValue, decayValue, sustainValue, trigValue, releaseAudioResult.ptr, exponentAudioResult.ptr)
        genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
      }
      genOpHelpers.writeOutputToHistoryRing(slot.history, outputPtr, params.bufferLength)
      genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
      break
    }
    case 56: {
      const slot: GenSlot = vm.genPools[448].get()
      genOpHelpers.writeCallStackMetaToSlot(vm, slot)
      const procLen: i32 = genOpHelpers.alignedProcLength(params.bufferLength)
      genOpHelpers.writeInputToHistoryRingZero(slot.history)
      const inputPtr: usize = 0
      output = vm.arena.get(procLen)
      const outputPtr: usize = output.dataStart
      const instance: Adsr_default_attack_scalar_decay_scalar_sustain_scalar_release_audio_exponent_audio_trig_audio = changetype<Adsr_default_attack_scalar_decay_scalar_sustain_scalar_release_audio_exponent_audio_trig_audio>(slot.instance)
      vm.paramScratch[6] = instance.stage
      vm.paramScratch[7] = instance.env
      const releaseAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, releaseTagged, procLen)
      const exponentAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, exponentTagged, procLen)
      const trigAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, trigTagged, procLen)
      const osFactor: i32 = genOpHelpers.getOversampleFactor(vm)
      if (osFactor > 0) {
        const baseLen: i32 = params.bufferLength / osFactor
        const baseSampleRate: f32 = params.sampleRate / f32(osFactor)
        const baseNyquist: f32 = baseSampleRate * 0.5
        const basePiOverNyquist: f32 = f32(Mathf.PI) / baseNyquist
        const baseSampleCount: f32 = f32(params.sampleCount) / f32(osFactor)
        const baseProcLen: i32 = (baseLen + 15) & ~15
        const baseOut: Float32Array = vm.arena.get(baseProcLen)
        const baseOutPtr: usize = baseOut.dataStart
        let baseInputPtr: usize = inputPtr
        const releaseSrc: usize = releaseAudioResult.ptr
        const releaseBuf: Float32Array = vm.arena.get(baseProcLen)
        const releasePtr: usize = releaseBuf.dataStart
        downsample(vm, releaseSrc, releasePtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(releasePtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(releasePtr + (usize(k) << 2), last)
          }
        }
        const exponentSrc: usize = exponentAudioResult.ptr
        const exponentBuf: Float32Array = vm.arena.get(baseProcLen)
        const exponentPtr: usize = exponentBuf.dataStart
        downsample(vm, exponentSrc, exponentPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(exponentPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(exponentPtr + (usize(k) << 2), last)
          }
        }
        const trigSrc: usize = trigAudioResult.ptr
        const trigBuf: Float32Array = vm.arena.get(baseProcLen)
        const trigPtr: usize = trigBuf.dataStart
        downsample(vm, trigSrc, trigPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(trigPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(trigPtr + (usize(k) << 2), last)
          }
        }
        slot.history.write(params.sampleCount / osFactor, vm.paramScratch)
        instance.process(baseLen, params.sampleCount / osFactor, baseSampleRate, baseNyquist, basePiOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, baseInputPtr, baseOutPtr, attackValue, decayValue, sustainValue, releasePtr, exponentPtr, trigPtr)
        upsample(vm, baseOutPtr, outputPtr, baseLen, osFactor)
        genOpHelpers.extendBufferWithLastSample(outputPtr, params.bufferLength, genOpHelpers.alignedProcLength(params.bufferLength))
        vm.arena.release(baseOut)
        vm.arena.release(releaseBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
        vm.arena.release(exponentBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
        vm.arena.release(trigBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      } else {
        slot.history.write(params.sampleCount, vm.paramScratch)
        instance.process(params.bufferLength, params.sampleCount, params.sampleRate, params.nyquist, params.piOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, inputPtr, outputPtr, attackValue, decayValue, sustainValue, releaseAudioResult.ptr, exponentAudioResult.ptr, trigAudioResult.ptr)
        genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      }
      genOpHelpers.writeOutputToHistoryRing(slot.history, outputPtr, params.bufferLength)
      genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      break
    }
    case 4: {
      const slot: GenSlot = vm.genPools[449].get()
      genOpHelpers.writeCallStackMetaToSlot(vm, slot)
      const procLen: i32 = genOpHelpers.alignedProcLength(params.bufferLength)
      genOpHelpers.writeInputToHistoryRingZero(slot.history)
      const inputPtr: usize = 0
      output = vm.arena.get(procLen)
      const outputPtr: usize = output.dataStart
      const instance: Adsr_default_attack_scalar_decay_scalar_sustain_audio_release_scalar_exponent_scalar_trig_scalar = changetype<Adsr_default_attack_scalar_decay_scalar_sustain_audio_release_scalar_exponent_scalar_trig_scalar>(slot.instance)
      vm.paramScratch[6] = instance.stage
      vm.paramScratch[7] = instance.env
      const sustainAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, sustainTagged, procLen)
      const osFactor: i32 = genOpHelpers.getOversampleFactor(vm)
      if (osFactor > 0) {
        const baseLen: i32 = params.bufferLength / osFactor
        const baseSampleRate: f32 = params.sampleRate / f32(osFactor)
        const baseNyquist: f32 = baseSampleRate * 0.5
        const basePiOverNyquist: f32 = f32(Mathf.PI) / baseNyquist
        const baseSampleCount: f32 = f32(params.sampleCount) / f32(osFactor)
        const baseProcLen: i32 = (baseLen + 15) & ~15
        const baseOut: Float32Array = vm.arena.get(baseProcLen)
        const baseOutPtr: usize = baseOut.dataStart
        let baseInputPtr: usize = inputPtr
        const sustainSrc: usize = sustainAudioResult.ptr
        const sustainBuf: Float32Array = vm.arena.get(baseProcLen)
        const sustainPtr: usize = sustainBuf.dataStart
        downsample(vm, sustainSrc, sustainPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(sustainPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(sustainPtr + (usize(k) << 2), last)
          }
        }
        slot.history.write(params.sampleCount / osFactor, vm.paramScratch)
        instance.process(baseLen, params.sampleCount / osFactor, baseSampleRate, baseNyquist, basePiOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, baseInputPtr, baseOutPtr, attackValue, decayValue, releaseValue, exponentValue, trigValue, sustainPtr)
        upsample(vm, baseOutPtr, outputPtr, baseLen, osFactor)
        genOpHelpers.extendBufferWithLastSample(outputPtr, params.bufferLength, genOpHelpers.alignedProcLength(params.bufferLength))
        vm.arena.release(baseOut)
        vm.arena.release(sustainBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
      } else {
        slot.history.write(params.sampleCount, vm.paramScratch)
        instance.process(params.bufferLength, params.sampleCount, params.sampleRate, params.nyquist, params.piOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, inputPtr, outputPtr, attackValue, decayValue, releaseValue, exponentValue, trigValue, sustainAudioResult.ptr)
        genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
      }
      genOpHelpers.writeOutputToHistoryRing(slot.history, outputPtr, params.bufferLength)
      genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
      break
    }
    case 36: {
      const slot: GenSlot = vm.genPools[450].get()
      genOpHelpers.writeCallStackMetaToSlot(vm, slot)
      const procLen: i32 = genOpHelpers.alignedProcLength(params.bufferLength)
      genOpHelpers.writeInputToHistoryRingZero(slot.history)
      const inputPtr: usize = 0
      output = vm.arena.get(procLen)
      const outputPtr: usize = output.dataStart
      const instance: Adsr_default_attack_scalar_decay_scalar_sustain_audio_release_scalar_exponent_scalar_trig_audio = changetype<Adsr_default_attack_scalar_decay_scalar_sustain_audio_release_scalar_exponent_scalar_trig_audio>(slot.instance)
      vm.paramScratch[6] = instance.stage
      vm.paramScratch[7] = instance.env
      const sustainAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, sustainTagged, procLen)
      const trigAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, trigTagged, procLen)
      const osFactor: i32 = genOpHelpers.getOversampleFactor(vm)
      if (osFactor > 0) {
        const baseLen: i32 = params.bufferLength / osFactor
        const baseSampleRate: f32 = params.sampleRate / f32(osFactor)
        const baseNyquist: f32 = baseSampleRate * 0.5
        const basePiOverNyquist: f32 = f32(Mathf.PI) / baseNyquist
        const baseSampleCount: f32 = f32(params.sampleCount) / f32(osFactor)
        const baseProcLen: i32 = (baseLen + 15) & ~15
        const baseOut: Float32Array = vm.arena.get(baseProcLen)
        const baseOutPtr: usize = baseOut.dataStart
        let baseInputPtr: usize = inputPtr
        const sustainSrc: usize = sustainAudioResult.ptr
        const sustainBuf: Float32Array = vm.arena.get(baseProcLen)
        const sustainPtr: usize = sustainBuf.dataStart
        downsample(vm, sustainSrc, sustainPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(sustainPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(sustainPtr + (usize(k) << 2), last)
          }
        }
        const trigSrc: usize = trigAudioResult.ptr
        const trigBuf: Float32Array = vm.arena.get(baseProcLen)
        const trigPtr: usize = trigBuf.dataStart
        downsample(vm, trigSrc, trigPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(trigPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(trigPtr + (usize(k) << 2), last)
          }
        }
        slot.history.write(params.sampleCount / osFactor, vm.paramScratch)
        instance.process(baseLen, params.sampleCount / osFactor, baseSampleRate, baseNyquist, basePiOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, baseInputPtr, baseOutPtr, attackValue, decayValue, releaseValue, exponentValue, sustainPtr, trigPtr)
        upsample(vm, baseOutPtr, outputPtr, baseLen, osFactor)
        genOpHelpers.extendBufferWithLastSample(outputPtr, params.bufferLength, genOpHelpers.alignedProcLength(params.bufferLength))
        vm.arena.release(baseOut)
        vm.arena.release(sustainBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
        vm.arena.release(trigBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      } else {
        slot.history.write(params.sampleCount, vm.paramScratch)
        instance.process(params.bufferLength, params.sampleCount, params.sampleRate, params.nyquist, params.piOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, inputPtr, outputPtr, attackValue, decayValue, releaseValue, exponentValue, sustainAudioResult.ptr, trigAudioResult.ptr)
        genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      }
      genOpHelpers.writeOutputToHistoryRing(slot.history, outputPtr, params.bufferLength)
      genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      break
    }
    case 20: {
      const slot: GenSlot = vm.genPools[451].get()
      genOpHelpers.writeCallStackMetaToSlot(vm, slot)
      const procLen: i32 = genOpHelpers.alignedProcLength(params.bufferLength)
      genOpHelpers.writeInputToHistoryRingZero(slot.history)
      const inputPtr: usize = 0
      output = vm.arena.get(procLen)
      const outputPtr: usize = output.dataStart
      const instance: Adsr_default_attack_scalar_decay_scalar_sustain_audio_release_scalar_exponent_audio_trig_scalar = changetype<Adsr_default_attack_scalar_decay_scalar_sustain_audio_release_scalar_exponent_audio_trig_scalar>(slot.instance)
      vm.paramScratch[6] = instance.stage
      vm.paramScratch[7] = instance.env
      const sustainAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, sustainTagged, procLen)
      const exponentAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, exponentTagged, procLen)
      const osFactor: i32 = genOpHelpers.getOversampleFactor(vm)
      if (osFactor > 0) {
        const baseLen: i32 = params.bufferLength / osFactor
        const baseSampleRate: f32 = params.sampleRate / f32(osFactor)
        const baseNyquist: f32 = baseSampleRate * 0.5
        const basePiOverNyquist: f32 = f32(Mathf.PI) / baseNyquist
        const baseSampleCount: f32 = f32(params.sampleCount) / f32(osFactor)
        const baseProcLen: i32 = (baseLen + 15) & ~15
        const baseOut: Float32Array = vm.arena.get(baseProcLen)
        const baseOutPtr: usize = baseOut.dataStart
        let baseInputPtr: usize = inputPtr
        const sustainSrc: usize = sustainAudioResult.ptr
        const sustainBuf: Float32Array = vm.arena.get(baseProcLen)
        const sustainPtr: usize = sustainBuf.dataStart
        downsample(vm, sustainSrc, sustainPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(sustainPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(sustainPtr + (usize(k) << 2), last)
          }
        }
        const exponentSrc: usize = exponentAudioResult.ptr
        const exponentBuf: Float32Array = vm.arena.get(baseProcLen)
        const exponentPtr: usize = exponentBuf.dataStart
        downsample(vm, exponentSrc, exponentPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(exponentPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(exponentPtr + (usize(k) << 2), last)
          }
        }
        slot.history.write(params.sampleCount / osFactor, vm.paramScratch)
        instance.process(baseLen, params.sampleCount / osFactor, baseSampleRate, baseNyquist, basePiOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, baseInputPtr, baseOutPtr, attackValue, decayValue, releaseValue, trigValue, sustainPtr, exponentPtr)
        upsample(vm, baseOutPtr, outputPtr, baseLen, osFactor)
        genOpHelpers.extendBufferWithLastSample(outputPtr, params.bufferLength, genOpHelpers.alignedProcLength(params.bufferLength))
        vm.arena.release(baseOut)
        vm.arena.release(sustainBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
        vm.arena.release(exponentBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
      } else {
        slot.history.write(params.sampleCount, vm.paramScratch)
        instance.process(params.bufferLength, params.sampleCount, params.sampleRate, params.nyquist, params.piOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, inputPtr, outputPtr, attackValue, decayValue, releaseValue, trigValue, sustainAudioResult.ptr, exponentAudioResult.ptr)
        genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
      }
      genOpHelpers.writeOutputToHistoryRing(slot.history, outputPtr, params.bufferLength)
      genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
      break
    }
    case 52: {
      const slot: GenSlot = vm.genPools[452].get()
      genOpHelpers.writeCallStackMetaToSlot(vm, slot)
      const procLen: i32 = genOpHelpers.alignedProcLength(params.bufferLength)
      genOpHelpers.writeInputToHistoryRingZero(slot.history)
      const inputPtr: usize = 0
      output = vm.arena.get(procLen)
      const outputPtr: usize = output.dataStart
      const instance: Adsr_default_attack_scalar_decay_scalar_sustain_audio_release_scalar_exponent_audio_trig_audio = changetype<Adsr_default_attack_scalar_decay_scalar_sustain_audio_release_scalar_exponent_audio_trig_audio>(slot.instance)
      vm.paramScratch[6] = instance.stage
      vm.paramScratch[7] = instance.env
      const sustainAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, sustainTagged, procLen)
      const exponentAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, exponentTagged, procLen)
      const trigAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, trigTagged, procLen)
      const osFactor: i32 = genOpHelpers.getOversampleFactor(vm)
      if (osFactor > 0) {
        const baseLen: i32 = params.bufferLength / osFactor
        const baseSampleRate: f32 = params.sampleRate / f32(osFactor)
        const baseNyquist: f32 = baseSampleRate * 0.5
        const basePiOverNyquist: f32 = f32(Mathf.PI) / baseNyquist
        const baseSampleCount: f32 = f32(params.sampleCount) / f32(osFactor)
        const baseProcLen: i32 = (baseLen + 15) & ~15
        const baseOut: Float32Array = vm.arena.get(baseProcLen)
        const baseOutPtr: usize = baseOut.dataStart
        let baseInputPtr: usize = inputPtr
        const sustainSrc: usize = sustainAudioResult.ptr
        const sustainBuf: Float32Array = vm.arena.get(baseProcLen)
        const sustainPtr: usize = sustainBuf.dataStart
        downsample(vm, sustainSrc, sustainPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(sustainPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(sustainPtr + (usize(k) << 2), last)
          }
        }
        const exponentSrc: usize = exponentAudioResult.ptr
        const exponentBuf: Float32Array = vm.arena.get(baseProcLen)
        const exponentPtr: usize = exponentBuf.dataStart
        downsample(vm, exponentSrc, exponentPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(exponentPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(exponentPtr + (usize(k) << 2), last)
          }
        }
        const trigSrc: usize = trigAudioResult.ptr
        const trigBuf: Float32Array = vm.arena.get(baseProcLen)
        const trigPtr: usize = trigBuf.dataStart
        downsample(vm, trigSrc, trigPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(trigPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(trigPtr + (usize(k) << 2), last)
          }
        }
        slot.history.write(params.sampleCount / osFactor, vm.paramScratch)
        instance.process(baseLen, params.sampleCount / osFactor, baseSampleRate, baseNyquist, basePiOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, baseInputPtr, baseOutPtr, attackValue, decayValue, releaseValue, sustainPtr, exponentPtr, trigPtr)
        upsample(vm, baseOutPtr, outputPtr, baseLen, osFactor)
        genOpHelpers.extendBufferWithLastSample(outputPtr, params.bufferLength, genOpHelpers.alignedProcLength(params.bufferLength))
        vm.arena.release(baseOut)
        vm.arena.release(sustainBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
        vm.arena.release(exponentBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
        vm.arena.release(trigBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      } else {
        slot.history.write(params.sampleCount, vm.paramScratch)
        instance.process(params.bufferLength, params.sampleCount, params.sampleRate, params.nyquist, params.piOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, inputPtr, outputPtr, attackValue, decayValue, releaseValue, sustainAudioResult.ptr, exponentAudioResult.ptr, trigAudioResult.ptr)
        genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      }
      genOpHelpers.writeOutputToHistoryRing(slot.history, outputPtr, params.bufferLength)
      genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      break
    }
    case 12: {
      const slot: GenSlot = vm.genPools[453].get()
      genOpHelpers.writeCallStackMetaToSlot(vm, slot)
      const procLen: i32 = genOpHelpers.alignedProcLength(params.bufferLength)
      genOpHelpers.writeInputToHistoryRingZero(slot.history)
      const inputPtr: usize = 0
      output = vm.arena.get(procLen)
      const outputPtr: usize = output.dataStart
      const instance: Adsr_default_attack_scalar_decay_scalar_sustain_audio_release_audio_exponent_scalar_trig_scalar = changetype<Adsr_default_attack_scalar_decay_scalar_sustain_audio_release_audio_exponent_scalar_trig_scalar>(slot.instance)
      vm.paramScratch[6] = instance.stage
      vm.paramScratch[7] = instance.env
      const sustainAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, sustainTagged, procLen)
      const releaseAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, releaseTagged, procLen)
      const osFactor: i32 = genOpHelpers.getOversampleFactor(vm)
      if (osFactor > 0) {
        const baseLen: i32 = params.bufferLength / osFactor
        const baseSampleRate: f32 = params.sampleRate / f32(osFactor)
        const baseNyquist: f32 = baseSampleRate * 0.5
        const basePiOverNyquist: f32 = f32(Mathf.PI) / baseNyquist
        const baseSampleCount: f32 = f32(params.sampleCount) / f32(osFactor)
        const baseProcLen: i32 = (baseLen + 15) & ~15
        const baseOut: Float32Array = vm.arena.get(baseProcLen)
        const baseOutPtr: usize = baseOut.dataStart
        let baseInputPtr: usize = inputPtr
        const sustainSrc: usize = sustainAudioResult.ptr
        const sustainBuf: Float32Array = vm.arena.get(baseProcLen)
        const sustainPtr: usize = sustainBuf.dataStart
        downsample(vm, sustainSrc, sustainPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(sustainPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(sustainPtr + (usize(k) << 2), last)
          }
        }
        const releaseSrc: usize = releaseAudioResult.ptr
        const releaseBuf: Float32Array = vm.arena.get(baseProcLen)
        const releasePtr: usize = releaseBuf.dataStart
        downsample(vm, releaseSrc, releasePtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(releasePtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(releasePtr + (usize(k) << 2), last)
          }
        }
        slot.history.write(params.sampleCount / osFactor, vm.paramScratch)
        instance.process(baseLen, params.sampleCount / osFactor, baseSampleRate, baseNyquist, basePiOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, baseInputPtr, baseOutPtr, attackValue, decayValue, exponentValue, trigValue, sustainPtr, releasePtr)
        upsample(vm, baseOutPtr, outputPtr, baseLen, osFactor)
        genOpHelpers.extendBufferWithLastSample(outputPtr, params.bufferLength, genOpHelpers.alignedProcLength(params.bufferLength))
        vm.arena.release(baseOut)
        vm.arena.release(sustainBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
        vm.arena.release(releaseBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
      } else {
        slot.history.write(params.sampleCount, vm.paramScratch)
        instance.process(params.bufferLength, params.sampleCount, params.sampleRate, params.nyquist, params.piOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, inputPtr, outputPtr, attackValue, decayValue, exponentValue, trigValue, sustainAudioResult.ptr, releaseAudioResult.ptr)
        genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
      }
      genOpHelpers.writeOutputToHistoryRing(slot.history, outputPtr, params.bufferLength)
      genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
      break
    }
    case 44: {
      const slot: GenSlot = vm.genPools[454].get()
      genOpHelpers.writeCallStackMetaToSlot(vm, slot)
      const procLen: i32 = genOpHelpers.alignedProcLength(params.bufferLength)
      genOpHelpers.writeInputToHistoryRingZero(slot.history)
      const inputPtr: usize = 0
      output = vm.arena.get(procLen)
      const outputPtr: usize = output.dataStart
      const instance: Adsr_default_attack_scalar_decay_scalar_sustain_audio_release_audio_exponent_scalar_trig_audio = changetype<Adsr_default_attack_scalar_decay_scalar_sustain_audio_release_audio_exponent_scalar_trig_audio>(slot.instance)
      vm.paramScratch[6] = instance.stage
      vm.paramScratch[7] = instance.env
      const sustainAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, sustainTagged, procLen)
      const releaseAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, releaseTagged, procLen)
      const trigAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, trigTagged, procLen)
      const osFactor: i32 = genOpHelpers.getOversampleFactor(vm)
      if (osFactor > 0) {
        const baseLen: i32 = params.bufferLength / osFactor
        const baseSampleRate: f32 = params.sampleRate / f32(osFactor)
        const baseNyquist: f32 = baseSampleRate * 0.5
        const basePiOverNyquist: f32 = f32(Mathf.PI) / baseNyquist
        const baseSampleCount: f32 = f32(params.sampleCount) / f32(osFactor)
        const baseProcLen: i32 = (baseLen + 15) & ~15
        const baseOut: Float32Array = vm.arena.get(baseProcLen)
        const baseOutPtr: usize = baseOut.dataStart
        let baseInputPtr: usize = inputPtr
        const sustainSrc: usize = sustainAudioResult.ptr
        const sustainBuf: Float32Array = vm.arena.get(baseProcLen)
        const sustainPtr: usize = sustainBuf.dataStart
        downsample(vm, sustainSrc, sustainPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(sustainPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(sustainPtr + (usize(k) << 2), last)
          }
        }
        const releaseSrc: usize = releaseAudioResult.ptr
        const releaseBuf: Float32Array = vm.arena.get(baseProcLen)
        const releasePtr: usize = releaseBuf.dataStart
        downsample(vm, releaseSrc, releasePtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(releasePtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(releasePtr + (usize(k) << 2), last)
          }
        }
        const trigSrc: usize = trigAudioResult.ptr
        const trigBuf: Float32Array = vm.arena.get(baseProcLen)
        const trigPtr: usize = trigBuf.dataStart
        downsample(vm, trigSrc, trigPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(trigPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(trigPtr + (usize(k) << 2), last)
          }
        }
        slot.history.write(params.sampleCount / osFactor, vm.paramScratch)
        instance.process(baseLen, params.sampleCount / osFactor, baseSampleRate, baseNyquist, basePiOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, baseInputPtr, baseOutPtr, attackValue, decayValue, exponentValue, sustainPtr, releasePtr, trigPtr)
        upsample(vm, baseOutPtr, outputPtr, baseLen, osFactor)
        genOpHelpers.extendBufferWithLastSample(outputPtr, params.bufferLength, genOpHelpers.alignedProcLength(params.bufferLength))
        vm.arena.release(baseOut)
        vm.arena.release(sustainBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
        vm.arena.release(releaseBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
        vm.arena.release(trigBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      } else {
        slot.history.write(params.sampleCount, vm.paramScratch)
        instance.process(params.bufferLength, params.sampleCount, params.sampleRate, params.nyquist, params.piOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, inputPtr, outputPtr, attackValue, decayValue, exponentValue, sustainAudioResult.ptr, releaseAudioResult.ptr, trigAudioResult.ptr)
        genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      }
      genOpHelpers.writeOutputToHistoryRing(slot.history, outputPtr, params.bufferLength)
      genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      break
    }
    case 28: {
      const slot: GenSlot = vm.genPools[455].get()
      genOpHelpers.writeCallStackMetaToSlot(vm, slot)
      const procLen: i32 = genOpHelpers.alignedProcLength(params.bufferLength)
      genOpHelpers.writeInputToHistoryRingZero(slot.history)
      const inputPtr: usize = 0
      output = vm.arena.get(procLen)
      const outputPtr: usize = output.dataStart
      const instance: Adsr_default_attack_scalar_decay_scalar_sustain_audio_release_audio_exponent_audio_trig_scalar = changetype<Adsr_default_attack_scalar_decay_scalar_sustain_audio_release_audio_exponent_audio_trig_scalar>(slot.instance)
      vm.paramScratch[6] = instance.stage
      vm.paramScratch[7] = instance.env
      const sustainAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, sustainTagged, procLen)
      const releaseAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, releaseTagged, procLen)
      const exponentAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, exponentTagged, procLen)
      const osFactor: i32 = genOpHelpers.getOversampleFactor(vm)
      if (osFactor > 0) {
        const baseLen: i32 = params.bufferLength / osFactor
        const baseSampleRate: f32 = params.sampleRate / f32(osFactor)
        const baseNyquist: f32 = baseSampleRate * 0.5
        const basePiOverNyquist: f32 = f32(Mathf.PI) / baseNyquist
        const baseSampleCount: f32 = f32(params.sampleCount) / f32(osFactor)
        const baseProcLen: i32 = (baseLen + 15) & ~15
        const baseOut: Float32Array = vm.arena.get(baseProcLen)
        const baseOutPtr: usize = baseOut.dataStart
        let baseInputPtr: usize = inputPtr
        const sustainSrc: usize = sustainAudioResult.ptr
        const sustainBuf: Float32Array = vm.arena.get(baseProcLen)
        const sustainPtr: usize = sustainBuf.dataStart
        downsample(vm, sustainSrc, sustainPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(sustainPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(sustainPtr + (usize(k) << 2), last)
          }
        }
        const releaseSrc: usize = releaseAudioResult.ptr
        const releaseBuf: Float32Array = vm.arena.get(baseProcLen)
        const releasePtr: usize = releaseBuf.dataStart
        downsample(vm, releaseSrc, releasePtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(releasePtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(releasePtr + (usize(k) << 2), last)
          }
        }
        const exponentSrc: usize = exponentAudioResult.ptr
        const exponentBuf: Float32Array = vm.arena.get(baseProcLen)
        const exponentPtr: usize = exponentBuf.dataStart
        downsample(vm, exponentSrc, exponentPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(exponentPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(exponentPtr + (usize(k) << 2), last)
          }
        }
        slot.history.write(params.sampleCount / osFactor, vm.paramScratch)
        instance.process(baseLen, params.sampleCount / osFactor, baseSampleRate, baseNyquist, basePiOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, baseInputPtr, baseOutPtr, attackValue, decayValue, trigValue, sustainPtr, releasePtr, exponentPtr)
        upsample(vm, baseOutPtr, outputPtr, baseLen, osFactor)
        genOpHelpers.extendBufferWithLastSample(outputPtr, params.bufferLength, genOpHelpers.alignedProcLength(params.bufferLength))
        vm.arena.release(baseOut)
        vm.arena.release(sustainBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
        vm.arena.release(releaseBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
        vm.arena.release(exponentBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
      } else {
        slot.history.write(params.sampleCount, vm.paramScratch)
        instance.process(params.bufferLength, params.sampleCount, params.sampleRate, params.nyquist, params.piOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, inputPtr, outputPtr, attackValue, decayValue, trigValue, sustainAudioResult.ptr, releaseAudioResult.ptr, exponentAudioResult.ptr)
        genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
      }
      genOpHelpers.writeOutputToHistoryRing(slot.history, outputPtr, params.bufferLength)
      genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
      break
    }
    case 60: {
      const slot: GenSlot = vm.genPools[456].get()
      genOpHelpers.writeCallStackMetaToSlot(vm, slot)
      const procLen: i32 = genOpHelpers.alignedProcLength(params.bufferLength)
      genOpHelpers.writeInputToHistoryRingZero(slot.history)
      const inputPtr: usize = 0
      output = vm.arena.get(procLen)
      const outputPtr: usize = output.dataStart
      const instance: Adsr_default_attack_scalar_decay_scalar_sustain_audio_release_audio_exponent_audio_trig_audio = changetype<Adsr_default_attack_scalar_decay_scalar_sustain_audio_release_audio_exponent_audio_trig_audio>(slot.instance)
      vm.paramScratch[6] = instance.stage
      vm.paramScratch[7] = instance.env
      const sustainAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, sustainTagged, procLen)
      const releaseAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, releaseTagged, procLen)
      const exponentAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, exponentTagged, procLen)
      const trigAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, trigTagged, procLen)
      const osFactor: i32 = genOpHelpers.getOversampleFactor(vm)
      if (osFactor > 0) {
        const baseLen: i32 = params.bufferLength / osFactor
        const baseSampleRate: f32 = params.sampleRate / f32(osFactor)
        const baseNyquist: f32 = baseSampleRate * 0.5
        const basePiOverNyquist: f32 = f32(Mathf.PI) / baseNyquist
        const baseSampleCount: f32 = f32(params.sampleCount) / f32(osFactor)
        const baseProcLen: i32 = (baseLen + 15) & ~15
        const baseOut: Float32Array = vm.arena.get(baseProcLen)
        const baseOutPtr: usize = baseOut.dataStart
        let baseInputPtr: usize = inputPtr
        const sustainSrc: usize = sustainAudioResult.ptr
        const sustainBuf: Float32Array = vm.arena.get(baseProcLen)
        const sustainPtr: usize = sustainBuf.dataStart
        downsample(vm, sustainSrc, sustainPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(sustainPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(sustainPtr + (usize(k) << 2), last)
          }
        }
        const releaseSrc: usize = releaseAudioResult.ptr
        const releaseBuf: Float32Array = vm.arena.get(baseProcLen)
        const releasePtr: usize = releaseBuf.dataStart
        downsample(vm, releaseSrc, releasePtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(releasePtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(releasePtr + (usize(k) << 2), last)
          }
        }
        const exponentSrc: usize = exponentAudioResult.ptr
        const exponentBuf: Float32Array = vm.arena.get(baseProcLen)
        const exponentPtr: usize = exponentBuf.dataStart
        downsample(vm, exponentSrc, exponentPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(exponentPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(exponentPtr + (usize(k) << 2), last)
          }
        }
        const trigSrc: usize = trigAudioResult.ptr
        const trigBuf: Float32Array = vm.arena.get(baseProcLen)
        const trigPtr: usize = trigBuf.dataStart
        downsample(vm, trigSrc, trigPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(trigPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(trigPtr + (usize(k) << 2), last)
          }
        }
        slot.history.write(params.sampleCount / osFactor, vm.paramScratch)
        instance.process(baseLen, params.sampleCount / osFactor, baseSampleRate, baseNyquist, basePiOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, baseInputPtr, baseOutPtr, attackValue, decayValue, sustainPtr, releasePtr, exponentPtr, trigPtr)
        upsample(vm, baseOutPtr, outputPtr, baseLen, osFactor)
        genOpHelpers.extendBufferWithLastSample(outputPtr, params.bufferLength, genOpHelpers.alignedProcLength(params.bufferLength))
        vm.arena.release(baseOut)
        vm.arena.release(sustainBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
        vm.arena.release(releaseBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
        vm.arena.release(exponentBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
        vm.arena.release(trigBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      } else {
        slot.history.write(params.sampleCount, vm.paramScratch)
        instance.process(params.bufferLength, params.sampleCount, params.sampleRate, params.nyquist, params.piOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, inputPtr, outputPtr, attackValue, decayValue, sustainAudioResult.ptr, releaseAudioResult.ptr, exponentAudioResult.ptr, trigAudioResult.ptr)
        genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      }
      genOpHelpers.writeOutputToHistoryRing(slot.history, outputPtr, params.bufferLength)
      genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      break
    }
    case 2: {
      const slot: GenSlot = vm.genPools[457].get()
      genOpHelpers.writeCallStackMetaToSlot(vm, slot)
      const procLen: i32 = genOpHelpers.alignedProcLength(params.bufferLength)
      genOpHelpers.writeInputToHistoryRingZero(slot.history)
      const inputPtr: usize = 0
      output = vm.arena.get(procLen)
      const outputPtr: usize = output.dataStart
      const instance: Adsr_default_attack_scalar_decay_audio_sustain_scalar_release_scalar_exponent_scalar_trig_scalar = changetype<Adsr_default_attack_scalar_decay_audio_sustain_scalar_release_scalar_exponent_scalar_trig_scalar>(slot.instance)
      vm.paramScratch[6] = instance.stage
      vm.paramScratch[7] = instance.env
      const decayAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, decayTagged, procLen)
      const osFactor: i32 = genOpHelpers.getOversampleFactor(vm)
      if (osFactor > 0) {
        const baseLen: i32 = params.bufferLength / osFactor
        const baseSampleRate: f32 = params.sampleRate / f32(osFactor)
        const baseNyquist: f32 = baseSampleRate * 0.5
        const basePiOverNyquist: f32 = f32(Mathf.PI) / baseNyquist
        const baseSampleCount: f32 = f32(params.sampleCount) / f32(osFactor)
        const baseProcLen: i32 = (baseLen + 15) & ~15
        const baseOut: Float32Array = vm.arena.get(baseProcLen)
        const baseOutPtr: usize = baseOut.dataStart
        let baseInputPtr: usize = inputPtr
        const decaySrc: usize = decayAudioResult.ptr
        const decayBuf: Float32Array = vm.arena.get(baseProcLen)
        const decayPtr: usize = decayBuf.dataStart
        downsample(vm, decaySrc, decayPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(decayPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(decayPtr + (usize(k) << 2), last)
          }
        }
        slot.history.write(params.sampleCount / osFactor, vm.paramScratch)
        instance.process(baseLen, params.sampleCount / osFactor, baseSampleRate, baseNyquist, basePiOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, baseInputPtr, baseOutPtr, attackValue, sustainValue, releaseValue, exponentValue, trigValue, decayPtr)
        upsample(vm, baseOutPtr, outputPtr, baseLen, osFactor)
        genOpHelpers.extendBufferWithLastSample(outputPtr, params.bufferLength, genOpHelpers.alignedProcLength(params.bufferLength))
        vm.arena.release(baseOut)
        vm.arena.release(decayBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
      } else {
        slot.history.write(params.sampleCount, vm.paramScratch)
        instance.process(params.bufferLength, params.sampleCount, params.sampleRate, params.nyquist, params.piOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, inputPtr, outputPtr, attackValue, sustainValue, releaseValue, exponentValue, trigValue, decayAudioResult.ptr)
        genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
      }
      genOpHelpers.writeOutputToHistoryRing(slot.history, outputPtr, params.bufferLength)
      genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
      break
    }
    case 34: {
      const slot: GenSlot = vm.genPools[458].get()
      genOpHelpers.writeCallStackMetaToSlot(vm, slot)
      const procLen: i32 = genOpHelpers.alignedProcLength(params.bufferLength)
      genOpHelpers.writeInputToHistoryRingZero(slot.history)
      const inputPtr: usize = 0
      output = vm.arena.get(procLen)
      const outputPtr: usize = output.dataStart
      const instance: Adsr_default_attack_scalar_decay_audio_sustain_scalar_release_scalar_exponent_scalar_trig_audio = changetype<Adsr_default_attack_scalar_decay_audio_sustain_scalar_release_scalar_exponent_scalar_trig_audio>(slot.instance)
      vm.paramScratch[6] = instance.stage
      vm.paramScratch[7] = instance.env
      const decayAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, decayTagged, procLen)
      const trigAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, trigTagged, procLen)
      const osFactor: i32 = genOpHelpers.getOversampleFactor(vm)
      if (osFactor > 0) {
        const baseLen: i32 = params.bufferLength / osFactor
        const baseSampleRate: f32 = params.sampleRate / f32(osFactor)
        const baseNyquist: f32 = baseSampleRate * 0.5
        const basePiOverNyquist: f32 = f32(Mathf.PI) / baseNyquist
        const baseSampleCount: f32 = f32(params.sampleCount) / f32(osFactor)
        const baseProcLen: i32 = (baseLen + 15) & ~15
        const baseOut: Float32Array = vm.arena.get(baseProcLen)
        const baseOutPtr: usize = baseOut.dataStart
        let baseInputPtr: usize = inputPtr
        const decaySrc: usize = decayAudioResult.ptr
        const decayBuf: Float32Array = vm.arena.get(baseProcLen)
        const decayPtr: usize = decayBuf.dataStart
        downsample(vm, decaySrc, decayPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(decayPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(decayPtr + (usize(k) << 2), last)
          }
        }
        const trigSrc: usize = trigAudioResult.ptr
        const trigBuf: Float32Array = vm.arena.get(baseProcLen)
        const trigPtr: usize = trigBuf.dataStart
        downsample(vm, trigSrc, trigPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(trigPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(trigPtr + (usize(k) << 2), last)
          }
        }
        slot.history.write(params.sampleCount / osFactor, vm.paramScratch)
        instance.process(baseLen, params.sampleCount / osFactor, baseSampleRate, baseNyquist, basePiOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, baseInputPtr, baseOutPtr, attackValue, sustainValue, releaseValue, exponentValue, decayPtr, trigPtr)
        upsample(vm, baseOutPtr, outputPtr, baseLen, osFactor)
        genOpHelpers.extendBufferWithLastSample(outputPtr, params.bufferLength, genOpHelpers.alignedProcLength(params.bufferLength))
        vm.arena.release(baseOut)
        vm.arena.release(decayBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
        vm.arena.release(trigBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      } else {
        slot.history.write(params.sampleCount, vm.paramScratch)
        instance.process(params.bufferLength, params.sampleCount, params.sampleRate, params.nyquist, params.piOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, inputPtr, outputPtr, attackValue, sustainValue, releaseValue, exponentValue, decayAudioResult.ptr, trigAudioResult.ptr)
        genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      }
      genOpHelpers.writeOutputToHistoryRing(slot.history, outputPtr, params.bufferLength)
      genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      break
    }
    case 18: {
      const slot: GenSlot = vm.genPools[459].get()
      genOpHelpers.writeCallStackMetaToSlot(vm, slot)
      const procLen: i32 = genOpHelpers.alignedProcLength(params.bufferLength)
      genOpHelpers.writeInputToHistoryRingZero(slot.history)
      const inputPtr: usize = 0
      output = vm.arena.get(procLen)
      const outputPtr: usize = output.dataStart
      const instance: Adsr_default_attack_scalar_decay_audio_sustain_scalar_release_scalar_exponent_audio_trig_scalar = changetype<Adsr_default_attack_scalar_decay_audio_sustain_scalar_release_scalar_exponent_audio_trig_scalar>(slot.instance)
      vm.paramScratch[6] = instance.stage
      vm.paramScratch[7] = instance.env
      const decayAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, decayTagged, procLen)
      const exponentAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, exponentTagged, procLen)
      const osFactor: i32 = genOpHelpers.getOversampleFactor(vm)
      if (osFactor > 0) {
        const baseLen: i32 = params.bufferLength / osFactor
        const baseSampleRate: f32 = params.sampleRate / f32(osFactor)
        const baseNyquist: f32 = baseSampleRate * 0.5
        const basePiOverNyquist: f32 = f32(Mathf.PI) / baseNyquist
        const baseSampleCount: f32 = f32(params.sampleCount) / f32(osFactor)
        const baseProcLen: i32 = (baseLen + 15) & ~15
        const baseOut: Float32Array = vm.arena.get(baseProcLen)
        const baseOutPtr: usize = baseOut.dataStart
        let baseInputPtr: usize = inputPtr
        const decaySrc: usize = decayAudioResult.ptr
        const decayBuf: Float32Array = vm.arena.get(baseProcLen)
        const decayPtr: usize = decayBuf.dataStart
        downsample(vm, decaySrc, decayPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(decayPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(decayPtr + (usize(k) << 2), last)
          }
        }
        const exponentSrc: usize = exponentAudioResult.ptr
        const exponentBuf: Float32Array = vm.arena.get(baseProcLen)
        const exponentPtr: usize = exponentBuf.dataStart
        downsample(vm, exponentSrc, exponentPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(exponentPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(exponentPtr + (usize(k) << 2), last)
          }
        }
        slot.history.write(params.sampleCount / osFactor, vm.paramScratch)
        instance.process(baseLen, params.sampleCount / osFactor, baseSampleRate, baseNyquist, basePiOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, baseInputPtr, baseOutPtr, attackValue, sustainValue, releaseValue, trigValue, decayPtr, exponentPtr)
        upsample(vm, baseOutPtr, outputPtr, baseLen, osFactor)
        genOpHelpers.extendBufferWithLastSample(outputPtr, params.bufferLength, genOpHelpers.alignedProcLength(params.bufferLength))
        vm.arena.release(baseOut)
        vm.arena.release(decayBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
        vm.arena.release(exponentBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
      } else {
        slot.history.write(params.sampleCount, vm.paramScratch)
        instance.process(params.bufferLength, params.sampleCount, params.sampleRate, params.nyquist, params.piOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, inputPtr, outputPtr, attackValue, sustainValue, releaseValue, trigValue, decayAudioResult.ptr, exponentAudioResult.ptr)
        genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
      }
      genOpHelpers.writeOutputToHistoryRing(slot.history, outputPtr, params.bufferLength)
      genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
      break
    }
    case 50: {
      const slot: GenSlot = vm.genPools[460].get()
      genOpHelpers.writeCallStackMetaToSlot(vm, slot)
      const procLen: i32 = genOpHelpers.alignedProcLength(params.bufferLength)
      genOpHelpers.writeInputToHistoryRingZero(slot.history)
      const inputPtr: usize = 0
      output = vm.arena.get(procLen)
      const outputPtr: usize = output.dataStart
      const instance: Adsr_default_attack_scalar_decay_audio_sustain_scalar_release_scalar_exponent_audio_trig_audio = changetype<Adsr_default_attack_scalar_decay_audio_sustain_scalar_release_scalar_exponent_audio_trig_audio>(slot.instance)
      vm.paramScratch[6] = instance.stage
      vm.paramScratch[7] = instance.env
      const decayAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, decayTagged, procLen)
      const exponentAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, exponentTagged, procLen)
      const trigAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, trigTagged, procLen)
      const osFactor: i32 = genOpHelpers.getOversampleFactor(vm)
      if (osFactor > 0) {
        const baseLen: i32 = params.bufferLength / osFactor
        const baseSampleRate: f32 = params.sampleRate / f32(osFactor)
        const baseNyquist: f32 = baseSampleRate * 0.5
        const basePiOverNyquist: f32 = f32(Mathf.PI) / baseNyquist
        const baseSampleCount: f32 = f32(params.sampleCount) / f32(osFactor)
        const baseProcLen: i32 = (baseLen + 15) & ~15
        const baseOut: Float32Array = vm.arena.get(baseProcLen)
        const baseOutPtr: usize = baseOut.dataStart
        let baseInputPtr: usize = inputPtr
        const decaySrc: usize = decayAudioResult.ptr
        const decayBuf: Float32Array = vm.arena.get(baseProcLen)
        const decayPtr: usize = decayBuf.dataStart
        downsample(vm, decaySrc, decayPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(decayPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(decayPtr + (usize(k) << 2), last)
          }
        }
        const exponentSrc: usize = exponentAudioResult.ptr
        const exponentBuf: Float32Array = vm.arena.get(baseProcLen)
        const exponentPtr: usize = exponentBuf.dataStart
        downsample(vm, exponentSrc, exponentPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(exponentPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(exponentPtr + (usize(k) << 2), last)
          }
        }
        const trigSrc: usize = trigAudioResult.ptr
        const trigBuf: Float32Array = vm.arena.get(baseProcLen)
        const trigPtr: usize = trigBuf.dataStart
        downsample(vm, trigSrc, trigPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(trigPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(trigPtr + (usize(k) << 2), last)
          }
        }
        slot.history.write(params.sampleCount / osFactor, vm.paramScratch)
        instance.process(baseLen, params.sampleCount / osFactor, baseSampleRate, baseNyquist, basePiOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, baseInputPtr, baseOutPtr, attackValue, sustainValue, releaseValue, decayPtr, exponentPtr, trigPtr)
        upsample(vm, baseOutPtr, outputPtr, baseLen, osFactor)
        genOpHelpers.extendBufferWithLastSample(outputPtr, params.bufferLength, genOpHelpers.alignedProcLength(params.bufferLength))
        vm.arena.release(baseOut)
        vm.arena.release(decayBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
        vm.arena.release(exponentBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
        vm.arena.release(trigBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      } else {
        slot.history.write(params.sampleCount, vm.paramScratch)
        instance.process(params.bufferLength, params.sampleCount, params.sampleRate, params.nyquist, params.piOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, inputPtr, outputPtr, attackValue, sustainValue, releaseValue, decayAudioResult.ptr, exponentAudioResult.ptr, trigAudioResult.ptr)
        genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      }
      genOpHelpers.writeOutputToHistoryRing(slot.history, outputPtr, params.bufferLength)
      genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      break
    }
    case 10: {
      const slot: GenSlot = vm.genPools[461].get()
      genOpHelpers.writeCallStackMetaToSlot(vm, slot)
      const procLen: i32 = genOpHelpers.alignedProcLength(params.bufferLength)
      genOpHelpers.writeInputToHistoryRingZero(slot.history)
      const inputPtr: usize = 0
      output = vm.arena.get(procLen)
      const outputPtr: usize = output.dataStart
      const instance: Adsr_default_attack_scalar_decay_audio_sustain_scalar_release_audio_exponent_scalar_trig_scalar = changetype<Adsr_default_attack_scalar_decay_audio_sustain_scalar_release_audio_exponent_scalar_trig_scalar>(slot.instance)
      vm.paramScratch[6] = instance.stage
      vm.paramScratch[7] = instance.env
      const decayAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, decayTagged, procLen)
      const releaseAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, releaseTagged, procLen)
      const osFactor: i32 = genOpHelpers.getOversampleFactor(vm)
      if (osFactor > 0) {
        const baseLen: i32 = params.bufferLength / osFactor
        const baseSampleRate: f32 = params.sampleRate / f32(osFactor)
        const baseNyquist: f32 = baseSampleRate * 0.5
        const basePiOverNyquist: f32 = f32(Mathf.PI) / baseNyquist
        const baseSampleCount: f32 = f32(params.sampleCount) / f32(osFactor)
        const baseProcLen: i32 = (baseLen + 15) & ~15
        const baseOut: Float32Array = vm.arena.get(baseProcLen)
        const baseOutPtr: usize = baseOut.dataStart
        let baseInputPtr: usize = inputPtr
        const decaySrc: usize = decayAudioResult.ptr
        const decayBuf: Float32Array = vm.arena.get(baseProcLen)
        const decayPtr: usize = decayBuf.dataStart
        downsample(vm, decaySrc, decayPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(decayPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(decayPtr + (usize(k) << 2), last)
          }
        }
        const releaseSrc: usize = releaseAudioResult.ptr
        const releaseBuf: Float32Array = vm.arena.get(baseProcLen)
        const releasePtr: usize = releaseBuf.dataStart
        downsample(vm, releaseSrc, releasePtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(releasePtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(releasePtr + (usize(k) << 2), last)
          }
        }
        slot.history.write(params.sampleCount / osFactor, vm.paramScratch)
        instance.process(baseLen, params.sampleCount / osFactor, baseSampleRate, baseNyquist, basePiOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, baseInputPtr, baseOutPtr, attackValue, sustainValue, exponentValue, trigValue, decayPtr, releasePtr)
        upsample(vm, baseOutPtr, outputPtr, baseLen, osFactor)
        genOpHelpers.extendBufferWithLastSample(outputPtr, params.bufferLength, genOpHelpers.alignedProcLength(params.bufferLength))
        vm.arena.release(baseOut)
        vm.arena.release(decayBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
        vm.arena.release(releaseBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
      } else {
        slot.history.write(params.sampleCount, vm.paramScratch)
        instance.process(params.bufferLength, params.sampleCount, params.sampleRate, params.nyquist, params.piOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, inputPtr, outputPtr, attackValue, sustainValue, exponentValue, trigValue, decayAudioResult.ptr, releaseAudioResult.ptr)
        genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
      }
      genOpHelpers.writeOutputToHistoryRing(slot.history, outputPtr, params.bufferLength)
      genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
      break
    }
    case 42: {
      const slot: GenSlot = vm.genPools[462].get()
      genOpHelpers.writeCallStackMetaToSlot(vm, slot)
      const procLen: i32 = genOpHelpers.alignedProcLength(params.bufferLength)
      genOpHelpers.writeInputToHistoryRingZero(slot.history)
      const inputPtr: usize = 0
      output = vm.arena.get(procLen)
      const outputPtr: usize = output.dataStart
      const instance: Adsr_default_attack_scalar_decay_audio_sustain_scalar_release_audio_exponent_scalar_trig_audio = changetype<Adsr_default_attack_scalar_decay_audio_sustain_scalar_release_audio_exponent_scalar_trig_audio>(slot.instance)
      vm.paramScratch[6] = instance.stage
      vm.paramScratch[7] = instance.env
      const decayAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, decayTagged, procLen)
      const releaseAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, releaseTagged, procLen)
      const trigAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, trigTagged, procLen)
      const osFactor: i32 = genOpHelpers.getOversampleFactor(vm)
      if (osFactor > 0) {
        const baseLen: i32 = params.bufferLength / osFactor
        const baseSampleRate: f32 = params.sampleRate / f32(osFactor)
        const baseNyquist: f32 = baseSampleRate * 0.5
        const basePiOverNyquist: f32 = f32(Mathf.PI) / baseNyquist
        const baseSampleCount: f32 = f32(params.sampleCount) / f32(osFactor)
        const baseProcLen: i32 = (baseLen + 15) & ~15
        const baseOut: Float32Array = vm.arena.get(baseProcLen)
        const baseOutPtr: usize = baseOut.dataStart
        let baseInputPtr: usize = inputPtr
        const decaySrc: usize = decayAudioResult.ptr
        const decayBuf: Float32Array = vm.arena.get(baseProcLen)
        const decayPtr: usize = decayBuf.dataStart
        downsample(vm, decaySrc, decayPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(decayPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(decayPtr + (usize(k) << 2), last)
          }
        }
        const releaseSrc: usize = releaseAudioResult.ptr
        const releaseBuf: Float32Array = vm.arena.get(baseProcLen)
        const releasePtr: usize = releaseBuf.dataStart
        downsample(vm, releaseSrc, releasePtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(releasePtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(releasePtr + (usize(k) << 2), last)
          }
        }
        const trigSrc: usize = trigAudioResult.ptr
        const trigBuf: Float32Array = vm.arena.get(baseProcLen)
        const trigPtr: usize = trigBuf.dataStart
        downsample(vm, trigSrc, trigPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(trigPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(trigPtr + (usize(k) << 2), last)
          }
        }
        slot.history.write(params.sampleCount / osFactor, vm.paramScratch)
        instance.process(baseLen, params.sampleCount / osFactor, baseSampleRate, baseNyquist, basePiOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, baseInputPtr, baseOutPtr, attackValue, sustainValue, exponentValue, decayPtr, releasePtr, trigPtr)
        upsample(vm, baseOutPtr, outputPtr, baseLen, osFactor)
        genOpHelpers.extendBufferWithLastSample(outputPtr, params.bufferLength, genOpHelpers.alignedProcLength(params.bufferLength))
        vm.arena.release(baseOut)
        vm.arena.release(decayBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
        vm.arena.release(releaseBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
        vm.arena.release(trigBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      } else {
        slot.history.write(params.sampleCount, vm.paramScratch)
        instance.process(params.bufferLength, params.sampleCount, params.sampleRate, params.nyquist, params.piOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, inputPtr, outputPtr, attackValue, sustainValue, exponentValue, decayAudioResult.ptr, releaseAudioResult.ptr, trigAudioResult.ptr)
        genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      }
      genOpHelpers.writeOutputToHistoryRing(slot.history, outputPtr, params.bufferLength)
      genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      break
    }
    case 26: {
      const slot: GenSlot = vm.genPools[463].get()
      genOpHelpers.writeCallStackMetaToSlot(vm, slot)
      const procLen: i32 = genOpHelpers.alignedProcLength(params.bufferLength)
      genOpHelpers.writeInputToHistoryRingZero(slot.history)
      const inputPtr: usize = 0
      output = vm.arena.get(procLen)
      const outputPtr: usize = output.dataStart
      const instance: Adsr_default_attack_scalar_decay_audio_sustain_scalar_release_audio_exponent_audio_trig_scalar = changetype<Adsr_default_attack_scalar_decay_audio_sustain_scalar_release_audio_exponent_audio_trig_scalar>(slot.instance)
      vm.paramScratch[6] = instance.stage
      vm.paramScratch[7] = instance.env
      const decayAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, decayTagged, procLen)
      const releaseAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, releaseTagged, procLen)
      const exponentAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, exponentTagged, procLen)
      const osFactor: i32 = genOpHelpers.getOversampleFactor(vm)
      if (osFactor > 0) {
        const baseLen: i32 = params.bufferLength / osFactor
        const baseSampleRate: f32 = params.sampleRate / f32(osFactor)
        const baseNyquist: f32 = baseSampleRate * 0.5
        const basePiOverNyquist: f32 = f32(Mathf.PI) / baseNyquist
        const baseSampleCount: f32 = f32(params.sampleCount) / f32(osFactor)
        const baseProcLen: i32 = (baseLen + 15) & ~15
        const baseOut: Float32Array = vm.arena.get(baseProcLen)
        const baseOutPtr: usize = baseOut.dataStart
        let baseInputPtr: usize = inputPtr
        const decaySrc: usize = decayAudioResult.ptr
        const decayBuf: Float32Array = vm.arena.get(baseProcLen)
        const decayPtr: usize = decayBuf.dataStart
        downsample(vm, decaySrc, decayPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(decayPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(decayPtr + (usize(k) << 2), last)
          }
        }
        const releaseSrc: usize = releaseAudioResult.ptr
        const releaseBuf: Float32Array = vm.arena.get(baseProcLen)
        const releasePtr: usize = releaseBuf.dataStart
        downsample(vm, releaseSrc, releasePtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(releasePtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(releasePtr + (usize(k) << 2), last)
          }
        }
        const exponentSrc: usize = exponentAudioResult.ptr
        const exponentBuf: Float32Array = vm.arena.get(baseProcLen)
        const exponentPtr: usize = exponentBuf.dataStart
        downsample(vm, exponentSrc, exponentPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(exponentPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(exponentPtr + (usize(k) << 2), last)
          }
        }
        slot.history.write(params.sampleCount / osFactor, vm.paramScratch)
        instance.process(baseLen, params.sampleCount / osFactor, baseSampleRate, baseNyquist, basePiOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, baseInputPtr, baseOutPtr, attackValue, sustainValue, trigValue, decayPtr, releasePtr, exponentPtr)
        upsample(vm, baseOutPtr, outputPtr, baseLen, osFactor)
        genOpHelpers.extendBufferWithLastSample(outputPtr, params.bufferLength, genOpHelpers.alignedProcLength(params.bufferLength))
        vm.arena.release(baseOut)
        vm.arena.release(decayBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
        vm.arena.release(releaseBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
        vm.arena.release(exponentBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
      } else {
        slot.history.write(params.sampleCount, vm.paramScratch)
        instance.process(params.bufferLength, params.sampleCount, params.sampleRate, params.nyquist, params.piOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, inputPtr, outputPtr, attackValue, sustainValue, trigValue, decayAudioResult.ptr, releaseAudioResult.ptr, exponentAudioResult.ptr)
        genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
      }
      genOpHelpers.writeOutputToHistoryRing(slot.history, outputPtr, params.bufferLength)
      genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
      break
    }
    case 58: {
      const slot: GenSlot = vm.genPools[464].get()
      genOpHelpers.writeCallStackMetaToSlot(vm, slot)
      const procLen: i32 = genOpHelpers.alignedProcLength(params.bufferLength)
      genOpHelpers.writeInputToHistoryRingZero(slot.history)
      const inputPtr: usize = 0
      output = vm.arena.get(procLen)
      const outputPtr: usize = output.dataStart
      const instance: Adsr_default_attack_scalar_decay_audio_sustain_scalar_release_audio_exponent_audio_trig_audio = changetype<Adsr_default_attack_scalar_decay_audio_sustain_scalar_release_audio_exponent_audio_trig_audio>(slot.instance)
      vm.paramScratch[6] = instance.stage
      vm.paramScratch[7] = instance.env
      const decayAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, decayTagged, procLen)
      const releaseAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, releaseTagged, procLen)
      const exponentAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, exponentTagged, procLen)
      const trigAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, trigTagged, procLen)
      const osFactor: i32 = genOpHelpers.getOversampleFactor(vm)
      if (osFactor > 0) {
        const baseLen: i32 = params.bufferLength / osFactor
        const baseSampleRate: f32 = params.sampleRate / f32(osFactor)
        const baseNyquist: f32 = baseSampleRate * 0.5
        const basePiOverNyquist: f32 = f32(Mathf.PI) / baseNyquist
        const baseSampleCount: f32 = f32(params.sampleCount) / f32(osFactor)
        const baseProcLen: i32 = (baseLen + 15) & ~15
        const baseOut: Float32Array = vm.arena.get(baseProcLen)
        const baseOutPtr: usize = baseOut.dataStart
        let baseInputPtr: usize = inputPtr
        const decaySrc: usize = decayAudioResult.ptr
        const decayBuf: Float32Array = vm.arena.get(baseProcLen)
        const decayPtr: usize = decayBuf.dataStart
        downsample(vm, decaySrc, decayPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(decayPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(decayPtr + (usize(k) << 2), last)
          }
        }
        const releaseSrc: usize = releaseAudioResult.ptr
        const releaseBuf: Float32Array = vm.arena.get(baseProcLen)
        const releasePtr: usize = releaseBuf.dataStart
        downsample(vm, releaseSrc, releasePtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(releasePtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(releasePtr + (usize(k) << 2), last)
          }
        }
        const exponentSrc: usize = exponentAudioResult.ptr
        const exponentBuf: Float32Array = vm.arena.get(baseProcLen)
        const exponentPtr: usize = exponentBuf.dataStart
        downsample(vm, exponentSrc, exponentPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(exponentPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(exponentPtr + (usize(k) << 2), last)
          }
        }
        const trigSrc: usize = trigAudioResult.ptr
        const trigBuf: Float32Array = vm.arena.get(baseProcLen)
        const trigPtr: usize = trigBuf.dataStart
        downsample(vm, trigSrc, trigPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(trigPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(trigPtr + (usize(k) << 2), last)
          }
        }
        slot.history.write(params.sampleCount / osFactor, vm.paramScratch)
        instance.process(baseLen, params.sampleCount / osFactor, baseSampleRate, baseNyquist, basePiOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, baseInputPtr, baseOutPtr, attackValue, sustainValue, decayPtr, releasePtr, exponentPtr, trigPtr)
        upsample(vm, baseOutPtr, outputPtr, baseLen, osFactor)
        genOpHelpers.extendBufferWithLastSample(outputPtr, params.bufferLength, genOpHelpers.alignedProcLength(params.bufferLength))
        vm.arena.release(baseOut)
        vm.arena.release(decayBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
        vm.arena.release(releaseBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
        vm.arena.release(exponentBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
        vm.arena.release(trigBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      } else {
        slot.history.write(params.sampleCount, vm.paramScratch)
        instance.process(params.bufferLength, params.sampleCount, params.sampleRate, params.nyquist, params.piOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, inputPtr, outputPtr, attackValue, sustainValue, decayAudioResult.ptr, releaseAudioResult.ptr, exponentAudioResult.ptr, trigAudioResult.ptr)
        genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      }
      genOpHelpers.writeOutputToHistoryRing(slot.history, outputPtr, params.bufferLength)
      genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      break
    }
    case 6: {
      const slot: GenSlot = vm.genPools[465].get()
      genOpHelpers.writeCallStackMetaToSlot(vm, slot)
      const procLen: i32 = genOpHelpers.alignedProcLength(params.bufferLength)
      genOpHelpers.writeInputToHistoryRingZero(slot.history)
      const inputPtr: usize = 0
      output = vm.arena.get(procLen)
      const outputPtr: usize = output.dataStart
      const instance: Adsr_default_attack_scalar_decay_audio_sustain_audio_release_scalar_exponent_scalar_trig_scalar = changetype<Adsr_default_attack_scalar_decay_audio_sustain_audio_release_scalar_exponent_scalar_trig_scalar>(slot.instance)
      vm.paramScratch[6] = instance.stage
      vm.paramScratch[7] = instance.env
      const decayAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, decayTagged, procLen)
      const sustainAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, sustainTagged, procLen)
      const osFactor: i32 = genOpHelpers.getOversampleFactor(vm)
      if (osFactor > 0) {
        const baseLen: i32 = params.bufferLength / osFactor
        const baseSampleRate: f32 = params.sampleRate / f32(osFactor)
        const baseNyquist: f32 = baseSampleRate * 0.5
        const basePiOverNyquist: f32 = f32(Mathf.PI) / baseNyquist
        const baseSampleCount: f32 = f32(params.sampleCount) / f32(osFactor)
        const baseProcLen: i32 = (baseLen + 15) & ~15
        const baseOut: Float32Array = vm.arena.get(baseProcLen)
        const baseOutPtr: usize = baseOut.dataStart
        let baseInputPtr: usize = inputPtr
        const decaySrc: usize = decayAudioResult.ptr
        const decayBuf: Float32Array = vm.arena.get(baseProcLen)
        const decayPtr: usize = decayBuf.dataStart
        downsample(vm, decaySrc, decayPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(decayPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(decayPtr + (usize(k) << 2), last)
          }
        }
        const sustainSrc: usize = sustainAudioResult.ptr
        const sustainBuf: Float32Array = vm.arena.get(baseProcLen)
        const sustainPtr: usize = sustainBuf.dataStart
        downsample(vm, sustainSrc, sustainPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(sustainPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(sustainPtr + (usize(k) << 2), last)
          }
        }
        slot.history.write(params.sampleCount / osFactor, vm.paramScratch)
        instance.process(baseLen, params.sampleCount / osFactor, baseSampleRate, baseNyquist, basePiOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, baseInputPtr, baseOutPtr, attackValue, releaseValue, exponentValue, trigValue, decayPtr, sustainPtr)
        upsample(vm, baseOutPtr, outputPtr, baseLen, osFactor)
        genOpHelpers.extendBufferWithLastSample(outputPtr, params.bufferLength, genOpHelpers.alignedProcLength(params.bufferLength))
        vm.arena.release(baseOut)
        vm.arena.release(decayBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
        vm.arena.release(sustainBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
      } else {
        slot.history.write(params.sampleCount, vm.paramScratch)
        instance.process(params.bufferLength, params.sampleCount, params.sampleRate, params.nyquist, params.piOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, inputPtr, outputPtr, attackValue, releaseValue, exponentValue, trigValue, decayAudioResult.ptr, sustainAudioResult.ptr)
        genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
      }
      genOpHelpers.writeOutputToHistoryRing(slot.history, outputPtr, params.bufferLength)
      genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
      break
    }
    case 38: {
      const slot: GenSlot = vm.genPools[466].get()
      genOpHelpers.writeCallStackMetaToSlot(vm, slot)
      const procLen: i32 = genOpHelpers.alignedProcLength(params.bufferLength)
      genOpHelpers.writeInputToHistoryRingZero(slot.history)
      const inputPtr: usize = 0
      output = vm.arena.get(procLen)
      const outputPtr: usize = output.dataStart
      const instance: Adsr_default_attack_scalar_decay_audio_sustain_audio_release_scalar_exponent_scalar_trig_audio = changetype<Adsr_default_attack_scalar_decay_audio_sustain_audio_release_scalar_exponent_scalar_trig_audio>(slot.instance)
      vm.paramScratch[6] = instance.stage
      vm.paramScratch[7] = instance.env
      const decayAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, decayTagged, procLen)
      const sustainAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, sustainTagged, procLen)
      const trigAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, trigTagged, procLen)
      const osFactor: i32 = genOpHelpers.getOversampleFactor(vm)
      if (osFactor > 0) {
        const baseLen: i32 = params.bufferLength / osFactor
        const baseSampleRate: f32 = params.sampleRate / f32(osFactor)
        const baseNyquist: f32 = baseSampleRate * 0.5
        const basePiOverNyquist: f32 = f32(Mathf.PI) / baseNyquist
        const baseSampleCount: f32 = f32(params.sampleCount) / f32(osFactor)
        const baseProcLen: i32 = (baseLen + 15) & ~15
        const baseOut: Float32Array = vm.arena.get(baseProcLen)
        const baseOutPtr: usize = baseOut.dataStart
        let baseInputPtr: usize = inputPtr
        const decaySrc: usize = decayAudioResult.ptr
        const decayBuf: Float32Array = vm.arena.get(baseProcLen)
        const decayPtr: usize = decayBuf.dataStart
        downsample(vm, decaySrc, decayPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(decayPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(decayPtr + (usize(k) << 2), last)
          }
        }
        const sustainSrc: usize = sustainAudioResult.ptr
        const sustainBuf: Float32Array = vm.arena.get(baseProcLen)
        const sustainPtr: usize = sustainBuf.dataStart
        downsample(vm, sustainSrc, sustainPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(sustainPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(sustainPtr + (usize(k) << 2), last)
          }
        }
        const trigSrc: usize = trigAudioResult.ptr
        const trigBuf: Float32Array = vm.arena.get(baseProcLen)
        const trigPtr: usize = trigBuf.dataStart
        downsample(vm, trigSrc, trigPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(trigPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(trigPtr + (usize(k) << 2), last)
          }
        }
        slot.history.write(params.sampleCount / osFactor, vm.paramScratch)
        instance.process(baseLen, params.sampleCount / osFactor, baseSampleRate, baseNyquist, basePiOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, baseInputPtr, baseOutPtr, attackValue, releaseValue, exponentValue, decayPtr, sustainPtr, trigPtr)
        upsample(vm, baseOutPtr, outputPtr, baseLen, osFactor)
        genOpHelpers.extendBufferWithLastSample(outputPtr, params.bufferLength, genOpHelpers.alignedProcLength(params.bufferLength))
        vm.arena.release(baseOut)
        vm.arena.release(decayBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
        vm.arena.release(sustainBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
        vm.arena.release(trigBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      } else {
        slot.history.write(params.sampleCount, vm.paramScratch)
        instance.process(params.bufferLength, params.sampleCount, params.sampleRate, params.nyquist, params.piOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, inputPtr, outputPtr, attackValue, releaseValue, exponentValue, decayAudioResult.ptr, sustainAudioResult.ptr, trigAudioResult.ptr)
        genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      }
      genOpHelpers.writeOutputToHistoryRing(slot.history, outputPtr, params.bufferLength)
      genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      break
    }
    case 22: {
      const slot: GenSlot = vm.genPools[467].get()
      genOpHelpers.writeCallStackMetaToSlot(vm, slot)
      const procLen: i32 = genOpHelpers.alignedProcLength(params.bufferLength)
      genOpHelpers.writeInputToHistoryRingZero(slot.history)
      const inputPtr: usize = 0
      output = vm.arena.get(procLen)
      const outputPtr: usize = output.dataStart
      const instance: Adsr_default_attack_scalar_decay_audio_sustain_audio_release_scalar_exponent_audio_trig_scalar = changetype<Adsr_default_attack_scalar_decay_audio_sustain_audio_release_scalar_exponent_audio_trig_scalar>(slot.instance)
      vm.paramScratch[6] = instance.stage
      vm.paramScratch[7] = instance.env
      const decayAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, decayTagged, procLen)
      const sustainAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, sustainTagged, procLen)
      const exponentAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, exponentTagged, procLen)
      const osFactor: i32 = genOpHelpers.getOversampleFactor(vm)
      if (osFactor > 0) {
        const baseLen: i32 = params.bufferLength / osFactor
        const baseSampleRate: f32 = params.sampleRate / f32(osFactor)
        const baseNyquist: f32 = baseSampleRate * 0.5
        const basePiOverNyquist: f32 = f32(Mathf.PI) / baseNyquist
        const baseSampleCount: f32 = f32(params.sampleCount) / f32(osFactor)
        const baseProcLen: i32 = (baseLen + 15) & ~15
        const baseOut: Float32Array = vm.arena.get(baseProcLen)
        const baseOutPtr: usize = baseOut.dataStart
        let baseInputPtr: usize = inputPtr
        const decaySrc: usize = decayAudioResult.ptr
        const decayBuf: Float32Array = vm.arena.get(baseProcLen)
        const decayPtr: usize = decayBuf.dataStart
        downsample(vm, decaySrc, decayPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(decayPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(decayPtr + (usize(k) << 2), last)
          }
        }
        const sustainSrc: usize = sustainAudioResult.ptr
        const sustainBuf: Float32Array = vm.arena.get(baseProcLen)
        const sustainPtr: usize = sustainBuf.dataStart
        downsample(vm, sustainSrc, sustainPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(sustainPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(sustainPtr + (usize(k) << 2), last)
          }
        }
        const exponentSrc: usize = exponentAudioResult.ptr
        const exponentBuf: Float32Array = vm.arena.get(baseProcLen)
        const exponentPtr: usize = exponentBuf.dataStart
        downsample(vm, exponentSrc, exponentPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(exponentPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(exponentPtr + (usize(k) << 2), last)
          }
        }
        slot.history.write(params.sampleCount / osFactor, vm.paramScratch)
        instance.process(baseLen, params.sampleCount / osFactor, baseSampleRate, baseNyquist, basePiOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, baseInputPtr, baseOutPtr, attackValue, releaseValue, trigValue, decayPtr, sustainPtr, exponentPtr)
        upsample(vm, baseOutPtr, outputPtr, baseLen, osFactor)
        genOpHelpers.extendBufferWithLastSample(outputPtr, params.bufferLength, genOpHelpers.alignedProcLength(params.bufferLength))
        vm.arena.release(baseOut)
        vm.arena.release(decayBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
        vm.arena.release(sustainBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
        vm.arena.release(exponentBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
      } else {
        slot.history.write(params.sampleCount, vm.paramScratch)
        instance.process(params.bufferLength, params.sampleCount, params.sampleRate, params.nyquist, params.piOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, inputPtr, outputPtr, attackValue, releaseValue, trigValue, decayAudioResult.ptr, sustainAudioResult.ptr, exponentAudioResult.ptr)
        genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
      }
      genOpHelpers.writeOutputToHistoryRing(slot.history, outputPtr, params.bufferLength)
      genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
      break
    }
    case 54: {
      const slot: GenSlot = vm.genPools[468].get()
      genOpHelpers.writeCallStackMetaToSlot(vm, slot)
      const procLen: i32 = genOpHelpers.alignedProcLength(params.bufferLength)
      genOpHelpers.writeInputToHistoryRingZero(slot.history)
      const inputPtr: usize = 0
      output = vm.arena.get(procLen)
      const outputPtr: usize = output.dataStart
      const instance: Adsr_default_attack_scalar_decay_audio_sustain_audio_release_scalar_exponent_audio_trig_audio = changetype<Adsr_default_attack_scalar_decay_audio_sustain_audio_release_scalar_exponent_audio_trig_audio>(slot.instance)
      vm.paramScratch[6] = instance.stage
      vm.paramScratch[7] = instance.env
      const decayAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, decayTagged, procLen)
      const sustainAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, sustainTagged, procLen)
      const exponentAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, exponentTagged, procLen)
      const trigAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, trigTagged, procLen)
      const osFactor: i32 = genOpHelpers.getOversampleFactor(vm)
      if (osFactor > 0) {
        const baseLen: i32 = params.bufferLength / osFactor
        const baseSampleRate: f32 = params.sampleRate / f32(osFactor)
        const baseNyquist: f32 = baseSampleRate * 0.5
        const basePiOverNyquist: f32 = f32(Mathf.PI) / baseNyquist
        const baseSampleCount: f32 = f32(params.sampleCount) / f32(osFactor)
        const baseProcLen: i32 = (baseLen + 15) & ~15
        const baseOut: Float32Array = vm.arena.get(baseProcLen)
        const baseOutPtr: usize = baseOut.dataStart
        let baseInputPtr: usize = inputPtr
        const decaySrc: usize = decayAudioResult.ptr
        const decayBuf: Float32Array = vm.arena.get(baseProcLen)
        const decayPtr: usize = decayBuf.dataStart
        downsample(vm, decaySrc, decayPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(decayPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(decayPtr + (usize(k) << 2), last)
          }
        }
        const sustainSrc: usize = sustainAudioResult.ptr
        const sustainBuf: Float32Array = vm.arena.get(baseProcLen)
        const sustainPtr: usize = sustainBuf.dataStart
        downsample(vm, sustainSrc, sustainPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(sustainPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(sustainPtr + (usize(k) << 2), last)
          }
        }
        const exponentSrc: usize = exponentAudioResult.ptr
        const exponentBuf: Float32Array = vm.arena.get(baseProcLen)
        const exponentPtr: usize = exponentBuf.dataStart
        downsample(vm, exponentSrc, exponentPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(exponentPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(exponentPtr + (usize(k) << 2), last)
          }
        }
        const trigSrc: usize = trigAudioResult.ptr
        const trigBuf: Float32Array = vm.arena.get(baseProcLen)
        const trigPtr: usize = trigBuf.dataStart
        downsample(vm, trigSrc, trigPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(trigPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(trigPtr + (usize(k) << 2), last)
          }
        }
        slot.history.write(params.sampleCount / osFactor, vm.paramScratch)
        instance.process(baseLen, params.sampleCount / osFactor, baseSampleRate, baseNyquist, basePiOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, baseInputPtr, baseOutPtr, attackValue, releaseValue, decayPtr, sustainPtr, exponentPtr, trigPtr)
        upsample(vm, baseOutPtr, outputPtr, baseLen, osFactor)
        genOpHelpers.extendBufferWithLastSample(outputPtr, params.bufferLength, genOpHelpers.alignedProcLength(params.bufferLength))
        vm.arena.release(baseOut)
        vm.arena.release(decayBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
        vm.arena.release(sustainBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
        vm.arena.release(exponentBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
        vm.arena.release(trigBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      } else {
        slot.history.write(params.sampleCount, vm.paramScratch)
        instance.process(params.bufferLength, params.sampleCount, params.sampleRate, params.nyquist, params.piOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, inputPtr, outputPtr, attackValue, releaseValue, decayAudioResult.ptr, sustainAudioResult.ptr, exponentAudioResult.ptr, trigAudioResult.ptr)
        genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      }
      genOpHelpers.writeOutputToHistoryRing(slot.history, outputPtr, params.bufferLength)
      genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      break
    }
    case 14: {
      const slot: GenSlot = vm.genPools[469].get()
      genOpHelpers.writeCallStackMetaToSlot(vm, slot)
      const procLen: i32 = genOpHelpers.alignedProcLength(params.bufferLength)
      genOpHelpers.writeInputToHistoryRingZero(slot.history)
      const inputPtr: usize = 0
      output = vm.arena.get(procLen)
      const outputPtr: usize = output.dataStart
      const instance: Adsr_default_attack_scalar_decay_audio_sustain_audio_release_audio_exponent_scalar_trig_scalar = changetype<Adsr_default_attack_scalar_decay_audio_sustain_audio_release_audio_exponent_scalar_trig_scalar>(slot.instance)
      vm.paramScratch[6] = instance.stage
      vm.paramScratch[7] = instance.env
      const decayAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, decayTagged, procLen)
      const sustainAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, sustainTagged, procLen)
      const releaseAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, releaseTagged, procLen)
      const osFactor: i32 = genOpHelpers.getOversampleFactor(vm)
      if (osFactor > 0) {
        const baseLen: i32 = params.bufferLength / osFactor
        const baseSampleRate: f32 = params.sampleRate / f32(osFactor)
        const baseNyquist: f32 = baseSampleRate * 0.5
        const basePiOverNyquist: f32 = f32(Mathf.PI) / baseNyquist
        const baseSampleCount: f32 = f32(params.sampleCount) / f32(osFactor)
        const baseProcLen: i32 = (baseLen + 15) & ~15
        const baseOut: Float32Array = vm.arena.get(baseProcLen)
        const baseOutPtr: usize = baseOut.dataStart
        let baseInputPtr: usize = inputPtr
        const decaySrc: usize = decayAudioResult.ptr
        const decayBuf: Float32Array = vm.arena.get(baseProcLen)
        const decayPtr: usize = decayBuf.dataStart
        downsample(vm, decaySrc, decayPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(decayPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(decayPtr + (usize(k) << 2), last)
          }
        }
        const sustainSrc: usize = sustainAudioResult.ptr
        const sustainBuf: Float32Array = vm.arena.get(baseProcLen)
        const sustainPtr: usize = sustainBuf.dataStart
        downsample(vm, sustainSrc, sustainPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(sustainPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(sustainPtr + (usize(k) << 2), last)
          }
        }
        const releaseSrc: usize = releaseAudioResult.ptr
        const releaseBuf: Float32Array = vm.arena.get(baseProcLen)
        const releasePtr: usize = releaseBuf.dataStart
        downsample(vm, releaseSrc, releasePtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(releasePtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(releasePtr + (usize(k) << 2), last)
          }
        }
        slot.history.write(params.sampleCount / osFactor, vm.paramScratch)
        instance.process(baseLen, params.sampleCount / osFactor, baseSampleRate, baseNyquist, basePiOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, baseInputPtr, baseOutPtr, attackValue, exponentValue, trigValue, decayPtr, sustainPtr, releasePtr)
        upsample(vm, baseOutPtr, outputPtr, baseLen, osFactor)
        genOpHelpers.extendBufferWithLastSample(outputPtr, params.bufferLength, genOpHelpers.alignedProcLength(params.bufferLength))
        vm.arena.release(baseOut)
        vm.arena.release(decayBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
        vm.arena.release(sustainBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
        vm.arena.release(releaseBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
      } else {
        slot.history.write(params.sampleCount, vm.paramScratch)
        instance.process(params.bufferLength, params.sampleCount, params.sampleRate, params.nyquist, params.piOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, inputPtr, outputPtr, attackValue, exponentValue, trigValue, decayAudioResult.ptr, sustainAudioResult.ptr, releaseAudioResult.ptr)
        genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
      }
      genOpHelpers.writeOutputToHistoryRing(slot.history, outputPtr, params.bufferLength)
      genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
      break
    }
    case 46: {
      const slot: GenSlot = vm.genPools[470].get()
      genOpHelpers.writeCallStackMetaToSlot(vm, slot)
      const procLen: i32 = genOpHelpers.alignedProcLength(params.bufferLength)
      genOpHelpers.writeInputToHistoryRingZero(slot.history)
      const inputPtr: usize = 0
      output = vm.arena.get(procLen)
      const outputPtr: usize = output.dataStart
      const instance: Adsr_default_attack_scalar_decay_audio_sustain_audio_release_audio_exponent_scalar_trig_audio = changetype<Adsr_default_attack_scalar_decay_audio_sustain_audio_release_audio_exponent_scalar_trig_audio>(slot.instance)
      vm.paramScratch[6] = instance.stage
      vm.paramScratch[7] = instance.env
      const decayAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, decayTagged, procLen)
      const sustainAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, sustainTagged, procLen)
      const releaseAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, releaseTagged, procLen)
      const trigAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, trigTagged, procLen)
      const osFactor: i32 = genOpHelpers.getOversampleFactor(vm)
      if (osFactor > 0) {
        const baseLen: i32 = params.bufferLength / osFactor
        const baseSampleRate: f32 = params.sampleRate / f32(osFactor)
        const baseNyquist: f32 = baseSampleRate * 0.5
        const basePiOverNyquist: f32 = f32(Mathf.PI) / baseNyquist
        const baseSampleCount: f32 = f32(params.sampleCount) / f32(osFactor)
        const baseProcLen: i32 = (baseLen + 15) & ~15
        const baseOut: Float32Array = vm.arena.get(baseProcLen)
        const baseOutPtr: usize = baseOut.dataStart
        let baseInputPtr: usize = inputPtr
        const decaySrc: usize = decayAudioResult.ptr
        const decayBuf: Float32Array = vm.arena.get(baseProcLen)
        const decayPtr: usize = decayBuf.dataStart
        downsample(vm, decaySrc, decayPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(decayPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(decayPtr + (usize(k) << 2), last)
          }
        }
        const sustainSrc: usize = sustainAudioResult.ptr
        const sustainBuf: Float32Array = vm.arena.get(baseProcLen)
        const sustainPtr: usize = sustainBuf.dataStart
        downsample(vm, sustainSrc, sustainPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(sustainPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(sustainPtr + (usize(k) << 2), last)
          }
        }
        const releaseSrc: usize = releaseAudioResult.ptr
        const releaseBuf: Float32Array = vm.arena.get(baseProcLen)
        const releasePtr: usize = releaseBuf.dataStart
        downsample(vm, releaseSrc, releasePtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(releasePtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(releasePtr + (usize(k) << 2), last)
          }
        }
        const trigSrc: usize = trigAudioResult.ptr
        const trigBuf: Float32Array = vm.arena.get(baseProcLen)
        const trigPtr: usize = trigBuf.dataStart
        downsample(vm, trigSrc, trigPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(trigPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(trigPtr + (usize(k) << 2), last)
          }
        }
        slot.history.write(params.sampleCount / osFactor, vm.paramScratch)
        instance.process(baseLen, params.sampleCount / osFactor, baseSampleRate, baseNyquist, basePiOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, baseInputPtr, baseOutPtr, attackValue, exponentValue, decayPtr, sustainPtr, releasePtr, trigPtr)
        upsample(vm, baseOutPtr, outputPtr, baseLen, osFactor)
        genOpHelpers.extendBufferWithLastSample(outputPtr, params.bufferLength, genOpHelpers.alignedProcLength(params.bufferLength))
        vm.arena.release(baseOut)
        vm.arena.release(decayBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
        vm.arena.release(sustainBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
        vm.arena.release(releaseBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
        vm.arena.release(trigBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      } else {
        slot.history.write(params.sampleCount, vm.paramScratch)
        instance.process(params.bufferLength, params.sampleCount, params.sampleRate, params.nyquist, params.piOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, inputPtr, outputPtr, attackValue, exponentValue, decayAudioResult.ptr, sustainAudioResult.ptr, releaseAudioResult.ptr, trigAudioResult.ptr)
        genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      }
      genOpHelpers.writeOutputToHistoryRing(slot.history, outputPtr, params.bufferLength)
      genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      break
    }
    case 30: {
      const slot: GenSlot = vm.genPools[471].get()
      genOpHelpers.writeCallStackMetaToSlot(vm, slot)
      const procLen: i32 = genOpHelpers.alignedProcLength(params.bufferLength)
      genOpHelpers.writeInputToHistoryRingZero(slot.history)
      const inputPtr: usize = 0
      output = vm.arena.get(procLen)
      const outputPtr: usize = output.dataStart
      const instance: Adsr_default_attack_scalar_decay_audio_sustain_audio_release_audio_exponent_audio_trig_scalar = changetype<Adsr_default_attack_scalar_decay_audio_sustain_audio_release_audio_exponent_audio_trig_scalar>(slot.instance)
      vm.paramScratch[6] = instance.stage
      vm.paramScratch[7] = instance.env
      const decayAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, decayTagged, procLen)
      const sustainAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, sustainTagged, procLen)
      const releaseAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, releaseTagged, procLen)
      const exponentAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, exponentTagged, procLen)
      const osFactor: i32 = genOpHelpers.getOversampleFactor(vm)
      if (osFactor > 0) {
        const baseLen: i32 = params.bufferLength / osFactor
        const baseSampleRate: f32 = params.sampleRate / f32(osFactor)
        const baseNyquist: f32 = baseSampleRate * 0.5
        const basePiOverNyquist: f32 = f32(Mathf.PI) / baseNyquist
        const baseSampleCount: f32 = f32(params.sampleCount) / f32(osFactor)
        const baseProcLen: i32 = (baseLen + 15) & ~15
        const baseOut: Float32Array = vm.arena.get(baseProcLen)
        const baseOutPtr: usize = baseOut.dataStart
        let baseInputPtr: usize = inputPtr
        const decaySrc: usize = decayAudioResult.ptr
        const decayBuf: Float32Array = vm.arena.get(baseProcLen)
        const decayPtr: usize = decayBuf.dataStart
        downsample(vm, decaySrc, decayPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(decayPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(decayPtr + (usize(k) << 2), last)
          }
        }
        const sustainSrc: usize = sustainAudioResult.ptr
        const sustainBuf: Float32Array = vm.arena.get(baseProcLen)
        const sustainPtr: usize = sustainBuf.dataStart
        downsample(vm, sustainSrc, sustainPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(sustainPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(sustainPtr + (usize(k) << 2), last)
          }
        }
        const releaseSrc: usize = releaseAudioResult.ptr
        const releaseBuf: Float32Array = vm.arena.get(baseProcLen)
        const releasePtr: usize = releaseBuf.dataStart
        downsample(vm, releaseSrc, releasePtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(releasePtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(releasePtr + (usize(k) << 2), last)
          }
        }
        const exponentSrc: usize = exponentAudioResult.ptr
        const exponentBuf: Float32Array = vm.arena.get(baseProcLen)
        const exponentPtr: usize = exponentBuf.dataStart
        downsample(vm, exponentSrc, exponentPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(exponentPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(exponentPtr + (usize(k) << 2), last)
          }
        }
        slot.history.write(params.sampleCount / osFactor, vm.paramScratch)
        instance.process(baseLen, params.sampleCount / osFactor, baseSampleRate, baseNyquist, basePiOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, baseInputPtr, baseOutPtr, attackValue, trigValue, decayPtr, sustainPtr, releasePtr, exponentPtr)
        upsample(vm, baseOutPtr, outputPtr, baseLen, osFactor)
        genOpHelpers.extendBufferWithLastSample(outputPtr, params.bufferLength, genOpHelpers.alignedProcLength(params.bufferLength))
        vm.arena.release(baseOut)
        vm.arena.release(decayBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
        vm.arena.release(sustainBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
        vm.arena.release(releaseBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
        vm.arena.release(exponentBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
      } else {
        slot.history.write(params.sampleCount, vm.paramScratch)
        instance.process(params.bufferLength, params.sampleCount, params.sampleRate, params.nyquist, params.piOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, inputPtr, outputPtr, attackValue, trigValue, decayAudioResult.ptr, sustainAudioResult.ptr, releaseAudioResult.ptr, exponentAudioResult.ptr)
        genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
      }
      genOpHelpers.writeOutputToHistoryRing(slot.history, outputPtr, params.bufferLength)
      genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
      break
    }
    case 62: {
      const slot: GenSlot = vm.genPools[472].get()
      genOpHelpers.writeCallStackMetaToSlot(vm, slot)
      const procLen: i32 = genOpHelpers.alignedProcLength(params.bufferLength)
      genOpHelpers.writeInputToHistoryRingZero(slot.history)
      const inputPtr: usize = 0
      output = vm.arena.get(procLen)
      const outputPtr: usize = output.dataStart
      const instance: Adsr_default_attack_scalar_decay_audio_sustain_audio_release_audio_exponent_audio_trig_audio = changetype<Adsr_default_attack_scalar_decay_audio_sustain_audio_release_audio_exponent_audio_trig_audio>(slot.instance)
      vm.paramScratch[6] = instance.stage
      vm.paramScratch[7] = instance.env
      const decayAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, decayTagged, procLen)
      const sustainAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, sustainTagged, procLen)
      const releaseAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, releaseTagged, procLen)
      const exponentAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, exponentTagged, procLen)
      const trigAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, trigTagged, procLen)
      const osFactor: i32 = genOpHelpers.getOversampleFactor(vm)
      if (osFactor > 0) {
        const baseLen: i32 = params.bufferLength / osFactor
        const baseSampleRate: f32 = params.sampleRate / f32(osFactor)
        const baseNyquist: f32 = baseSampleRate * 0.5
        const basePiOverNyquist: f32 = f32(Mathf.PI) / baseNyquist
        const baseSampleCount: f32 = f32(params.sampleCount) / f32(osFactor)
        const baseProcLen: i32 = (baseLen + 15) & ~15
        const baseOut: Float32Array = vm.arena.get(baseProcLen)
        const baseOutPtr: usize = baseOut.dataStart
        let baseInputPtr: usize = inputPtr
        const decaySrc: usize = decayAudioResult.ptr
        const decayBuf: Float32Array = vm.arena.get(baseProcLen)
        const decayPtr: usize = decayBuf.dataStart
        downsample(vm, decaySrc, decayPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(decayPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(decayPtr + (usize(k) << 2), last)
          }
        }
        const sustainSrc: usize = sustainAudioResult.ptr
        const sustainBuf: Float32Array = vm.arena.get(baseProcLen)
        const sustainPtr: usize = sustainBuf.dataStart
        downsample(vm, sustainSrc, sustainPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(sustainPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(sustainPtr + (usize(k) << 2), last)
          }
        }
        const releaseSrc: usize = releaseAudioResult.ptr
        const releaseBuf: Float32Array = vm.arena.get(baseProcLen)
        const releasePtr: usize = releaseBuf.dataStart
        downsample(vm, releaseSrc, releasePtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(releasePtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(releasePtr + (usize(k) << 2), last)
          }
        }
        const exponentSrc: usize = exponentAudioResult.ptr
        const exponentBuf: Float32Array = vm.arena.get(baseProcLen)
        const exponentPtr: usize = exponentBuf.dataStart
        downsample(vm, exponentSrc, exponentPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(exponentPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(exponentPtr + (usize(k) << 2), last)
          }
        }
        const trigSrc: usize = trigAudioResult.ptr
        const trigBuf: Float32Array = vm.arena.get(baseProcLen)
        const trigPtr: usize = trigBuf.dataStart
        downsample(vm, trigSrc, trigPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(trigPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(trigPtr + (usize(k) << 2), last)
          }
        }
        slot.history.write(params.sampleCount / osFactor, vm.paramScratch)
        instance.process(baseLen, params.sampleCount / osFactor, baseSampleRate, baseNyquist, basePiOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, baseInputPtr, baseOutPtr, attackValue, decayPtr, sustainPtr, releasePtr, exponentPtr, trigPtr)
        upsample(vm, baseOutPtr, outputPtr, baseLen, osFactor)
        genOpHelpers.extendBufferWithLastSample(outputPtr, params.bufferLength, genOpHelpers.alignedProcLength(params.bufferLength))
        vm.arena.release(baseOut)
        vm.arena.release(decayBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
        vm.arena.release(sustainBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
        vm.arena.release(releaseBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
        vm.arena.release(exponentBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
        vm.arena.release(trigBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      } else {
        slot.history.write(params.sampleCount, vm.paramScratch)
        instance.process(params.bufferLength, params.sampleCount, params.sampleRate, params.nyquist, params.piOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, inputPtr, outputPtr, attackValue, decayAudioResult.ptr, sustainAudioResult.ptr, releaseAudioResult.ptr, exponentAudioResult.ptr, trigAudioResult.ptr)
        genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      }
      genOpHelpers.writeOutputToHistoryRing(slot.history, outputPtr, params.bufferLength)
      genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      break
    }
    case 1: {
      const slot: GenSlot = vm.genPools[473].get()
      genOpHelpers.writeCallStackMetaToSlot(vm, slot)
      const procLen: i32 = genOpHelpers.alignedProcLength(params.bufferLength)
      genOpHelpers.writeInputToHistoryRingZero(slot.history)
      const inputPtr: usize = 0
      output = vm.arena.get(procLen)
      const outputPtr: usize = output.dataStart
      const instance: Adsr_default_attack_audio_decay_scalar_sustain_scalar_release_scalar_exponent_scalar_trig_scalar = changetype<Adsr_default_attack_audio_decay_scalar_sustain_scalar_release_scalar_exponent_scalar_trig_scalar>(slot.instance)
      vm.paramScratch[6] = instance.stage
      vm.paramScratch[7] = instance.env
      const attackAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, attackTagged, procLen)
      const osFactor: i32 = genOpHelpers.getOversampleFactor(vm)
      if (osFactor > 0) {
        const baseLen: i32 = params.bufferLength / osFactor
        const baseSampleRate: f32 = params.sampleRate / f32(osFactor)
        const baseNyquist: f32 = baseSampleRate * 0.5
        const basePiOverNyquist: f32 = f32(Mathf.PI) / baseNyquist
        const baseSampleCount: f32 = f32(params.sampleCount) / f32(osFactor)
        const baseProcLen: i32 = (baseLen + 15) & ~15
        const baseOut: Float32Array = vm.arena.get(baseProcLen)
        const baseOutPtr: usize = baseOut.dataStart
        let baseInputPtr: usize = inputPtr
        const attackSrc: usize = attackAudioResult.ptr
        const attackBuf: Float32Array = vm.arena.get(baseProcLen)
        const attackPtr: usize = attackBuf.dataStart
        downsample(vm, attackSrc, attackPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(attackPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(attackPtr + (usize(k) << 2), last)
          }
        }
        slot.history.write(params.sampleCount / osFactor, vm.paramScratch)
        instance.process(baseLen, params.sampleCount / osFactor, baseSampleRate, baseNyquist, basePiOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, baseInputPtr, baseOutPtr, decayValue, sustainValue, releaseValue, exponentValue, trigValue, attackPtr)
        upsample(vm, baseOutPtr, outputPtr, baseLen, osFactor)
        genOpHelpers.extendBufferWithLastSample(outputPtr, params.bufferLength, genOpHelpers.alignedProcLength(params.bufferLength))
        vm.arena.release(baseOut)
        vm.arena.release(attackBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
      } else {
        slot.history.write(params.sampleCount, vm.paramScratch)
        instance.process(params.bufferLength, params.sampleCount, params.sampleRate, params.nyquist, params.piOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, inputPtr, outputPtr, decayValue, sustainValue, releaseValue, exponentValue, trigValue, attackAudioResult.ptr)
        genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
      }
      genOpHelpers.writeOutputToHistoryRing(slot.history, outputPtr, params.bufferLength)
      genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
      break
    }
    case 33: {
      const slot: GenSlot = vm.genPools[474].get()
      genOpHelpers.writeCallStackMetaToSlot(vm, slot)
      const procLen: i32 = genOpHelpers.alignedProcLength(params.bufferLength)
      genOpHelpers.writeInputToHistoryRingZero(slot.history)
      const inputPtr: usize = 0
      output = vm.arena.get(procLen)
      const outputPtr: usize = output.dataStart
      const instance: Adsr_default_attack_audio_decay_scalar_sustain_scalar_release_scalar_exponent_scalar_trig_audio = changetype<Adsr_default_attack_audio_decay_scalar_sustain_scalar_release_scalar_exponent_scalar_trig_audio>(slot.instance)
      vm.paramScratch[6] = instance.stage
      vm.paramScratch[7] = instance.env
      const attackAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, attackTagged, procLen)
      const trigAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, trigTagged, procLen)
      const osFactor: i32 = genOpHelpers.getOversampleFactor(vm)
      if (osFactor > 0) {
        const baseLen: i32 = params.bufferLength / osFactor
        const baseSampleRate: f32 = params.sampleRate / f32(osFactor)
        const baseNyquist: f32 = baseSampleRate * 0.5
        const basePiOverNyquist: f32 = f32(Mathf.PI) / baseNyquist
        const baseSampleCount: f32 = f32(params.sampleCount) / f32(osFactor)
        const baseProcLen: i32 = (baseLen + 15) & ~15
        const baseOut: Float32Array = vm.arena.get(baseProcLen)
        const baseOutPtr: usize = baseOut.dataStart
        let baseInputPtr: usize = inputPtr
        const attackSrc: usize = attackAudioResult.ptr
        const attackBuf: Float32Array = vm.arena.get(baseProcLen)
        const attackPtr: usize = attackBuf.dataStart
        downsample(vm, attackSrc, attackPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(attackPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(attackPtr + (usize(k) << 2), last)
          }
        }
        const trigSrc: usize = trigAudioResult.ptr
        const trigBuf: Float32Array = vm.arena.get(baseProcLen)
        const trigPtr: usize = trigBuf.dataStart
        downsample(vm, trigSrc, trigPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(trigPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(trigPtr + (usize(k) << 2), last)
          }
        }
        slot.history.write(params.sampleCount / osFactor, vm.paramScratch)
        instance.process(baseLen, params.sampleCount / osFactor, baseSampleRate, baseNyquist, basePiOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, baseInputPtr, baseOutPtr, decayValue, sustainValue, releaseValue, exponentValue, attackPtr, trigPtr)
        upsample(vm, baseOutPtr, outputPtr, baseLen, osFactor)
        genOpHelpers.extendBufferWithLastSample(outputPtr, params.bufferLength, genOpHelpers.alignedProcLength(params.bufferLength))
        vm.arena.release(baseOut)
        vm.arena.release(attackBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
        vm.arena.release(trigBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      } else {
        slot.history.write(params.sampleCount, vm.paramScratch)
        instance.process(params.bufferLength, params.sampleCount, params.sampleRate, params.nyquist, params.piOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, inputPtr, outputPtr, decayValue, sustainValue, releaseValue, exponentValue, attackAudioResult.ptr, trigAudioResult.ptr)
        genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      }
      genOpHelpers.writeOutputToHistoryRing(slot.history, outputPtr, params.bufferLength)
      genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      break
    }
    case 17: {
      const slot: GenSlot = vm.genPools[475].get()
      genOpHelpers.writeCallStackMetaToSlot(vm, slot)
      const procLen: i32 = genOpHelpers.alignedProcLength(params.bufferLength)
      genOpHelpers.writeInputToHistoryRingZero(slot.history)
      const inputPtr: usize = 0
      output = vm.arena.get(procLen)
      const outputPtr: usize = output.dataStart
      const instance: Adsr_default_attack_audio_decay_scalar_sustain_scalar_release_scalar_exponent_audio_trig_scalar = changetype<Adsr_default_attack_audio_decay_scalar_sustain_scalar_release_scalar_exponent_audio_trig_scalar>(slot.instance)
      vm.paramScratch[6] = instance.stage
      vm.paramScratch[7] = instance.env
      const attackAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, attackTagged, procLen)
      const exponentAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, exponentTagged, procLen)
      const osFactor: i32 = genOpHelpers.getOversampleFactor(vm)
      if (osFactor > 0) {
        const baseLen: i32 = params.bufferLength / osFactor
        const baseSampleRate: f32 = params.sampleRate / f32(osFactor)
        const baseNyquist: f32 = baseSampleRate * 0.5
        const basePiOverNyquist: f32 = f32(Mathf.PI) / baseNyquist
        const baseSampleCount: f32 = f32(params.sampleCount) / f32(osFactor)
        const baseProcLen: i32 = (baseLen + 15) & ~15
        const baseOut: Float32Array = vm.arena.get(baseProcLen)
        const baseOutPtr: usize = baseOut.dataStart
        let baseInputPtr: usize = inputPtr
        const attackSrc: usize = attackAudioResult.ptr
        const attackBuf: Float32Array = vm.arena.get(baseProcLen)
        const attackPtr: usize = attackBuf.dataStart
        downsample(vm, attackSrc, attackPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(attackPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(attackPtr + (usize(k) << 2), last)
          }
        }
        const exponentSrc: usize = exponentAudioResult.ptr
        const exponentBuf: Float32Array = vm.arena.get(baseProcLen)
        const exponentPtr: usize = exponentBuf.dataStart
        downsample(vm, exponentSrc, exponentPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(exponentPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(exponentPtr + (usize(k) << 2), last)
          }
        }
        slot.history.write(params.sampleCount / osFactor, vm.paramScratch)
        instance.process(baseLen, params.sampleCount / osFactor, baseSampleRate, baseNyquist, basePiOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, baseInputPtr, baseOutPtr, decayValue, sustainValue, releaseValue, trigValue, attackPtr, exponentPtr)
        upsample(vm, baseOutPtr, outputPtr, baseLen, osFactor)
        genOpHelpers.extendBufferWithLastSample(outputPtr, params.bufferLength, genOpHelpers.alignedProcLength(params.bufferLength))
        vm.arena.release(baseOut)
        vm.arena.release(attackBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
        vm.arena.release(exponentBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
      } else {
        slot.history.write(params.sampleCount, vm.paramScratch)
        instance.process(params.bufferLength, params.sampleCount, params.sampleRate, params.nyquist, params.piOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, inputPtr, outputPtr, decayValue, sustainValue, releaseValue, trigValue, attackAudioResult.ptr, exponentAudioResult.ptr)
        genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
      }
      genOpHelpers.writeOutputToHistoryRing(slot.history, outputPtr, params.bufferLength)
      genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
      break
    }
    case 49: {
      const slot: GenSlot = vm.genPools[476].get()
      genOpHelpers.writeCallStackMetaToSlot(vm, slot)
      const procLen: i32 = genOpHelpers.alignedProcLength(params.bufferLength)
      genOpHelpers.writeInputToHistoryRingZero(slot.history)
      const inputPtr: usize = 0
      output = vm.arena.get(procLen)
      const outputPtr: usize = output.dataStart
      const instance: Adsr_default_attack_audio_decay_scalar_sustain_scalar_release_scalar_exponent_audio_trig_audio = changetype<Adsr_default_attack_audio_decay_scalar_sustain_scalar_release_scalar_exponent_audio_trig_audio>(slot.instance)
      vm.paramScratch[6] = instance.stage
      vm.paramScratch[7] = instance.env
      const attackAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, attackTagged, procLen)
      const exponentAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, exponentTagged, procLen)
      const trigAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, trigTagged, procLen)
      const osFactor: i32 = genOpHelpers.getOversampleFactor(vm)
      if (osFactor > 0) {
        const baseLen: i32 = params.bufferLength / osFactor
        const baseSampleRate: f32 = params.sampleRate / f32(osFactor)
        const baseNyquist: f32 = baseSampleRate * 0.5
        const basePiOverNyquist: f32 = f32(Mathf.PI) / baseNyquist
        const baseSampleCount: f32 = f32(params.sampleCount) / f32(osFactor)
        const baseProcLen: i32 = (baseLen + 15) & ~15
        const baseOut: Float32Array = vm.arena.get(baseProcLen)
        const baseOutPtr: usize = baseOut.dataStart
        let baseInputPtr: usize = inputPtr
        const attackSrc: usize = attackAudioResult.ptr
        const attackBuf: Float32Array = vm.arena.get(baseProcLen)
        const attackPtr: usize = attackBuf.dataStart
        downsample(vm, attackSrc, attackPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(attackPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(attackPtr + (usize(k) << 2), last)
          }
        }
        const exponentSrc: usize = exponentAudioResult.ptr
        const exponentBuf: Float32Array = vm.arena.get(baseProcLen)
        const exponentPtr: usize = exponentBuf.dataStart
        downsample(vm, exponentSrc, exponentPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(exponentPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(exponentPtr + (usize(k) << 2), last)
          }
        }
        const trigSrc: usize = trigAudioResult.ptr
        const trigBuf: Float32Array = vm.arena.get(baseProcLen)
        const trigPtr: usize = trigBuf.dataStart
        downsample(vm, trigSrc, trigPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(trigPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(trigPtr + (usize(k) << 2), last)
          }
        }
        slot.history.write(params.sampleCount / osFactor, vm.paramScratch)
        instance.process(baseLen, params.sampleCount / osFactor, baseSampleRate, baseNyquist, basePiOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, baseInputPtr, baseOutPtr, decayValue, sustainValue, releaseValue, attackPtr, exponentPtr, trigPtr)
        upsample(vm, baseOutPtr, outputPtr, baseLen, osFactor)
        genOpHelpers.extendBufferWithLastSample(outputPtr, params.bufferLength, genOpHelpers.alignedProcLength(params.bufferLength))
        vm.arena.release(baseOut)
        vm.arena.release(attackBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
        vm.arena.release(exponentBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
        vm.arena.release(trigBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      } else {
        slot.history.write(params.sampleCount, vm.paramScratch)
        instance.process(params.bufferLength, params.sampleCount, params.sampleRate, params.nyquist, params.piOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, inputPtr, outputPtr, decayValue, sustainValue, releaseValue, attackAudioResult.ptr, exponentAudioResult.ptr, trigAudioResult.ptr)
        genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      }
      genOpHelpers.writeOutputToHistoryRing(slot.history, outputPtr, params.bufferLength)
      genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      break
    }
    case 9: {
      const slot: GenSlot = vm.genPools[477].get()
      genOpHelpers.writeCallStackMetaToSlot(vm, slot)
      const procLen: i32 = genOpHelpers.alignedProcLength(params.bufferLength)
      genOpHelpers.writeInputToHistoryRingZero(slot.history)
      const inputPtr: usize = 0
      output = vm.arena.get(procLen)
      const outputPtr: usize = output.dataStart
      const instance: Adsr_default_attack_audio_decay_scalar_sustain_scalar_release_audio_exponent_scalar_trig_scalar = changetype<Adsr_default_attack_audio_decay_scalar_sustain_scalar_release_audio_exponent_scalar_trig_scalar>(slot.instance)
      vm.paramScratch[6] = instance.stage
      vm.paramScratch[7] = instance.env
      const attackAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, attackTagged, procLen)
      const releaseAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, releaseTagged, procLen)
      const osFactor: i32 = genOpHelpers.getOversampleFactor(vm)
      if (osFactor > 0) {
        const baseLen: i32 = params.bufferLength / osFactor
        const baseSampleRate: f32 = params.sampleRate / f32(osFactor)
        const baseNyquist: f32 = baseSampleRate * 0.5
        const basePiOverNyquist: f32 = f32(Mathf.PI) / baseNyquist
        const baseSampleCount: f32 = f32(params.sampleCount) / f32(osFactor)
        const baseProcLen: i32 = (baseLen + 15) & ~15
        const baseOut: Float32Array = vm.arena.get(baseProcLen)
        const baseOutPtr: usize = baseOut.dataStart
        let baseInputPtr: usize = inputPtr
        const attackSrc: usize = attackAudioResult.ptr
        const attackBuf: Float32Array = vm.arena.get(baseProcLen)
        const attackPtr: usize = attackBuf.dataStart
        downsample(vm, attackSrc, attackPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(attackPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(attackPtr + (usize(k) << 2), last)
          }
        }
        const releaseSrc: usize = releaseAudioResult.ptr
        const releaseBuf: Float32Array = vm.arena.get(baseProcLen)
        const releasePtr: usize = releaseBuf.dataStart
        downsample(vm, releaseSrc, releasePtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(releasePtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(releasePtr + (usize(k) << 2), last)
          }
        }
        slot.history.write(params.sampleCount / osFactor, vm.paramScratch)
        instance.process(baseLen, params.sampleCount / osFactor, baseSampleRate, baseNyquist, basePiOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, baseInputPtr, baseOutPtr, decayValue, sustainValue, exponentValue, trigValue, attackPtr, releasePtr)
        upsample(vm, baseOutPtr, outputPtr, baseLen, osFactor)
        genOpHelpers.extendBufferWithLastSample(outputPtr, params.bufferLength, genOpHelpers.alignedProcLength(params.bufferLength))
        vm.arena.release(baseOut)
        vm.arena.release(attackBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
        vm.arena.release(releaseBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
      } else {
        slot.history.write(params.sampleCount, vm.paramScratch)
        instance.process(params.bufferLength, params.sampleCount, params.sampleRate, params.nyquist, params.piOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, inputPtr, outputPtr, decayValue, sustainValue, exponentValue, trigValue, attackAudioResult.ptr, releaseAudioResult.ptr)
        genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
      }
      genOpHelpers.writeOutputToHistoryRing(slot.history, outputPtr, params.bufferLength)
      genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
      break
    }
    case 41: {
      const slot: GenSlot = vm.genPools[478].get()
      genOpHelpers.writeCallStackMetaToSlot(vm, slot)
      const procLen: i32 = genOpHelpers.alignedProcLength(params.bufferLength)
      genOpHelpers.writeInputToHistoryRingZero(slot.history)
      const inputPtr: usize = 0
      output = vm.arena.get(procLen)
      const outputPtr: usize = output.dataStart
      const instance: Adsr_default_attack_audio_decay_scalar_sustain_scalar_release_audio_exponent_scalar_trig_audio = changetype<Adsr_default_attack_audio_decay_scalar_sustain_scalar_release_audio_exponent_scalar_trig_audio>(slot.instance)
      vm.paramScratch[6] = instance.stage
      vm.paramScratch[7] = instance.env
      const attackAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, attackTagged, procLen)
      const releaseAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, releaseTagged, procLen)
      const trigAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, trigTagged, procLen)
      const osFactor: i32 = genOpHelpers.getOversampleFactor(vm)
      if (osFactor > 0) {
        const baseLen: i32 = params.bufferLength / osFactor
        const baseSampleRate: f32 = params.sampleRate / f32(osFactor)
        const baseNyquist: f32 = baseSampleRate * 0.5
        const basePiOverNyquist: f32 = f32(Mathf.PI) / baseNyquist
        const baseSampleCount: f32 = f32(params.sampleCount) / f32(osFactor)
        const baseProcLen: i32 = (baseLen + 15) & ~15
        const baseOut: Float32Array = vm.arena.get(baseProcLen)
        const baseOutPtr: usize = baseOut.dataStart
        let baseInputPtr: usize = inputPtr
        const attackSrc: usize = attackAudioResult.ptr
        const attackBuf: Float32Array = vm.arena.get(baseProcLen)
        const attackPtr: usize = attackBuf.dataStart
        downsample(vm, attackSrc, attackPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(attackPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(attackPtr + (usize(k) << 2), last)
          }
        }
        const releaseSrc: usize = releaseAudioResult.ptr
        const releaseBuf: Float32Array = vm.arena.get(baseProcLen)
        const releasePtr: usize = releaseBuf.dataStart
        downsample(vm, releaseSrc, releasePtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(releasePtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(releasePtr + (usize(k) << 2), last)
          }
        }
        const trigSrc: usize = trigAudioResult.ptr
        const trigBuf: Float32Array = vm.arena.get(baseProcLen)
        const trigPtr: usize = trigBuf.dataStart
        downsample(vm, trigSrc, trigPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(trigPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(trigPtr + (usize(k) << 2), last)
          }
        }
        slot.history.write(params.sampleCount / osFactor, vm.paramScratch)
        instance.process(baseLen, params.sampleCount / osFactor, baseSampleRate, baseNyquist, basePiOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, baseInputPtr, baseOutPtr, decayValue, sustainValue, exponentValue, attackPtr, releasePtr, trigPtr)
        upsample(vm, baseOutPtr, outputPtr, baseLen, osFactor)
        genOpHelpers.extendBufferWithLastSample(outputPtr, params.bufferLength, genOpHelpers.alignedProcLength(params.bufferLength))
        vm.arena.release(baseOut)
        vm.arena.release(attackBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
        vm.arena.release(releaseBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
        vm.arena.release(trigBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      } else {
        slot.history.write(params.sampleCount, vm.paramScratch)
        instance.process(params.bufferLength, params.sampleCount, params.sampleRate, params.nyquist, params.piOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, inputPtr, outputPtr, decayValue, sustainValue, exponentValue, attackAudioResult.ptr, releaseAudioResult.ptr, trigAudioResult.ptr)
        genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      }
      genOpHelpers.writeOutputToHistoryRing(slot.history, outputPtr, params.bufferLength)
      genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      break
    }
    case 25: {
      const slot: GenSlot = vm.genPools[479].get()
      genOpHelpers.writeCallStackMetaToSlot(vm, slot)
      const procLen: i32 = genOpHelpers.alignedProcLength(params.bufferLength)
      genOpHelpers.writeInputToHistoryRingZero(slot.history)
      const inputPtr: usize = 0
      output = vm.arena.get(procLen)
      const outputPtr: usize = output.dataStart
      const instance: Adsr_default_attack_audio_decay_scalar_sustain_scalar_release_audio_exponent_audio_trig_scalar = changetype<Adsr_default_attack_audio_decay_scalar_sustain_scalar_release_audio_exponent_audio_trig_scalar>(slot.instance)
      vm.paramScratch[6] = instance.stage
      vm.paramScratch[7] = instance.env
      const attackAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, attackTagged, procLen)
      const releaseAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, releaseTagged, procLen)
      const exponentAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, exponentTagged, procLen)
      const osFactor: i32 = genOpHelpers.getOversampleFactor(vm)
      if (osFactor > 0) {
        const baseLen: i32 = params.bufferLength / osFactor
        const baseSampleRate: f32 = params.sampleRate / f32(osFactor)
        const baseNyquist: f32 = baseSampleRate * 0.5
        const basePiOverNyquist: f32 = f32(Mathf.PI) / baseNyquist
        const baseSampleCount: f32 = f32(params.sampleCount) / f32(osFactor)
        const baseProcLen: i32 = (baseLen + 15) & ~15
        const baseOut: Float32Array = vm.arena.get(baseProcLen)
        const baseOutPtr: usize = baseOut.dataStart
        let baseInputPtr: usize = inputPtr
        const attackSrc: usize = attackAudioResult.ptr
        const attackBuf: Float32Array = vm.arena.get(baseProcLen)
        const attackPtr: usize = attackBuf.dataStart
        downsample(vm, attackSrc, attackPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(attackPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(attackPtr + (usize(k) << 2), last)
          }
        }
        const releaseSrc: usize = releaseAudioResult.ptr
        const releaseBuf: Float32Array = vm.arena.get(baseProcLen)
        const releasePtr: usize = releaseBuf.dataStart
        downsample(vm, releaseSrc, releasePtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(releasePtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(releasePtr + (usize(k) << 2), last)
          }
        }
        const exponentSrc: usize = exponentAudioResult.ptr
        const exponentBuf: Float32Array = vm.arena.get(baseProcLen)
        const exponentPtr: usize = exponentBuf.dataStart
        downsample(vm, exponentSrc, exponentPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(exponentPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(exponentPtr + (usize(k) << 2), last)
          }
        }
        slot.history.write(params.sampleCount / osFactor, vm.paramScratch)
        instance.process(baseLen, params.sampleCount / osFactor, baseSampleRate, baseNyquist, basePiOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, baseInputPtr, baseOutPtr, decayValue, sustainValue, trigValue, attackPtr, releasePtr, exponentPtr)
        upsample(vm, baseOutPtr, outputPtr, baseLen, osFactor)
        genOpHelpers.extendBufferWithLastSample(outputPtr, params.bufferLength, genOpHelpers.alignedProcLength(params.bufferLength))
        vm.arena.release(baseOut)
        vm.arena.release(attackBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
        vm.arena.release(releaseBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
        vm.arena.release(exponentBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
      } else {
        slot.history.write(params.sampleCount, vm.paramScratch)
        instance.process(params.bufferLength, params.sampleCount, params.sampleRate, params.nyquist, params.piOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, inputPtr, outputPtr, decayValue, sustainValue, trigValue, attackAudioResult.ptr, releaseAudioResult.ptr, exponentAudioResult.ptr)
        genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
      }
      genOpHelpers.writeOutputToHistoryRing(slot.history, outputPtr, params.bufferLength)
      genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
      break
    }
    case 57: {
      const slot: GenSlot = vm.genPools[480].get()
      genOpHelpers.writeCallStackMetaToSlot(vm, slot)
      const procLen: i32 = genOpHelpers.alignedProcLength(params.bufferLength)
      genOpHelpers.writeInputToHistoryRingZero(slot.history)
      const inputPtr: usize = 0
      output = vm.arena.get(procLen)
      const outputPtr: usize = output.dataStart
      const instance: Adsr_default_attack_audio_decay_scalar_sustain_scalar_release_audio_exponent_audio_trig_audio = changetype<Adsr_default_attack_audio_decay_scalar_sustain_scalar_release_audio_exponent_audio_trig_audio>(slot.instance)
      vm.paramScratch[6] = instance.stage
      vm.paramScratch[7] = instance.env
      const attackAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, attackTagged, procLen)
      const releaseAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, releaseTagged, procLen)
      const exponentAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, exponentTagged, procLen)
      const trigAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, trigTagged, procLen)
      const osFactor: i32 = genOpHelpers.getOversampleFactor(vm)
      if (osFactor > 0) {
        const baseLen: i32 = params.bufferLength / osFactor
        const baseSampleRate: f32 = params.sampleRate / f32(osFactor)
        const baseNyquist: f32 = baseSampleRate * 0.5
        const basePiOverNyquist: f32 = f32(Mathf.PI) / baseNyquist
        const baseSampleCount: f32 = f32(params.sampleCount) / f32(osFactor)
        const baseProcLen: i32 = (baseLen + 15) & ~15
        const baseOut: Float32Array = vm.arena.get(baseProcLen)
        const baseOutPtr: usize = baseOut.dataStart
        let baseInputPtr: usize = inputPtr
        const attackSrc: usize = attackAudioResult.ptr
        const attackBuf: Float32Array = vm.arena.get(baseProcLen)
        const attackPtr: usize = attackBuf.dataStart
        downsample(vm, attackSrc, attackPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(attackPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(attackPtr + (usize(k) << 2), last)
          }
        }
        const releaseSrc: usize = releaseAudioResult.ptr
        const releaseBuf: Float32Array = vm.arena.get(baseProcLen)
        const releasePtr: usize = releaseBuf.dataStart
        downsample(vm, releaseSrc, releasePtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(releasePtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(releasePtr + (usize(k) << 2), last)
          }
        }
        const exponentSrc: usize = exponentAudioResult.ptr
        const exponentBuf: Float32Array = vm.arena.get(baseProcLen)
        const exponentPtr: usize = exponentBuf.dataStart
        downsample(vm, exponentSrc, exponentPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(exponentPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(exponentPtr + (usize(k) << 2), last)
          }
        }
        const trigSrc: usize = trigAudioResult.ptr
        const trigBuf: Float32Array = vm.arena.get(baseProcLen)
        const trigPtr: usize = trigBuf.dataStart
        downsample(vm, trigSrc, trigPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(trigPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(trigPtr + (usize(k) << 2), last)
          }
        }
        slot.history.write(params.sampleCount / osFactor, vm.paramScratch)
        instance.process(baseLen, params.sampleCount / osFactor, baseSampleRate, baseNyquist, basePiOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, baseInputPtr, baseOutPtr, decayValue, sustainValue, attackPtr, releasePtr, exponentPtr, trigPtr)
        upsample(vm, baseOutPtr, outputPtr, baseLen, osFactor)
        genOpHelpers.extendBufferWithLastSample(outputPtr, params.bufferLength, genOpHelpers.alignedProcLength(params.bufferLength))
        vm.arena.release(baseOut)
        vm.arena.release(attackBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
        vm.arena.release(releaseBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
        vm.arena.release(exponentBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
        vm.arena.release(trigBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      } else {
        slot.history.write(params.sampleCount, vm.paramScratch)
        instance.process(params.bufferLength, params.sampleCount, params.sampleRate, params.nyquist, params.piOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, inputPtr, outputPtr, decayValue, sustainValue, attackAudioResult.ptr, releaseAudioResult.ptr, exponentAudioResult.ptr, trigAudioResult.ptr)
        genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      }
      genOpHelpers.writeOutputToHistoryRing(slot.history, outputPtr, params.bufferLength)
      genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      break
    }
    case 5: {
      const slot: GenSlot = vm.genPools[481].get()
      genOpHelpers.writeCallStackMetaToSlot(vm, slot)
      const procLen: i32 = genOpHelpers.alignedProcLength(params.bufferLength)
      genOpHelpers.writeInputToHistoryRingZero(slot.history)
      const inputPtr: usize = 0
      output = vm.arena.get(procLen)
      const outputPtr: usize = output.dataStart
      const instance: Adsr_default_attack_audio_decay_scalar_sustain_audio_release_scalar_exponent_scalar_trig_scalar = changetype<Adsr_default_attack_audio_decay_scalar_sustain_audio_release_scalar_exponent_scalar_trig_scalar>(slot.instance)
      vm.paramScratch[6] = instance.stage
      vm.paramScratch[7] = instance.env
      const attackAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, attackTagged, procLen)
      const sustainAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, sustainTagged, procLen)
      const osFactor: i32 = genOpHelpers.getOversampleFactor(vm)
      if (osFactor > 0) {
        const baseLen: i32 = params.bufferLength / osFactor
        const baseSampleRate: f32 = params.sampleRate / f32(osFactor)
        const baseNyquist: f32 = baseSampleRate * 0.5
        const basePiOverNyquist: f32 = f32(Mathf.PI) / baseNyquist
        const baseSampleCount: f32 = f32(params.sampleCount) / f32(osFactor)
        const baseProcLen: i32 = (baseLen + 15) & ~15
        const baseOut: Float32Array = vm.arena.get(baseProcLen)
        const baseOutPtr: usize = baseOut.dataStart
        let baseInputPtr: usize = inputPtr
        const attackSrc: usize = attackAudioResult.ptr
        const attackBuf: Float32Array = vm.arena.get(baseProcLen)
        const attackPtr: usize = attackBuf.dataStart
        downsample(vm, attackSrc, attackPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(attackPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(attackPtr + (usize(k) << 2), last)
          }
        }
        const sustainSrc: usize = sustainAudioResult.ptr
        const sustainBuf: Float32Array = vm.arena.get(baseProcLen)
        const sustainPtr: usize = sustainBuf.dataStart
        downsample(vm, sustainSrc, sustainPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(sustainPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(sustainPtr + (usize(k) << 2), last)
          }
        }
        slot.history.write(params.sampleCount / osFactor, vm.paramScratch)
        instance.process(baseLen, params.sampleCount / osFactor, baseSampleRate, baseNyquist, basePiOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, baseInputPtr, baseOutPtr, decayValue, releaseValue, exponentValue, trigValue, attackPtr, sustainPtr)
        upsample(vm, baseOutPtr, outputPtr, baseLen, osFactor)
        genOpHelpers.extendBufferWithLastSample(outputPtr, params.bufferLength, genOpHelpers.alignedProcLength(params.bufferLength))
        vm.arena.release(baseOut)
        vm.arena.release(attackBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
        vm.arena.release(sustainBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
      } else {
        slot.history.write(params.sampleCount, vm.paramScratch)
        instance.process(params.bufferLength, params.sampleCount, params.sampleRate, params.nyquist, params.piOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, inputPtr, outputPtr, decayValue, releaseValue, exponentValue, trigValue, attackAudioResult.ptr, sustainAudioResult.ptr)
        genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
      }
      genOpHelpers.writeOutputToHistoryRing(slot.history, outputPtr, params.bufferLength)
      genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
      break
    }
    case 37: {
      const slot: GenSlot = vm.genPools[482].get()
      genOpHelpers.writeCallStackMetaToSlot(vm, slot)
      const procLen: i32 = genOpHelpers.alignedProcLength(params.bufferLength)
      genOpHelpers.writeInputToHistoryRingZero(slot.history)
      const inputPtr: usize = 0
      output = vm.arena.get(procLen)
      const outputPtr: usize = output.dataStart
      const instance: Adsr_default_attack_audio_decay_scalar_sustain_audio_release_scalar_exponent_scalar_trig_audio = changetype<Adsr_default_attack_audio_decay_scalar_sustain_audio_release_scalar_exponent_scalar_trig_audio>(slot.instance)
      vm.paramScratch[6] = instance.stage
      vm.paramScratch[7] = instance.env
      const attackAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, attackTagged, procLen)
      const sustainAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, sustainTagged, procLen)
      const trigAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, trigTagged, procLen)
      const osFactor: i32 = genOpHelpers.getOversampleFactor(vm)
      if (osFactor > 0) {
        const baseLen: i32 = params.bufferLength / osFactor
        const baseSampleRate: f32 = params.sampleRate / f32(osFactor)
        const baseNyquist: f32 = baseSampleRate * 0.5
        const basePiOverNyquist: f32 = f32(Mathf.PI) / baseNyquist
        const baseSampleCount: f32 = f32(params.sampleCount) / f32(osFactor)
        const baseProcLen: i32 = (baseLen + 15) & ~15
        const baseOut: Float32Array = vm.arena.get(baseProcLen)
        const baseOutPtr: usize = baseOut.dataStart
        let baseInputPtr: usize = inputPtr
        const attackSrc: usize = attackAudioResult.ptr
        const attackBuf: Float32Array = vm.arena.get(baseProcLen)
        const attackPtr: usize = attackBuf.dataStart
        downsample(vm, attackSrc, attackPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(attackPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(attackPtr + (usize(k) << 2), last)
          }
        }
        const sustainSrc: usize = sustainAudioResult.ptr
        const sustainBuf: Float32Array = vm.arena.get(baseProcLen)
        const sustainPtr: usize = sustainBuf.dataStart
        downsample(vm, sustainSrc, sustainPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(sustainPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(sustainPtr + (usize(k) << 2), last)
          }
        }
        const trigSrc: usize = trigAudioResult.ptr
        const trigBuf: Float32Array = vm.arena.get(baseProcLen)
        const trigPtr: usize = trigBuf.dataStart
        downsample(vm, trigSrc, trigPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(trigPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(trigPtr + (usize(k) << 2), last)
          }
        }
        slot.history.write(params.sampleCount / osFactor, vm.paramScratch)
        instance.process(baseLen, params.sampleCount / osFactor, baseSampleRate, baseNyquist, basePiOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, baseInputPtr, baseOutPtr, decayValue, releaseValue, exponentValue, attackPtr, sustainPtr, trigPtr)
        upsample(vm, baseOutPtr, outputPtr, baseLen, osFactor)
        genOpHelpers.extendBufferWithLastSample(outputPtr, params.bufferLength, genOpHelpers.alignedProcLength(params.bufferLength))
        vm.arena.release(baseOut)
        vm.arena.release(attackBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
        vm.arena.release(sustainBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
        vm.arena.release(trigBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      } else {
        slot.history.write(params.sampleCount, vm.paramScratch)
        instance.process(params.bufferLength, params.sampleCount, params.sampleRate, params.nyquist, params.piOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, inputPtr, outputPtr, decayValue, releaseValue, exponentValue, attackAudioResult.ptr, sustainAudioResult.ptr, trigAudioResult.ptr)
        genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      }
      genOpHelpers.writeOutputToHistoryRing(slot.history, outputPtr, params.bufferLength)
      genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      break
    }
    case 21: {
      const slot: GenSlot = vm.genPools[483].get()
      genOpHelpers.writeCallStackMetaToSlot(vm, slot)
      const procLen: i32 = genOpHelpers.alignedProcLength(params.bufferLength)
      genOpHelpers.writeInputToHistoryRingZero(slot.history)
      const inputPtr: usize = 0
      output = vm.arena.get(procLen)
      const outputPtr: usize = output.dataStart
      const instance: Adsr_default_attack_audio_decay_scalar_sustain_audio_release_scalar_exponent_audio_trig_scalar = changetype<Adsr_default_attack_audio_decay_scalar_sustain_audio_release_scalar_exponent_audio_trig_scalar>(slot.instance)
      vm.paramScratch[6] = instance.stage
      vm.paramScratch[7] = instance.env
      const attackAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, attackTagged, procLen)
      const sustainAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, sustainTagged, procLen)
      const exponentAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, exponentTagged, procLen)
      const osFactor: i32 = genOpHelpers.getOversampleFactor(vm)
      if (osFactor > 0) {
        const baseLen: i32 = params.bufferLength / osFactor
        const baseSampleRate: f32 = params.sampleRate / f32(osFactor)
        const baseNyquist: f32 = baseSampleRate * 0.5
        const basePiOverNyquist: f32 = f32(Mathf.PI) / baseNyquist
        const baseSampleCount: f32 = f32(params.sampleCount) / f32(osFactor)
        const baseProcLen: i32 = (baseLen + 15) & ~15
        const baseOut: Float32Array = vm.arena.get(baseProcLen)
        const baseOutPtr: usize = baseOut.dataStart
        let baseInputPtr: usize = inputPtr
        const attackSrc: usize = attackAudioResult.ptr
        const attackBuf: Float32Array = vm.arena.get(baseProcLen)
        const attackPtr: usize = attackBuf.dataStart
        downsample(vm, attackSrc, attackPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(attackPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(attackPtr + (usize(k) << 2), last)
          }
        }
        const sustainSrc: usize = sustainAudioResult.ptr
        const sustainBuf: Float32Array = vm.arena.get(baseProcLen)
        const sustainPtr: usize = sustainBuf.dataStart
        downsample(vm, sustainSrc, sustainPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(sustainPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(sustainPtr + (usize(k) << 2), last)
          }
        }
        const exponentSrc: usize = exponentAudioResult.ptr
        const exponentBuf: Float32Array = vm.arena.get(baseProcLen)
        const exponentPtr: usize = exponentBuf.dataStart
        downsample(vm, exponentSrc, exponentPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(exponentPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(exponentPtr + (usize(k) << 2), last)
          }
        }
        slot.history.write(params.sampleCount / osFactor, vm.paramScratch)
        instance.process(baseLen, params.sampleCount / osFactor, baseSampleRate, baseNyquist, basePiOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, baseInputPtr, baseOutPtr, decayValue, releaseValue, trigValue, attackPtr, sustainPtr, exponentPtr)
        upsample(vm, baseOutPtr, outputPtr, baseLen, osFactor)
        genOpHelpers.extendBufferWithLastSample(outputPtr, params.bufferLength, genOpHelpers.alignedProcLength(params.bufferLength))
        vm.arena.release(baseOut)
        vm.arena.release(attackBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
        vm.arena.release(sustainBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
        vm.arena.release(exponentBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
      } else {
        slot.history.write(params.sampleCount, vm.paramScratch)
        instance.process(params.bufferLength, params.sampleCount, params.sampleRate, params.nyquist, params.piOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, inputPtr, outputPtr, decayValue, releaseValue, trigValue, attackAudioResult.ptr, sustainAudioResult.ptr, exponentAudioResult.ptr)
        genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
      }
      genOpHelpers.writeOutputToHistoryRing(slot.history, outputPtr, params.bufferLength)
      genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
      break
    }
    case 53: {
      const slot: GenSlot = vm.genPools[484].get()
      genOpHelpers.writeCallStackMetaToSlot(vm, slot)
      const procLen: i32 = genOpHelpers.alignedProcLength(params.bufferLength)
      genOpHelpers.writeInputToHistoryRingZero(slot.history)
      const inputPtr: usize = 0
      output = vm.arena.get(procLen)
      const outputPtr: usize = output.dataStart
      const instance: Adsr_default_attack_audio_decay_scalar_sustain_audio_release_scalar_exponent_audio_trig_audio = changetype<Adsr_default_attack_audio_decay_scalar_sustain_audio_release_scalar_exponent_audio_trig_audio>(slot.instance)
      vm.paramScratch[6] = instance.stage
      vm.paramScratch[7] = instance.env
      const attackAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, attackTagged, procLen)
      const sustainAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, sustainTagged, procLen)
      const exponentAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, exponentTagged, procLen)
      const trigAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, trigTagged, procLen)
      const osFactor: i32 = genOpHelpers.getOversampleFactor(vm)
      if (osFactor > 0) {
        const baseLen: i32 = params.bufferLength / osFactor
        const baseSampleRate: f32 = params.sampleRate / f32(osFactor)
        const baseNyquist: f32 = baseSampleRate * 0.5
        const basePiOverNyquist: f32 = f32(Mathf.PI) / baseNyquist
        const baseSampleCount: f32 = f32(params.sampleCount) / f32(osFactor)
        const baseProcLen: i32 = (baseLen + 15) & ~15
        const baseOut: Float32Array = vm.arena.get(baseProcLen)
        const baseOutPtr: usize = baseOut.dataStart
        let baseInputPtr: usize = inputPtr
        const attackSrc: usize = attackAudioResult.ptr
        const attackBuf: Float32Array = vm.arena.get(baseProcLen)
        const attackPtr: usize = attackBuf.dataStart
        downsample(vm, attackSrc, attackPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(attackPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(attackPtr + (usize(k) << 2), last)
          }
        }
        const sustainSrc: usize = sustainAudioResult.ptr
        const sustainBuf: Float32Array = vm.arena.get(baseProcLen)
        const sustainPtr: usize = sustainBuf.dataStart
        downsample(vm, sustainSrc, sustainPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(sustainPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(sustainPtr + (usize(k) << 2), last)
          }
        }
        const exponentSrc: usize = exponentAudioResult.ptr
        const exponentBuf: Float32Array = vm.arena.get(baseProcLen)
        const exponentPtr: usize = exponentBuf.dataStart
        downsample(vm, exponentSrc, exponentPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(exponentPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(exponentPtr + (usize(k) << 2), last)
          }
        }
        const trigSrc: usize = trigAudioResult.ptr
        const trigBuf: Float32Array = vm.arena.get(baseProcLen)
        const trigPtr: usize = trigBuf.dataStart
        downsample(vm, trigSrc, trigPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(trigPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(trigPtr + (usize(k) << 2), last)
          }
        }
        slot.history.write(params.sampleCount / osFactor, vm.paramScratch)
        instance.process(baseLen, params.sampleCount / osFactor, baseSampleRate, baseNyquist, basePiOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, baseInputPtr, baseOutPtr, decayValue, releaseValue, attackPtr, sustainPtr, exponentPtr, trigPtr)
        upsample(vm, baseOutPtr, outputPtr, baseLen, osFactor)
        genOpHelpers.extendBufferWithLastSample(outputPtr, params.bufferLength, genOpHelpers.alignedProcLength(params.bufferLength))
        vm.arena.release(baseOut)
        vm.arena.release(attackBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
        vm.arena.release(sustainBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
        vm.arena.release(exponentBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
        vm.arena.release(trigBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      } else {
        slot.history.write(params.sampleCount, vm.paramScratch)
        instance.process(params.bufferLength, params.sampleCount, params.sampleRate, params.nyquist, params.piOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, inputPtr, outputPtr, decayValue, releaseValue, attackAudioResult.ptr, sustainAudioResult.ptr, exponentAudioResult.ptr, trigAudioResult.ptr)
        genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      }
      genOpHelpers.writeOutputToHistoryRing(slot.history, outputPtr, params.bufferLength)
      genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      break
    }
    case 13: {
      const slot: GenSlot = vm.genPools[485].get()
      genOpHelpers.writeCallStackMetaToSlot(vm, slot)
      const procLen: i32 = genOpHelpers.alignedProcLength(params.bufferLength)
      genOpHelpers.writeInputToHistoryRingZero(slot.history)
      const inputPtr: usize = 0
      output = vm.arena.get(procLen)
      const outputPtr: usize = output.dataStart
      const instance: Adsr_default_attack_audio_decay_scalar_sustain_audio_release_audio_exponent_scalar_trig_scalar = changetype<Adsr_default_attack_audio_decay_scalar_sustain_audio_release_audio_exponent_scalar_trig_scalar>(slot.instance)
      vm.paramScratch[6] = instance.stage
      vm.paramScratch[7] = instance.env
      const attackAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, attackTagged, procLen)
      const sustainAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, sustainTagged, procLen)
      const releaseAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, releaseTagged, procLen)
      const osFactor: i32 = genOpHelpers.getOversampleFactor(vm)
      if (osFactor > 0) {
        const baseLen: i32 = params.bufferLength / osFactor
        const baseSampleRate: f32 = params.sampleRate / f32(osFactor)
        const baseNyquist: f32 = baseSampleRate * 0.5
        const basePiOverNyquist: f32 = f32(Mathf.PI) / baseNyquist
        const baseSampleCount: f32 = f32(params.sampleCount) / f32(osFactor)
        const baseProcLen: i32 = (baseLen + 15) & ~15
        const baseOut: Float32Array = vm.arena.get(baseProcLen)
        const baseOutPtr: usize = baseOut.dataStart
        let baseInputPtr: usize = inputPtr
        const attackSrc: usize = attackAudioResult.ptr
        const attackBuf: Float32Array = vm.arena.get(baseProcLen)
        const attackPtr: usize = attackBuf.dataStart
        downsample(vm, attackSrc, attackPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(attackPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(attackPtr + (usize(k) << 2), last)
          }
        }
        const sustainSrc: usize = sustainAudioResult.ptr
        const sustainBuf: Float32Array = vm.arena.get(baseProcLen)
        const sustainPtr: usize = sustainBuf.dataStart
        downsample(vm, sustainSrc, sustainPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(sustainPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(sustainPtr + (usize(k) << 2), last)
          }
        }
        const releaseSrc: usize = releaseAudioResult.ptr
        const releaseBuf: Float32Array = vm.arena.get(baseProcLen)
        const releasePtr: usize = releaseBuf.dataStart
        downsample(vm, releaseSrc, releasePtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(releasePtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(releasePtr + (usize(k) << 2), last)
          }
        }
        slot.history.write(params.sampleCount / osFactor, vm.paramScratch)
        instance.process(baseLen, params.sampleCount / osFactor, baseSampleRate, baseNyquist, basePiOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, baseInputPtr, baseOutPtr, decayValue, exponentValue, trigValue, attackPtr, sustainPtr, releasePtr)
        upsample(vm, baseOutPtr, outputPtr, baseLen, osFactor)
        genOpHelpers.extendBufferWithLastSample(outputPtr, params.bufferLength, genOpHelpers.alignedProcLength(params.bufferLength))
        vm.arena.release(baseOut)
        vm.arena.release(attackBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
        vm.arena.release(sustainBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
        vm.arena.release(releaseBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
      } else {
        slot.history.write(params.sampleCount, vm.paramScratch)
        instance.process(params.bufferLength, params.sampleCount, params.sampleRate, params.nyquist, params.piOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, inputPtr, outputPtr, decayValue, exponentValue, trigValue, attackAudioResult.ptr, sustainAudioResult.ptr, releaseAudioResult.ptr)
        genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
      }
      genOpHelpers.writeOutputToHistoryRing(slot.history, outputPtr, params.bufferLength)
      genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
      break
    }
    case 45: {
      const slot: GenSlot = vm.genPools[486].get()
      genOpHelpers.writeCallStackMetaToSlot(vm, slot)
      const procLen: i32 = genOpHelpers.alignedProcLength(params.bufferLength)
      genOpHelpers.writeInputToHistoryRingZero(slot.history)
      const inputPtr: usize = 0
      output = vm.arena.get(procLen)
      const outputPtr: usize = output.dataStart
      const instance: Adsr_default_attack_audio_decay_scalar_sustain_audio_release_audio_exponent_scalar_trig_audio = changetype<Adsr_default_attack_audio_decay_scalar_sustain_audio_release_audio_exponent_scalar_trig_audio>(slot.instance)
      vm.paramScratch[6] = instance.stage
      vm.paramScratch[7] = instance.env
      const attackAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, attackTagged, procLen)
      const sustainAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, sustainTagged, procLen)
      const releaseAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, releaseTagged, procLen)
      const trigAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, trigTagged, procLen)
      const osFactor: i32 = genOpHelpers.getOversampleFactor(vm)
      if (osFactor > 0) {
        const baseLen: i32 = params.bufferLength / osFactor
        const baseSampleRate: f32 = params.sampleRate / f32(osFactor)
        const baseNyquist: f32 = baseSampleRate * 0.5
        const basePiOverNyquist: f32 = f32(Mathf.PI) / baseNyquist
        const baseSampleCount: f32 = f32(params.sampleCount) / f32(osFactor)
        const baseProcLen: i32 = (baseLen + 15) & ~15
        const baseOut: Float32Array = vm.arena.get(baseProcLen)
        const baseOutPtr: usize = baseOut.dataStart
        let baseInputPtr: usize = inputPtr
        const attackSrc: usize = attackAudioResult.ptr
        const attackBuf: Float32Array = vm.arena.get(baseProcLen)
        const attackPtr: usize = attackBuf.dataStart
        downsample(vm, attackSrc, attackPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(attackPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(attackPtr + (usize(k) << 2), last)
          }
        }
        const sustainSrc: usize = sustainAudioResult.ptr
        const sustainBuf: Float32Array = vm.arena.get(baseProcLen)
        const sustainPtr: usize = sustainBuf.dataStart
        downsample(vm, sustainSrc, sustainPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(sustainPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(sustainPtr + (usize(k) << 2), last)
          }
        }
        const releaseSrc: usize = releaseAudioResult.ptr
        const releaseBuf: Float32Array = vm.arena.get(baseProcLen)
        const releasePtr: usize = releaseBuf.dataStart
        downsample(vm, releaseSrc, releasePtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(releasePtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(releasePtr + (usize(k) << 2), last)
          }
        }
        const trigSrc: usize = trigAudioResult.ptr
        const trigBuf: Float32Array = vm.arena.get(baseProcLen)
        const trigPtr: usize = trigBuf.dataStart
        downsample(vm, trigSrc, trigPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(trigPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(trigPtr + (usize(k) << 2), last)
          }
        }
        slot.history.write(params.sampleCount / osFactor, vm.paramScratch)
        instance.process(baseLen, params.sampleCount / osFactor, baseSampleRate, baseNyquist, basePiOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, baseInputPtr, baseOutPtr, decayValue, exponentValue, attackPtr, sustainPtr, releasePtr, trigPtr)
        upsample(vm, baseOutPtr, outputPtr, baseLen, osFactor)
        genOpHelpers.extendBufferWithLastSample(outputPtr, params.bufferLength, genOpHelpers.alignedProcLength(params.bufferLength))
        vm.arena.release(baseOut)
        vm.arena.release(attackBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
        vm.arena.release(sustainBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
        vm.arena.release(releaseBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
        vm.arena.release(trigBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      } else {
        slot.history.write(params.sampleCount, vm.paramScratch)
        instance.process(params.bufferLength, params.sampleCount, params.sampleRate, params.nyquist, params.piOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, inputPtr, outputPtr, decayValue, exponentValue, attackAudioResult.ptr, sustainAudioResult.ptr, releaseAudioResult.ptr, trigAudioResult.ptr)
        genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      }
      genOpHelpers.writeOutputToHistoryRing(slot.history, outputPtr, params.bufferLength)
      genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      break
    }
    case 29: {
      const slot: GenSlot = vm.genPools[487].get()
      genOpHelpers.writeCallStackMetaToSlot(vm, slot)
      const procLen: i32 = genOpHelpers.alignedProcLength(params.bufferLength)
      genOpHelpers.writeInputToHistoryRingZero(slot.history)
      const inputPtr: usize = 0
      output = vm.arena.get(procLen)
      const outputPtr: usize = output.dataStart
      const instance: Adsr_default_attack_audio_decay_scalar_sustain_audio_release_audio_exponent_audio_trig_scalar = changetype<Adsr_default_attack_audio_decay_scalar_sustain_audio_release_audio_exponent_audio_trig_scalar>(slot.instance)
      vm.paramScratch[6] = instance.stage
      vm.paramScratch[7] = instance.env
      const attackAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, attackTagged, procLen)
      const sustainAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, sustainTagged, procLen)
      const releaseAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, releaseTagged, procLen)
      const exponentAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, exponentTagged, procLen)
      const osFactor: i32 = genOpHelpers.getOversampleFactor(vm)
      if (osFactor > 0) {
        const baseLen: i32 = params.bufferLength / osFactor
        const baseSampleRate: f32 = params.sampleRate / f32(osFactor)
        const baseNyquist: f32 = baseSampleRate * 0.5
        const basePiOverNyquist: f32 = f32(Mathf.PI) / baseNyquist
        const baseSampleCount: f32 = f32(params.sampleCount) / f32(osFactor)
        const baseProcLen: i32 = (baseLen + 15) & ~15
        const baseOut: Float32Array = vm.arena.get(baseProcLen)
        const baseOutPtr: usize = baseOut.dataStart
        let baseInputPtr: usize = inputPtr
        const attackSrc: usize = attackAudioResult.ptr
        const attackBuf: Float32Array = vm.arena.get(baseProcLen)
        const attackPtr: usize = attackBuf.dataStart
        downsample(vm, attackSrc, attackPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(attackPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(attackPtr + (usize(k) << 2), last)
          }
        }
        const sustainSrc: usize = sustainAudioResult.ptr
        const sustainBuf: Float32Array = vm.arena.get(baseProcLen)
        const sustainPtr: usize = sustainBuf.dataStart
        downsample(vm, sustainSrc, sustainPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(sustainPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(sustainPtr + (usize(k) << 2), last)
          }
        }
        const releaseSrc: usize = releaseAudioResult.ptr
        const releaseBuf: Float32Array = vm.arena.get(baseProcLen)
        const releasePtr: usize = releaseBuf.dataStart
        downsample(vm, releaseSrc, releasePtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(releasePtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(releasePtr + (usize(k) << 2), last)
          }
        }
        const exponentSrc: usize = exponentAudioResult.ptr
        const exponentBuf: Float32Array = vm.arena.get(baseProcLen)
        const exponentPtr: usize = exponentBuf.dataStart
        downsample(vm, exponentSrc, exponentPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(exponentPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(exponentPtr + (usize(k) << 2), last)
          }
        }
        slot.history.write(params.sampleCount / osFactor, vm.paramScratch)
        instance.process(baseLen, params.sampleCount / osFactor, baseSampleRate, baseNyquist, basePiOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, baseInputPtr, baseOutPtr, decayValue, trigValue, attackPtr, sustainPtr, releasePtr, exponentPtr)
        upsample(vm, baseOutPtr, outputPtr, baseLen, osFactor)
        genOpHelpers.extendBufferWithLastSample(outputPtr, params.bufferLength, genOpHelpers.alignedProcLength(params.bufferLength))
        vm.arena.release(baseOut)
        vm.arena.release(attackBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
        vm.arena.release(sustainBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
        vm.arena.release(releaseBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
        vm.arena.release(exponentBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
      } else {
        slot.history.write(params.sampleCount, vm.paramScratch)
        instance.process(params.bufferLength, params.sampleCount, params.sampleRate, params.nyquist, params.piOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, inputPtr, outputPtr, decayValue, trigValue, attackAudioResult.ptr, sustainAudioResult.ptr, releaseAudioResult.ptr, exponentAudioResult.ptr)
        genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
      }
      genOpHelpers.writeOutputToHistoryRing(slot.history, outputPtr, params.bufferLength)
      genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
      break
    }
    case 61: {
      const slot: GenSlot = vm.genPools[488].get()
      genOpHelpers.writeCallStackMetaToSlot(vm, slot)
      const procLen: i32 = genOpHelpers.alignedProcLength(params.bufferLength)
      genOpHelpers.writeInputToHistoryRingZero(slot.history)
      const inputPtr: usize = 0
      output = vm.arena.get(procLen)
      const outputPtr: usize = output.dataStart
      const instance: Adsr_default_attack_audio_decay_scalar_sustain_audio_release_audio_exponent_audio_trig_audio = changetype<Adsr_default_attack_audio_decay_scalar_sustain_audio_release_audio_exponent_audio_trig_audio>(slot.instance)
      vm.paramScratch[6] = instance.stage
      vm.paramScratch[7] = instance.env
      const attackAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, attackTagged, procLen)
      const sustainAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, sustainTagged, procLen)
      const releaseAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, releaseTagged, procLen)
      const exponentAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, exponentTagged, procLen)
      const trigAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, trigTagged, procLen)
      const osFactor: i32 = genOpHelpers.getOversampleFactor(vm)
      if (osFactor > 0) {
        const baseLen: i32 = params.bufferLength / osFactor
        const baseSampleRate: f32 = params.sampleRate / f32(osFactor)
        const baseNyquist: f32 = baseSampleRate * 0.5
        const basePiOverNyquist: f32 = f32(Mathf.PI) / baseNyquist
        const baseSampleCount: f32 = f32(params.sampleCount) / f32(osFactor)
        const baseProcLen: i32 = (baseLen + 15) & ~15
        const baseOut: Float32Array = vm.arena.get(baseProcLen)
        const baseOutPtr: usize = baseOut.dataStart
        let baseInputPtr: usize = inputPtr
        const attackSrc: usize = attackAudioResult.ptr
        const attackBuf: Float32Array = vm.arena.get(baseProcLen)
        const attackPtr: usize = attackBuf.dataStart
        downsample(vm, attackSrc, attackPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(attackPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(attackPtr + (usize(k) << 2), last)
          }
        }
        const sustainSrc: usize = sustainAudioResult.ptr
        const sustainBuf: Float32Array = vm.arena.get(baseProcLen)
        const sustainPtr: usize = sustainBuf.dataStart
        downsample(vm, sustainSrc, sustainPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(sustainPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(sustainPtr + (usize(k) << 2), last)
          }
        }
        const releaseSrc: usize = releaseAudioResult.ptr
        const releaseBuf: Float32Array = vm.arena.get(baseProcLen)
        const releasePtr: usize = releaseBuf.dataStart
        downsample(vm, releaseSrc, releasePtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(releasePtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(releasePtr + (usize(k) << 2), last)
          }
        }
        const exponentSrc: usize = exponentAudioResult.ptr
        const exponentBuf: Float32Array = vm.arena.get(baseProcLen)
        const exponentPtr: usize = exponentBuf.dataStart
        downsample(vm, exponentSrc, exponentPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(exponentPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(exponentPtr + (usize(k) << 2), last)
          }
        }
        const trigSrc: usize = trigAudioResult.ptr
        const trigBuf: Float32Array = vm.arena.get(baseProcLen)
        const trigPtr: usize = trigBuf.dataStart
        downsample(vm, trigSrc, trigPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(trigPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(trigPtr + (usize(k) << 2), last)
          }
        }
        slot.history.write(params.sampleCount / osFactor, vm.paramScratch)
        instance.process(baseLen, params.sampleCount / osFactor, baseSampleRate, baseNyquist, basePiOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, baseInputPtr, baseOutPtr, decayValue, attackPtr, sustainPtr, releasePtr, exponentPtr, trigPtr)
        upsample(vm, baseOutPtr, outputPtr, baseLen, osFactor)
        genOpHelpers.extendBufferWithLastSample(outputPtr, params.bufferLength, genOpHelpers.alignedProcLength(params.bufferLength))
        vm.arena.release(baseOut)
        vm.arena.release(attackBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
        vm.arena.release(sustainBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
        vm.arena.release(releaseBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
        vm.arena.release(exponentBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
        vm.arena.release(trigBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      } else {
        slot.history.write(params.sampleCount, vm.paramScratch)
        instance.process(params.bufferLength, params.sampleCount, params.sampleRate, params.nyquist, params.piOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, inputPtr, outputPtr, decayValue, attackAudioResult.ptr, sustainAudioResult.ptr, releaseAudioResult.ptr, exponentAudioResult.ptr, trigAudioResult.ptr)
        genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      }
      genOpHelpers.writeOutputToHistoryRing(slot.history, outputPtr, params.bufferLength)
      genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      break
    }
    case 3: {
      const slot: GenSlot = vm.genPools[489].get()
      genOpHelpers.writeCallStackMetaToSlot(vm, slot)
      const procLen: i32 = genOpHelpers.alignedProcLength(params.bufferLength)
      genOpHelpers.writeInputToHistoryRingZero(slot.history)
      const inputPtr: usize = 0
      output = vm.arena.get(procLen)
      const outputPtr: usize = output.dataStart
      const instance: Adsr_default_attack_audio_decay_audio_sustain_scalar_release_scalar_exponent_scalar_trig_scalar = changetype<Adsr_default_attack_audio_decay_audio_sustain_scalar_release_scalar_exponent_scalar_trig_scalar>(slot.instance)
      vm.paramScratch[6] = instance.stage
      vm.paramScratch[7] = instance.env
      const attackAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, attackTagged, procLen)
      const decayAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, decayTagged, procLen)
      const osFactor: i32 = genOpHelpers.getOversampleFactor(vm)
      if (osFactor > 0) {
        const baseLen: i32 = params.bufferLength / osFactor
        const baseSampleRate: f32 = params.sampleRate / f32(osFactor)
        const baseNyquist: f32 = baseSampleRate * 0.5
        const basePiOverNyquist: f32 = f32(Mathf.PI) / baseNyquist
        const baseSampleCount: f32 = f32(params.sampleCount) / f32(osFactor)
        const baseProcLen: i32 = (baseLen + 15) & ~15
        const baseOut: Float32Array = vm.arena.get(baseProcLen)
        const baseOutPtr: usize = baseOut.dataStart
        let baseInputPtr: usize = inputPtr
        const attackSrc: usize = attackAudioResult.ptr
        const attackBuf: Float32Array = vm.arena.get(baseProcLen)
        const attackPtr: usize = attackBuf.dataStart
        downsample(vm, attackSrc, attackPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(attackPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(attackPtr + (usize(k) << 2), last)
          }
        }
        const decaySrc: usize = decayAudioResult.ptr
        const decayBuf: Float32Array = vm.arena.get(baseProcLen)
        const decayPtr: usize = decayBuf.dataStart
        downsample(vm, decaySrc, decayPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(decayPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(decayPtr + (usize(k) << 2), last)
          }
        }
        slot.history.write(params.sampleCount / osFactor, vm.paramScratch)
        instance.process(baseLen, params.sampleCount / osFactor, baseSampleRate, baseNyquist, basePiOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, baseInputPtr, baseOutPtr, sustainValue, releaseValue, exponentValue, trigValue, attackPtr, decayPtr)
        upsample(vm, baseOutPtr, outputPtr, baseLen, osFactor)
        genOpHelpers.extendBufferWithLastSample(outputPtr, params.bufferLength, genOpHelpers.alignedProcLength(params.bufferLength))
        vm.arena.release(baseOut)
        vm.arena.release(attackBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
        vm.arena.release(decayBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
      } else {
        slot.history.write(params.sampleCount, vm.paramScratch)
        instance.process(params.bufferLength, params.sampleCount, params.sampleRate, params.nyquist, params.piOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, inputPtr, outputPtr, sustainValue, releaseValue, exponentValue, trigValue, attackAudioResult.ptr, decayAudioResult.ptr)
        genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
      }
      genOpHelpers.writeOutputToHistoryRing(slot.history, outputPtr, params.bufferLength)
      genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
      break
    }
    case 35: {
      const slot: GenSlot = vm.genPools[490].get()
      genOpHelpers.writeCallStackMetaToSlot(vm, slot)
      const procLen: i32 = genOpHelpers.alignedProcLength(params.bufferLength)
      genOpHelpers.writeInputToHistoryRingZero(slot.history)
      const inputPtr: usize = 0
      output = vm.arena.get(procLen)
      const outputPtr: usize = output.dataStart
      const instance: Adsr_default_attack_audio_decay_audio_sustain_scalar_release_scalar_exponent_scalar_trig_audio = changetype<Adsr_default_attack_audio_decay_audio_sustain_scalar_release_scalar_exponent_scalar_trig_audio>(slot.instance)
      vm.paramScratch[6] = instance.stage
      vm.paramScratch[7] = instance.env
      const attackAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, attackTagged, procLen)
      const decayAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, decayTagged, procLen)
      const trigAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, trigTagged, procLen)
      const osFactor: i32 = genOpHelpers.getOversampleFactor(vm)
      if (osFactor > 0) {
        const baseLen: i32 = params.bufferLength / osFactor
        const baseSampleRate: f32 = params.sampleRate / f32(osFactor)
        const baseNyquist: f32 = baseSampleRate * 0.5
        const basePiOverNyquist: f32 = f32(Mathf.PI) / baseNyquist
        const baseSampleCount: f32 = f32(params.sampleCount) / f32(osFactor)
        const baseProcLen: i32 = (baseLen + 15) & ~15
        const baseOut: Float32Array = vm.arena.get(baseProcLen)
        const baseOutPtr: usize = baseOut.dataStart
        let baseInputPtr: usize = inputPtr
        const attackSrc: usize = attackAudioResult.ptr
        const attackBuf: Float32Array = vm.arena.get(baseProcLen)
        const attackPtr: usize = attackBuf.dataStart
        downsample(vm, attackSrc, attackPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(attackPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(attackPtr + (usize(k) << 2), last)
          }
        }
        const decaySrc: usize = decayAudioResult.ptr
        const decayBuf: Float32Array = vm.arena.get(baseProcLen)
        const decayPtr: usize = decayBuf.dataStart
        downsample(vm, decaySrc, decayPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(decayPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(decayPtr + (usize(k) << 2), last)
          }
        }
        const trigSrc: usize = trigAudioResult.ptr
        const trigBuf: Float32Array = vm.arena.get(baseProcLen)
        const trigPtr: usize = trigBuf.dataStart
        downsample(vm, trigSrc, trigPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(trigPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(trigPtr + (usize(k) << 2), last)
          }
        }
        slot.history.write(params.sampleCount / osFactor, vm.paramScratch)
        instance.process(baseLen, params.sampleCount / osFactor, baseSampleRate, baseNyquist, basePiOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, baseInputPtr, baseOutPtr, sustainValue, releaseValue, exponentValue, attackPtr, decayPtr, trigPtr)
        upsample(vm, baseOutPtr, outputPtr, baseLen, osFactor)
        genOpHelpers.extendBufferWithLastSample(outputPtr, params.bufferLength, genOpHelpers.alignedProcLength(params.bufferLength))
        vm.arena.release(baseOut)
        vm.arena.release(attackBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
        vm.arena.release(decayBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
        vm.arena.release(trigBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      } else {
        slot.history.write(params.sampleCount, vm.paramScratch)
        instance.process(params.bufferLength, params.sampleCount, params.sampleRate, params.nyquist, params.piOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, inputPtr, outputPtr, sustainValue, releaseValue, exponentValue, attackAudioResult.ptr, decayAudioResult.ptr, trigAudioResult.ptr)
        genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      }
      genOpHelpers.writeOutputToHistoryRing(slot.history, outputPtr, params.bufferLength)
      genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      break
    }
    case 19: {
      const slot: GenSlot = vm.genPools[491].get()
      genOpHelpers.writeCallStackMetaToSlot(vm, slot)
      const procLen: i32 = genOpHelpers.alignedProcLength(params.bufferLength)
      genOpHelpers.writeInputToHistoryRingZero(slot.history)
      const inputPtr: usize = 0
      output = vm.arena.get(procLen)
      const outputPtr: usize = output.dataStart
      const instance: Adsr_default_attack_audio_decay_audio_sustain_scalar_release_scalar_exponent_audio_trig_scalar = changetype<Adsr_default_attack_audio_decay_audio_sustain_scalar_release_scalar_exponent_audio_trig_scalar>(slot.instance)
      vm.paramScratch[6] = instance.stage
      vm.paramScratch[7] = instance.env
      const attackAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, attackTagged, procLen)
      const decayAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, decayTagged, procLen)
      const exponentAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, exponentTagged, procLen)
      const osFactor: i32 = genOpHelpers.getOversampleFactor(vm)
      if (osFactor > 0) {
        const baseLen: i32 = params.bufferLength / osFactor
        const baseSampleRate: f32 = params.sampleRate / f32(osFactor)
        const baseNyquist: f32 = baseSampleRate * 0.5
        const basePiOverNyquist: f32 = f32(Mathf.PI) / baseNyquist
        const baseSampleCount: f32 = f32(params.sampleCount) / f32(osFactor)
        const baseProcLen: i32 = (baseLen + 15) & ~15
        const baseOut: Float32Array = vm.arena.get(baseProcLen)
        const baseOutPtr: usize = baseOut.dataStart
        let baseInputPtr: usize = inputPtr
        const attackSrc: usize = attackAudioResult.ptr
        const attackBuf: Float32Array = vm.arena.get(baseProcLen)
        const attackPtr: usize = attackBuf.dataStart
        downsample(vm, attackSrc, attackPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(attackPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(attackPtr + (usize(k) << 2), last)
          }
        }
        const decaySrc: usize = decayAudioResult.ptr
        const decayBuf: Float32Array = vm.arena.get(baseProcLen)
        const decayPtr: usize = decayBuf.dataStart
        downsample(vm, decaySrc, decayPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(decayPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(decayPtr + (usize(k) << 2), last)
          }
        }
        const exponentSrc: usize = exponentAudioResult.ptr
        const exponentBuf: Float32Array = vm.arena.get(baseProcLen)
        const exponentPtr: usize = exponentBuf.dataStart
        downsample(vm, exponentSrc, exponentPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(exponentPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(exponentPtr + (usize(k) << 2), last)
          }
        }
        slot.history.write(params.sampleCount / osFactor, vm.paramScratch)
        instance.process(baseLen, params.sampleCount / osFactor, baseSampleRate, baseNyquist, basePiOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, baseInputPtr, baseOutPtr, sustainValue, releaseValue, trigValue, attackPtr, decayPtr, exponentPtr)
        upsample(vm, baseOutPtr, outputPtr, baseLen, osFactor)
        genOpHelpers.extendBufferWithLastSample(outputPtr, params.bufferLength, genOpHelpers.alignedProcLength(params.bufferLength))
        vm.arena.release(baseOut)
        vm.arena.release(attackBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
        vm.arena.release(decayBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
        vm.arena.release(exponentBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
      } else {
        slot.history.write(params.sampleCount, vm.paramScratch)
        instance.process(params.bufferLength, params.sampleCount, params.sampleRate, params.nyquist, params.piOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, inputPtr, outputPtr, sustainValue, releaseValue, trigValue, attackAudioResult.ptr, decayAudioResult.ptr, exponentAudioResult.ptr)
        genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
      }
      genOpHelpers.writeOutputToHistoryRing(slot.history, outputPtr, params.bufferLength)
      genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
      break
    }
    case 51: {
      const slot: GenSlot = vm.genPools[492].get()
      genOpHelpers.writeCallStackMetaToSlot(vm, slot)
      const procLen: i32 = genOpHelpers.alignedProcLength(params.bufferLength)
      genOpHelpers.writeInputToHistoryRingZero(slot.history)
      const inputPtr: usize = 0
      output = vm.arena.get(procLen)
      const outputPtr: usize = output.dataStart
      const instance: Adsr_default_attack_audio_decay_audio_sustain_scalar_release_scalar_exponent_audio_trig_audio = changetype<Adsr_default_attack_audio_decay_audio_sustain_scalar_release_scalar_exponent_audio_trig_audio>(slot.instance)
      vm.paramScratch[6] = instance.stage
      vm.paramScratch[7] = instance.env
      const attackAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, attackTagged, procLen)
      const decayAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, decayTagged, procLen)
      const exponentAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, exponentTagged, procLen)
      const trigAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, trigTagged, procLen)
      const osFactor: i32 = genOpHelpers.getOversampleFactor(vm)
      if (osFactor > 0) {
        const baseLen: i32 = params.bufferLength / osFactor
        const baseSampleRate: f32 = params.sampleRate / f32(osFactor)
        const baseNyquist: f32 = baseSampleRate * 0.5
        const basePiOverNyquist: f32 = f32(Mathf.PI) / baseNyquist
        const baseSampleCount: f32 = f32(params.sampleCount) / f32(osFactor)
        const baseProcLen: i32 = (baseLen + 15) & ~15
        const baseOut: Float32Array = vm.arena.get(baseProcLen)
        const baseOutPtr: usize = baseOut.dataStart
        let baseInputPtr: usize = inputPtr
        const attackSrc: usize = attackAudioResult.ptr
        const attackBuf: Float32Array = vm.arena.get(baseProcLen)
        const attackPtr: usize = attackBuf.dataStart
        downsample(vm, attackSrc, attackPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(attackPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(attackPtr + (usize(k) << 2), last)
          }
        }
        const decaySrc: usize = decayAudioResult.ptr
        const decayBuf: Float32Array = vm.arena.get(baseProcLen)
        const decayPtr: usize = decayBuf.dataStart
        downsample(vm, decaySrc, decayPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(decayPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(decayPtr + (usize(k) << 2), last)
          }
        }
        const exponentSrc: usize = exponentAudioResult.ptr
        const exponentBuf: Float32Array = vm.arena.get(baseProcLen)
        const exponentPtr: usize = exponentBuf.dataStart
        downsample(vm, exponentSrc, exponentPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(exponentPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(exponentPtr + (usize(k) << 2), last)
          }
        }
        const trigSrc: usize = trigAudioResult.ptr
        const trigBuf: Float32Array = vm.arena.get(baseProcLen)
        const trigPtr: usize = trigBuf.dataStart
        downsample(vm, trigSrc, trigPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(trigPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(trigPtr + (usize(k) << 2), last)
          }
        }
        slot.history.write(params.sampleCount / osFactor, vm.paramScratch)
        instance.process(baseLen, params.sampleCount / osFactor, baseSampleRate, baseNyquist, basePiOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, baseInputPtr, baseOutPtr, sustainValue, releaseValue, attackPtr, decayPtr, exponentPtr, trigPtr)
        upsample(vm, baseOutPtr, outputPtr, baseLen, osFactor)
        genOpHelpers.extendBufferWithLastSample(outputPtr, params.bufferLength, genOpHelpers.alignedProcLength(params.bufferLength))
        vm.arena.release(baseOut)
        vm.arena.release(attackBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
        vm.arena.release(decayBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
        vm.arena.release(exponentBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
        vm.arena.release(trigBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      } else {
        slot.history.write(params.sampleCount, vm.paramScratch)
        instance.process(params.bufferLength, params.sampleCount, params.sampleRate, params.nyquist, params.piOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, inputPtr, outputPtr, sustainValue, releaseValue, attackAudioResult.ptr, decayAudioResult.ptr, exponentAudioResult.ptr, trigAudioResult.ptr)
        genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      }
      genOpHelpers.writeOutputToHistoryRing(slot.history, outputPtr, params.bufferLength)
      genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      break
    }
    case 11: {
      const slot: GenSlot = vm.genPools[493].get()
      genOpHelpers.writeCallStackMetaToSlot(vm, slot)
      const procLen: i32 = genOpHelpers.alignedProcLength(params.bufferLength)
      genOpHelpers.writeInputToHistoryRingZero(slot.history)
      const inputPtr: usize = 0
      output = vm.arena.get(procLen)
      const outputPtr: usize = output.dataStart
      const instance: Adsr_default_attack_audio_decay_audio_sustain_scalar_release_audio_exponent_scalar_trig_scalar = changetype<Adsr_default_attack_audio_decay_audio_sustain_scalar_release_audio_exponent_scalar_trig_scalar>(slot.instance)
      vm.paramScratch[6] = instance.stage
      vm.paramScratch[7] = instance.env
      const attackAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, attackTagged, procLen)
      const decayAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, decayTagged, procLen)
      const releaseAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, releaseTagged, procLen)
      const osFactor: i32 = genOpHelpers.getOversampleFactor(vm)
      if (osFactor > 0) {
        const baseLen: i32 = params.bufferLength / osFactor
        const baseSampleRate: f32 = params.sampleRate / f32(osFactor)
        const baseNyquist: f32 = baseSampleRate * 0.5
        const basePiOverNyquist: f32 = f32(Mathf.PI) / baseNyquist
        const baseSampleCount: f32 = f32(params.sampleCount) / f32(osFactor)
        const baseProcLen: i32 = (baseLen + 15) & ~15
        const baseOut: Float32Array = vm.arena.get(baseProcLen)
        const baseOutPtr: usize = baseOut.dataStart
        let baseInputPtr: usize = inputPtr
        const attackSrc: usize = attackAudioResult.ptr
        const attackBuf: Float32Array = vm.arena.get(baseProcLen)
        const attackPtr: usize = attackBuf.dataStart
        downsample(vm, attackSrc, attackPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(attackPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(attackPtr + (usize(k) << 2), last)
          }
        }
        const decaySrc: usize = decayAudioResult.ptr
        const decayBuf: Float32Array = vm.arena.get(baseProcLen)
        const decayPtr: usize = decayBuf.dataStart
        downsample(vm, decaySrc, decayPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(decayPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(decayPtr + (usize(k) << 2), last)
          }
        }
        const releaseSrc: usize = releaseAudioResult.ptr
        const releaseBuf: Float32Array = vm.arena.get(baseProcLen)
        const releasePtr: usize = releaseBuf.dataStart
        downsample(vm, releaseSrc, releasePtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(releasePtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(releasePtr + (usize(k) << 2), last)
          }
        }
        slot.history.write(params.sampleCount / osFactor, vm.paramScratch)
        instance.process(baseLen, params.sampleCount / osFactor, baseSampleRate, baseNyquist, basePiOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, baseInputPtr, baseOutPtr, sustainValue, exponentValue, trigValue, attackPtr, decayPtr, releasePtr)
        upsample(vm, baseOutPtr, outputPtr, baseLen, osFactor)
        genOpHelpers.extendBufferWithLastSample(outputPtr, params.bufferLength, genOpHelpers.alignedProcLength(params.bufferLength))
        vm.arena.release(baseOut)
        vm.arena.release(attackBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
        vm.arena.release(decayBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
        vm.arena.release(releaseBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
      } else {
        slot.history.write(params.sampleCount, vm.paramScratch)
        instance.process(params.bufferLength, params.sampleCount, params.sampleRate, params.nyquist, params.piOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, inputPtr, outputPtr, sustainValue, exponentValue, trigValue, attackAudioResult.ptr, decayAudioResult.ptr, releaseAudioResult.ptr)
        genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
      }
      genOpHelpers.writeOutputToHistoryRing(slot.history, outputPtr, params.bufferLength)
      genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
      break
    }
    case 43: {
      const slot: GenSlot = vm.genPools[494].get()
      genOpHelpers.writeCallStackMetaToSlot(vm, slot)
      const procLen: i32 = genOpHelpers.alignedProcLength(params.bufferLength)
      genOpHelpers.writeInputToHistoryRingZero(slot.history)
      const inputPtr: usize = 0
      output = vm.arena.get(procLen)
      const outputPtr: usize = output.dataStart
      const instance: Adsr_default_attack_audio_decay_audio_sustain_scalar_release_audio_exponent_scalar_trig_audio = changetype<Adsr_default_attack_audio_decay_audio_sustain_scalar_release_audio_exponent_scalar_trig_audio>(slot.instance)
      vm.paramScratch[6] = instance.stage
      vm.paramScratch[7] = instance.env
      const attackAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, attackTagged, procLen)
      const decayAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, decayTagged, procLen)
      const releaseAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, releaseTagged, procLen)
      const trigAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, trigTagged, procLen)
      const osFactor: i32 = genOpHelpers.getOversampleFactor(vm)
      if (osFactor > 0) {
        const baseLen: i32 = params.bufferLength / osFactor
        const baseSampleRate: f32 = params.sampleRate / f32(osFactor)
        const baseNyquist: f32 = baseSampleRate * 0.5
        const basePiOverNyquist: f32 = f32(Mathf.PI) / baseNyquist
        const baseSampleCount: f32 = f32(params.sampleCount) / f32(osFactor)
        const baseProcLen: i32 = (baseLen + 15) & ~15
        const baseOut: Float32Array = vm.arena.get(baseProcLen)
        const baseOutPtr: usize = baseOut.dataStart
        let baseInputPtr: usize = inputPtr
        const attackSrc: usize = attackAudioResult.ptr
        const attackBuf: Float32Array = vm.arena.get(baseProcLen)
        const attackPtr: usize = attackBuf.dataStart
        downsample(vm, attackSrc, attackPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(attackPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(attackPtr + (usize(k) << 2), last)
          }
        }
        const decaySrc: usize = decayAudioResult.ptr
        const decayBuf: Float32Array = vm.arena.get(baseProcLen)
        const decayPtr: usize = decayBuf.dataStart
        downsample(vm, decaySrc, decayPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(decayPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(decayPtr + (usize(k) << 2), last)
          }
        }
        const releaseSrc: usize = releaseAudioResult.ptr
        const releaseBuf: Float32Array = vm.arena.get(baseProcLen)
        const releasePtr: usize = releaseBuf.dataStart
        downsample(vm, releaseSrc, releasePtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(releasePtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(releasePtr + (usize(k) << 2), last)
          }
        }
        const trigSrc: usize = trigAudioResult.ptr
        const trigBuf: Float32Array = vm.arena.get(baseProcLen)
        const trigPtr: usize = trigBuf.dataStart
        downsample(vm, trigSrc, trigPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(trigPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(trigPtr + (usize(k) << 2), last)
          }
        }
        slot.history.write(params.sampleCount / osFactor, vm.paramScratch)
        instance.process(baseLen, params.sampleCount / osFactor, baseSampleRate, baseNyquist, basePiOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, baseInputPtr, baseOutPtr, sustainValue, exponentValue, attackPtr, decayPtr, releasePtr, trigPtr)
        upsample(vm, baseOutPtr, outputPtr, baseLen, osFactor)
        genOpHelpers.extendBufferWithLastSample(outputPtr, params.bufferLength, genOpHelpers.alignedProcLength(params.bufferLength))
        vm.arena.release(baseOut)
        vm.arena.release(attackBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
        vm.arena.release(decayBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
        vm.arena.release(releaseBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
        vm.arena.release(trigBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      } else {
        slot.history.write(params.sampleCount, vm.paramScratch)
        instance.process(params.bufferLength, params.sampleCount, params.sampleRate, params.nyquist, params.piOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, inputPtr, outputPtr, sustainValue, exponentValue, attackAudioResult.ptr, decayAudioResult.ptr, releaseAudioResult.ptr, trigAudioResult.ptr)
        genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      }
      genOpHelpers.writeOutputToHistoryRing(slot.history, outputPtr, params.bufferLength)
      genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      break
    }
    case 27: {
      const slot: GenSlot = vm.genPools[495].get()
      genOpHelpers.writeCallStackMetaToSlot(vm, slot)
      const procLen: i32 = genOpHelpers.alignedProcLength(params.bufferLength)
      genOpHelpers.writeInputToHistoryRingZero(slot.history)
      const inputPtr: usize = 0
      output = vm.arena.get(procLen)
      const outputPtr: usize = output.dataStart
      const instance: Adsr_default_attack_audio_decay_audio_sustain_scalar_release_audio_exponent_audio_trig_scalar = changetype<Adsr_default_attack_audio_decay_audio_sustain_scalar_release_audio_exponent_audio_trig_scalar>(slot.instance)
      vm.paramScratch[6] = instance.stage
      vm.paramScratch[7] = instance.env
      const attackAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, attackTagged, procLen)
      const decayAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, decayTagged, procLen)
      const releaseAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, releaseTagged, procLen)
      const exponentAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, exponentTagged, procLen)
      const osFactor: i32 = genOpHelpers.getOversampleFactor(vm)
      if (osFactor > 0) {
        const baseLen: i32 = params.bufferLength / osFactor
        const baseSampleRate: f32 = params.sampleRate / f32(osFactor)
        const baseNyquist: f32 = baseSampleRate * 0.5
        const basePiOverNyquist: f32 = f32(Mathf.PI) / baseNyquist
        const baseSampleCount: f32 = f32(params.sampleCount) / f32(osFactor)
        const baseProcLen: i32 = (baseLen + 15) & ~15
        const baseOut: Float32Array = vm.arena.get(baseProcLen)
        const baseOutPtr: usize = baseOut.dataStart
        let baseInputPtr: usize = inputPtr
        const attackSrc: usize = attackAudioResult.ptr
        const attackBuf: Float32Array = vm.arena.get(baseProcLen)
        const attackPtr: usize = attackBuf.dataStart
        downsample(vm, attackSrc, attackPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(attackPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(attackPtr + (usize(k) << 2), last)
          }
        }
        const decaySrc: usize = decayAudioResult.ptr
        const decayBuf: Float32Array = vm.arena.get(baseProcLen)
        const decayPtr: usize = decayBuf.dataStart
        downsample(vm, decaySrc, decayPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(decayPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(decayPtr + (usize(k) << 2), last)
          }
        }
        const releaseSrc: usize = releaseAudioResult.ptr
        const releaseBuf: Float32Array = vm.arena.get(baseProcLen)
        const releasePtr: usize = releaseBuf.dataStart
        downsample(vm, releaseSrc, releasePtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(releasePtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(releasePtr + (usize(k) << 2), last)
          }
        }
        const exponentSrc: usize = exponentAudioResult.ptr
        const exponentBuf: Float32Array = vm.arena.get(baseProcLen)
        const exponentPtr: usize = exponentBuf.dataStart
        downsample(vm, exponentSrc, exponentPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(exponentPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(exponentPtr + (usize(k) << 2), last)
          }
        }
        slot.history.write(params.sampleCount / osFactor, vm.paramScratch)
        instance.process(baseLen, params.sampleCount / osFactor, baseSampleRate, baseNyquist, basePiOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, baseInputPtr, baseOutPtr, sustainValue, trigValue, attackPtr, decayPtr, releasePtr, exponentPtr)
        upsample(vm, baseOutPtr, outputPtr, baseLen, osFactor)
        genOpHelpers.extendBufferWithLastSample(outputPtr, params.bufferLength, genOpHelpers.alignedProcLength(params.bufferLength))
        vm.arena.release(baseOut)
        vm.arena.release(attackBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
        vm.arena.release(decayBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
        vm.arena.release(releaseBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
        vm.arena.release(exponentBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
      } else {
        slot.history.write(params.sampleCount, vm.paramScratch)
        instance.process(params.bufferLength, params.sampleCount, params.sampleRate, params.nyquist, params.piOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, inputPtr, outputPtr, sustainValue, trigValue, attackAudioResult.ptr, decayAudioResult.ptr, releaseAudioResult.ptr, exponentAudioResult.ptr)
        genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
      }
      genOpHelpers.writeOutputToHistoryRing(slot.history, outputPtr, params.bufferLength)
      genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
      break
    }
    case 59: {
      const slot: GenSlot = vm.genPools[496].get()
      genOpHelpers.writeCallStackMetaToSlot(vm, slot)
      const procLen: i32 = genOpHelpers.alignedProcLength(params.bufferLength)
      genOpHelpers.writeInputToHistoryRingZero(slot.history)
      const inputPtr: usize = 0
      output = vm.arena.get(procLen)
      const outputPtr: usize = output.dataStart
      const instance: Adsr_default_attack_audio_decay_audio_sustain_scalar_release_audio_exponent_audio_trig_audio = changetype<Adsr_default_attack_audio_decay_audio_sustain_scalar_release_audio_exponent_audio_trig_audio>(slot.instance)
      vm.paramScratch[6] = instance.stage
      vm.paramScratch[7] = instance.env
      const attackAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, attackTagged, procLen)
      const decayAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, decayTagged, procLen)
      const releaseAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, releaseTagged, procLen)
      const exponentAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, exponentTagged, procLen)
      const trigAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, trigTagged, procLen)
      const osFactor: i32 = genOpHelpers.getOversampleFactor(vm)
      if (osFactor > 0) {
        const baseLen: i32 = params.bufferLength / osFactor
        const baseSampleRate: f32 = params.sampleRate / f32(osFactor)
        const baseNyquist: f32 = baseSampleRate * 0.5
        const basePiOverNyquist: f32 = f32(Mathf.PI) / baseNyquist
        const baseSampleCount: f32 = f32(params.sampleCount) / f32(osFactor)
        const baseProcLen: i32 = (baseLen + 15) & ~15
        const baseOut: Float32Array = vm.arena.get(baseProcLen)
        const baseOutPtr: usize = baseOut.dataStart
        let baseInputPtr: usize = inputPtr
        const attackSrc: usize = attackAudioResult.ptr
        const attackBuf: Float32Array = vm.arena.get(baseProcLen)
        const attackPtr: usize = attackBuf.dataStart
        downsample(vm, attackSrc, attackPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(attackPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(attackPtr + (usize(k) << 2), last)
          }
        }
        const decaySrc: usize = decayAudioResult.ptr
        const decayBuf: Float32Array = vm.arena.get(baseProcLen)
        const decayPtr: usize = decayBuf.dataStart
        downsample(vm, decaySrc, decayPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(decayPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(decayPtr + (usize(k) << 2), last)
          }
        }
        const releaseSrc: usize = releaseAudioResult.ptr
        const releaseBuf: Float32Array = vm.arena.get(baseProcLen)
        const releasePtr: usize = releaseBuf.dataStart
        downsample(vm, releaseSrc, releasePtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(releasePtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(releasePtr + (usize(k) << 2), last)
          }
        }
        const exponentSrc: usize = exponentAudioResult.ptr
        const exponentBuf: Float32Array = vm.arena.get(baseProcLen)
        const exponentPtr: usize = exponentBuf.dataStart
        downsample(vm, exponentSrc, exponentPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(exponentPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(exponentPtr + (usize(k) << 2), last)
          }
        }
        const trigSrc: usize = trigAudioResult.ptr
        const trigBuf: Float32Array = vm.arena.get(baseProcLen)
        const trigPtr: usize = trigBuf.dataStart
        downsample(vm, trigSrc, trigPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(trigPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(trigPtr + (usize(k) << 2), last)
          }
        }
        slot.history.write(params.sampleCount / osFactor, vm.paramScratch)
        instance.process(baseLen, params.sampleCount / osFactor, baseSampleRate, baseNyquist, basePiOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, baseInputPtr, baseOutPtr, sustainValue, attackPtr, decayPtr, releasePtr, exponentPtr, trigPtr)
        upsample(vm, baseOutPtr, outputPtr, baseLen, osFactor)
        genOpHelpers.extendBufferWithLastSample(outputPtr, params.bufferLength, genOpHelpers.alignedProcLength(params.bufferLength))
        vm.arena.release(baseOut)
        vm.arena.release(attackBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
        vm.arena.release(decayBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
        vm.arena.release(releaseBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
        vm.arena.release(exponentBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
        vm.arena.release(trigBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      } else {
        slot.history.write(params.sampleCount, vm.paramScratch)
        instance.process(params.bufferLength, params.sampleCount, params.sampleRate, params.nyquist, params.piOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, inputPtr, outputPtr, sustainValue, attackAudioResult.ptr, decayAudioResult.ptr, releaseAudioResult.ptr, exponentAudioResult.ptr, trigAudioResult.ptr)
        genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      }
      genOpHelpers.writeOutputToHistoryRing(slot.history, outputPtr, params.bufferLength)
      genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      break
    }
    case 7: {
      const slot: GenSlot = vm.genPools[497].get()
      genOpHelpers.writeCallStackMetaToSlot(vm, slot)
      const procLen: i32 = genOpHelpers.alignedProcLength(params.bufferLength)
      genOpHelpers.writeInputToHistoryRingZero(slot.history)
      const inputPtr: usize = 0
      output = vm.arena.get(procLen)
      const outputPtr: usize = output.dataStart
      const instance: Adsr_default_attack_audio_decay_audio_sustain_audio_release_scalar_exponent_scalar_trig_scalar = changetype<Adsr_default_attack_audio_decay_audio_sustain_audio_release_scalar_exponent_scalar_trig_scalar>(slot.instance)
      vm.paramScratch[6] = instance.stage
      vm.paramScratch[7] = instance.env
      const attackAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, attackTagged, procLen)
      const decayAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, decayTagged, procLen)
      const sustainAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, sustainTagged, procLen)
      const osFactor: i32 = genOpHelpers.getOversampleFactor(vm)
      if (osFactor > 0) {
        const baseLen: i32 = params.bufferLength / osFactor
        const baseSampleRate: f32 = params.sampleRate / f32(osFactor)
        const baseNyquist: f32 = baseSampleRate * 0.5
        const basePiOverNyquist: f32 = f32(Mathf.PI) / baseNyquist
        const baseSampleCount: f32 = f32(params.sampleCount) / f32(osFactor)
        const baseProcLen: i32 = (baseLen + 15) & ~15
        const baseOut: Float32Array = vm.arena.get(baseProcLen)
        const baseOutPtr: usize = baseOut.dataStart
        let baseInputPtr: usize = inputPtr
        const attackSrc: usize = attackAudioResult.ptr
        const attackBuf: Float32Array = vm.arena.get(baseProcLen)
        const attackPtr: usize = attackBuf.dataStart
        downsample(vm, attackSrc, attackPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(attackPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(attackPtr + (usize(k) << 2), last)
          }
        }
        const decaySrc: usize = decayAudioResult.ptr
        const decayBuf: Float32Array = vm.arena.get(baseProcLen)
        const decayPtr: usize = decayBuf.dataStart
        downsample(vm, decaySrc, decayPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(decayPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(decayPtr + (usize(k) << 2), last)
          }
        }
        const sustainSrc: usize = sustainAudioResult.ptr
        const sustainBuf: Float32Array = vm.arena.get(baseProcLen)
        const sustainPtr: usize = sustainBuf.dataStart
        downsample(vm, sustainSrc, sustainPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(sustainPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(sustainPtr + (usize(k) << 2), last)
          }
        }
        slot.history.write(params.sampleCount / osFactor, vm.paramScratch)
        instance.process(baseLen, params.sampleCount / osFactor, baseSampleRate, baseNyquist, basePiOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, baseInputPtr, baseOutPtr, releaseValue, exponentValue, trigValue, attackPtr, decayPtr, sustainPtr)
        upsample(vm, baseOutPtr, outputPtr, baseLen, osFactor)
        genOpHelpers.extendBufferWithLastSample(outputPtr, params.bufferLength, genOpHelpers.alignedProcLength(params.bufferLength))
        vm.arena.release(baseOut)
        vm.arena.release(attackBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
        vm.arena.release(decayBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
        vm.arena.release(sustainBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
      } else {
        slot.history.write(params.sampleCount, vm.paramScratch)
        instance.process(params.bufferLength, params.sampleCount, params.sampleRate, params.nyquist, params.piOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, inputPtr, outputPtr, releaseValue, exponentValue, trigValue, attackAudioResult.ptr, decayAudioResult.ptr, sustainAudioResult.ptr)
        genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
      }
      genOpHelpers.writeOutputToHistoryRing(slot.history, outputPtr, params.bufferLength)
      genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
      break
    }
    case 39: {
      const slot: GenSlot = vm.genPools[498].get()
      genOpHelpers.writeCallStackMetaToSlot(vm, slot)
      const procLen: i32 = genOpHelpers.alignedProcLength(params.bufferLength)
      genOpHelpers.writeInputToHistoryRingZero(slot.history)
      const inputPtr: usize = 0
      output = vm.arena.get(procLen)
      const outputPtr: usize = output.dataStart
      const instance: Adsr_default_attack_audio_decay_audio_sustain_audio_release_scalar_exponent_scalar_trig_audio = changetype<Adsr_default_attack_audio_decay_audio_sustain_audio_release_scalar_exponent_scalar_trig_audio>(slot.instance)
      vm.paramScratch[6] = instance.stage
      vm.paramScratch[7] = instance.env
      const attackAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, attackTagged, procLen)
      const decayAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, decayTagged, procLen)
      const sustainAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, sustainTagged, procLen)
      const trigAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, trigTagged, procLen)
      const osFactor: i32 = genOpHelpers.getOversampleFactor(vm)
      if (osFactor > 0) {
        const baseLen: i32 = params.bufferLength / osFactor
        const baseSampleRate: f32 = params.sampleRate / f32(osFactor)
        const baseNyquist: f32 = baseSampleRate * 0.5
        const basePiOverNyquist: f32 = f32(Mathf.PI) / baseNyquist
        const baseSampleCount: f32 = f32(params.sampleCount) / f32(osFactor)
        const baseProcLen: i32 = (baseLen + 15) & ~15
        const baseOut: Float32Array = vm.arena.get(baseProcLen)
        const baseOutPtr: usize = baseOut.dataStart
        let baseInputPtr: usize = inputPtr
        const attackSrc: usize = attackAudioResult.ptr
        const attackBuf: Float32Array = vm.arena.get(baseProcLen)
        const attackPtr: usize = attackBuf.dataStart
        downsample(vm, attackSrc, attackPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(attackPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(attackPtr + (usize(k) << 2), last)
          }
        }
        const decaySrc: usize = decayAudioResult.ptr
        const decayBuf: Float32Array = vm.arena.get(baseProcLen)
        const decayPtr: usize = decayBuf.dataStart
        downsample(vm, decaySrc, decayPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(decayPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(decayPtr + (usize(k) << 2), last)
          }
        }
        const sustainSrc: usize = sustainAudioResult.ptr
        const sustainBuf: Float32Array = vm.arena.get(baseProcLen)
        const sustainPtr: usize = sustainBuf.dataStart
        downsample(vm, sustainSrc, sustainPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(sustainPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(sustainPtr + (usize(k) << 2), last)
          }
        }
        const trigSrc: usize = trigAudioResult.ptr
        const trigBuf: Float32Array = vm.arena.get(baseProcLen)
        const trigPtr: usize = trigBuf.dataStart
        downsample(vm, trigSrc, trigPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(trigPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(trigPtr + (usize(k) << 2), last)
          }
        }
        slot.history.write(params.sampleCount / osFactor, vm.paramScratch)
        instance.process(baseLen, params.sampleCount / osFactor, baseSampleRate, baseNyquist, basePiOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, baseInputPtr, baseOutPtr, releaseValue, exponentValue, attackPtr, decayPtr, sustainPtr, trigPtr)
        upsample(vm, baseOutPtr, outputPtr, baseLen, osFactor)
        genOpHelpers.extendBufferWithLastSample(outputPtr, params.bufferLength, genOpHelpers.alignedProcLength(params.bufferLength))
        vm.arena.release(baseOut)
        vm.arena.release(attackBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
        vm.arena.release(decayBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
        vm.arena.release(sustainBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
        vm.arena.release(trigBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      } else {
        slot.history.write(params.sampleCount, vm.paramScratch)
        instance.process(params.bufferLength, params.sampleCount, params.sampleRate, params.nyquist, params.piOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, inputPtr, outputPtr, releaseValue, exponentValue, attackAudioResult.ptr, decayAudioResult.ptr, sustainAudioResult.ptr, trigAudioResult.ptr)
        genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      }
      genOpHelpers.writeOutputToHistoryRing(slot.history, outputPtr, params.bufferLength)
      genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      break
    }
    case 23: {
      const slot: GenSlot = vm.genPools[499].get()
      genOpHelpers.writeCallStackMetaToSlot(vm, slot)
      const procLen: i32 = genOpHelpers.alignedProcLength(params.bufferLength)
      genOpHelpers.writeInputToHistoryRingZero(slot.history)
      const inputPtr: usize = 0
      output = vm.arena.get(procLen)
      const outputPtr: usize = output.dataStart
      const instance: Adsr_default_attack_audio_decay_audio_sustain_audio_release_scalar_exponent_audio_trig_scalar = changetype<Adsr_default_attack_audio_decay_audio_sustain_audio_release_scalar_exponent_audio_trig_scalar>(slot.instance)
      vm.paramScratch[6] = instance.stage
      vm.paramScratch[7] = instance.env
      const attackAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, attackTagged, procLen)
      const decayAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, decayTagged, procLen)
      const sustainAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, sustainTagged, procLen)
      const exponentAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, exponentTagged, procLen)
      const osFactor: i32 = genOpHelpers.getOversampleFactor(vm)
      if (osFactor > 0) {
        const baseLen: i32 = params.bufferLength / osFactor
        const baseSampleRate: f32 = params.sampleRate / f32(osFactor)
        const baseNyquist: f32 = baseSampleRate * 0.5
        const basePiOverNyquist: f32 = f32(Mathf.PI) / baseNyquist
        const baseSampleCount: f32 = f32(params.sampleCount) / f32(osFactor)
        const baseProcLen: i32 = (baseLen + 15) & ~15
        const baseOut: Float32Array = vm.arena.get(baseProcLen)
        const baseOutPtr: usize = baseOut.dataStart
        let baseInputPtr: usize = inputPtr
        const attackSrc: usize = attackAudioResult.ptr
        const attackBuf: Float32Array = vm.arena.get(baseProcLen)
        const attackPtr: usize = attackBuf.dataStart
        downsample(vm, attackSrc, attackPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(attackPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(attackPtr + (usize(k) << 2), last)
          }
        }
        const decaySrc: usize = decayAudioResult.ptr
        const decayBuf: Float32Array = vm.arena.get(baseProcLen)
        const decayPtr: usize = decayBuf.dataStart
        downsample(vm, decaySrc, decayPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(decayPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(decayPtr + (usize(k) << 2), last)
          }
        }
        const sustainSrc: usize = sustainAudioResult.ptr
        const sustainBuf: Float32Array = vm.arena.get(baseProcLen)
        const sustainPtr: usize = sustainBuf.dataStart
        downsample(vm, sustainSrc, sustainPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(sustainPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(sustainPtr + (usize(k) << 2), last)
          }
        }
        const exponentSrc: usize = exponentAudioResult.ptr
        const exponentBuf: Float32Array = vm.arena.get(baseProcLen)
        const exponentPtr: usize = exponentBuf.dataStart
        downsample(vm, exponentSrc, exponentPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(exponentPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(exponentPtr + (usize(k) << 2), last)
          }
        }
        slot.history.write(params.sampleCount / osFactor, vm.paramScratch)
        instance.process(baseLen, params.sampleCount / osFactor, baseSampleRate, baseNyquist, basePiOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, baseInputPtr, baseOutPtr, releaseValue, trigValue, attackPtr, decayPtr, sustainPtr, exponentPtr)
        upsample(vm, baseOutPtr, outputPtr, baseLen, osFactor)
        genOpHelpers.extendBufferWithLastSample(outputPtr, params.bufferLength, genOpHelpers.alignedProcLength(params.bufferLength))
        vm.arena.release(baseOut)
        vm.arena.release(attackBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
        vm.arena.release(decayBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
        vm.arena.release(sustainBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
        vm.arena.release(exponentBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
      } else {
        slot.history.write(params.sampleCount, vm.paramScratch)
        instance.process(params.bufferLength, params.sampleCount, params.sampleRate, params.nyquist, params.piOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, inputPtr, outputPtr, releaseValue, trigValue, attackAudioResult.ptr, decayAudioResult.ptr, sustainAudioResult.ptr, exponentAudioResult.ptr)
        genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
      }
      genOpHelpers.writeOutputToHistoryRing(slot.history, outputPtr, params.bufferLength)
      genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
      break
    }
    case 55: {
      const slot: GenSlot = vm.genPools[500].get()
      genOpHelpers.writeCallStackMetaToSlot(vm, slot)
      const procLen: i32 = genOpHelpers.alignedProcLength(params.bufferLength)
      genOpHelpers.writeInputToHistoryRingZero(slot.history)
      const inputPtr: usize = 0
      output = vm.arena.get(procLen)
      const outputPtr: usize = output.dataStart
      const instance: Adsr_default_attack_audio_decay_audio_sustain_audio_release_scalar_exponent_audio_trig_audio = changetype<Adsr_default_attack_audio_decay_audio_sustain_audio_release_scalar_exponent_audio_trig_audio>(slot.instance)
      vm.paramScratch[6] = instance.stage
      vm.paramScratch[7] = instance.env
      const attackAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, attackTagged, procLen)
      const decayAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, decayTagged, procLen)
      const sustainAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, sustainTagged, procLen)
      const exponentAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, exponentTagged, procLen)
      const trigAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, trigTagged, procLen)
      const osFactor: i32 = genOpHelpers.getOversampleFactor(vm)
      if (osFactor > 0) {
        const baseLen: i32 = params.bufferLength / osFactor
        const baseSampleRate: f32 = params.sampleRate / f32(osFactor)
        const baseNyquist: f32 = baseSampleRate * 0.5
        const basePiOverNyquist: f32 = f32(Mathf.PI) / baseNyquist
        const baseSampleCount: f32 = f32(params.sampleCount) / f32(osFactor)
        const baseProcLen: i32 = (baseLen + 15) & ~15
        const baseOut: Float32Array = vm.arena.get(baseProcLen)
        const baseOutPtr: usize = baseOut.dataStart
        let baseInputPtr: usize = inputPtr
        const attackSrc: usize = attackAudioResult.ptr
        const attackBuf: Float32Array = vm.arena.get(baseProcLen)
        const attackPtr: usize = attackBuf.dataStart
        downsample(vm, attackSrc, attackPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(attackPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(attackPtr + (usize(k) << 2), last)
          }
        }
        const decaySrc: usize = decayAudioResult.ptr
        const decayBuf: Float32Array = vm.arena.get(baseProcLen)
        const decayPtr: usize = decayBuf.dataStart
        downsample(vm, decaySrc, decayPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(decayPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(decayPtr + (usize(k) << 2), last)
          }
        }
        const sustainSrc: usize = sustainAudioResult.ptr
        const sustainBuf: Float32Array = vm.arena.get(baseProcLen)
        const sustainPtr: usize = sustainBuf.dataStart
        downsample(vm, sustainSrc, sustainPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(sustainPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(sustainPtr + (usize(k) << 2), last)
          }
        }
        const exponentSrc: usize = exponentAudioResult.ptr
        const exponentBuf: Float32Array = vm.arena.get(baseProcLen)
        const exponentPtr: usize = exponentBuf.dataStart
        downsample(vm, exponentSrc, exponentPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(exponentPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(exponentPtr + (usize(k) << 2), last)
          }
        }
        const trigSrc: usize = trigAudioResult.ptr
        const trigBuf: Float32Array = vm.arena.get(baseProcLen)
        const trigPtr: usize = trigBuf.dataStart
        downsample(vm, trigSrc, trigPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(trigPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(trigPtr + (usize(k) << 2), last)
          }
        }
        slot.history.write(params.sampleCount / osFactor, vm.paramScratch)
        instance.process(baseLen, params.sampleCount / osFactor, baseSampleRate, baseNyquist, basePiOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, baseInputPtr, baseOutPtr, releaseValue, attackPtr, decayPtr, sustainPtr, exponentPtr, trigPtr)
        upsample(vm, baseOutPtr, outputPtr, baseLen, osFactor)
        genOpHelpers.extendBufferWithLastSample(outputPtr, params.bufferLength, genOpHelpers.alignedProcLength(params.bufferLength))
        vm.arena.release(baseOut)
        vm.arena.release(attackBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
        vm.arena.release(decayBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
        vm.arena.release(sustainBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
        vm.arena.release(exponentBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
        vm.arena.release(trigBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      } else {
        slot.history.write(params.sampleCount, vm.paramScratch)
        instance.process(params.bufferLength, params.sampleCount, params.sampleRate, params.nyquist, params.piOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, inputPtr, outputPtr, releaseValue, attackAudioResult.ptr, decayAudioResult.ptr, sustainAudioResult.ptr, exponentAudioResult.ptr, trigAudioResult.ptr)
        genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      }
      genOpHelpers.writeOutputToHistoryRing(slot.history, outputPtr, params.bufferLength)
      genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      break
    }
    case 15: {
      const slot: GenSlot = vm.genPools[501].get()
      genOpHelpers.writeCallStackMetaToSlot(vm, slot)
      const procLen: i32 = genOpHelpers.alignedProcLength(params.bufferLength)
      genOpHelpers.writeInputToHistoryRingZero(slot.history)
      const inputPtr: usize = 0
      output = vm.arena.get(procLen)
      const outputPtr: usize = output.dataStart
      const instance: Adsr_default_attack_audio_decay_audio_sustain_audio_release_audio_exponent_scalar_trig_scalar = changetype<Adsr_default_attack_audio_decay_audio_sustain_audio_release_audio_exponent_scalar_trig_scalar>(slot.instance)
      vm.paramScratch[6] = instance.stage
      vm.paramScratch[7] = instance.env
      const attackAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, attackTagged, procLen)
      const decayAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, decayTagged, procLen)
      const sustainAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, sustainTagged, procLen)
      const releaseAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, releaseTagged, procLen)
      const osFactor: i32 = genOpHelpers.getOversampleFactor(vm)
      if (osFactor > 0) {
        const baseLen: i32 = params.bufferLength / osFactor
        const baseSampleRate: f32 = params.sampleRate / f32(osFactor)
        const baseNyquist: f32 = baseSampleRate * 0.5
        const basePiOverNyquist: f32 = f32(Mathf.PI) / baseNyquist
        const baseSampleCount: f32 = f32(params.sampleCount) / f32(osFactor)
        const baseProcLen: i32 = (baseLen + 15) & ~15
        const baseOut: Float32Array = vm.arena.get(baseProcLen)
        const baseOutPtr: usize = baseOut.dataStart
        let baseInputPtr: usize = inputPtr
        const attackSrc: usize = attackAudioResult.ptr
        const attackBuf: Float32Array = vm.arena.get(baseProcLen)
        const attackPtr: usize = attackBuf.dataStart
        downsample(vm, attackSrc, attackPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(attackPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(attackPtr + (usize(k) << 2), last)
          }
        }
        const decaySrc: usize = decayAudioResult.ptr
        const decayBuf: Float32Array = vm.arena.get(baseProcLen)
        const decayPtr: usize = decayBuf.dataStart
        downsample(vm, decaySrc, decayPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(decayPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(decayPtr + (usize(k) << 2), last)
          }
        }
        const sustainSrc: usize = sustainAudioResult.ptr
        const sustainBuf: Float32Array = vm.arena.get(baseProcLen)
        const sustainPtr: usize = sustainBuf.dataStart
        downsample(vm, sustainSrc, sustainPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(sustainPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(sustainPtr + (usize(k) << 2), last)
          }
        }
        const releaseSrc: usize = releaseAudioResult.ptr
        const releaseBuf: Float32Array = vm.arena.get(baseProcLen)
        const releasePtr: usize = releaseBuf.dataStart
        downsample(vm, releaseSrc, releasePtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(releasePtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(releasePtr + (usize(k) << 2), last)
          }
        }
        slot.history.write(params.sampleCount / osFactor, vm.paramScratch)
        instance.process(baseLen, params.sampleCount / osFactor, baseSampleRate, baseNyquist, basePiOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, baseInputPtr, baseOutPtr, exponentValue, trigValue, attackPtr, decayPtr, sustainPtr, releasePtr)
        upsample(vm, baseOutPtr, outputPtr, baseLen, osFactor)
        genOpHelpers.extendBufferWithLastSample(outputPtr, params.bufferLength, genOpHelpers.alignedProcLength(params.bufferLength))
        vm.arena.release(baseOut)
        vm.arena.release(attackBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
        vm.arena.release(decayBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
        vm.arena.release(sustainBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
        vm.arena.release(releaseBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
      } else {
        slot.history.write(params.sampleCount, vm.paramScratch)
        instance.process(params.bufferLength, params.sampleCount, params.sampleRate, params.nyquist, params.piOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, inputPtr, outputPtr, exponentValue, trigValue, attackAudioResult.ptr, decayAudioResult.ptr, sustainAudioResult.ptr, releaseAudioResult.ptr)
        genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
      }
      genOpHelpers.writeOutputToHistoryRing(slot.history, outputPtr, params.bufferLength)
      genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
      break
    }
    case 47: {
      const slot: GenSlot = vm.genPools[502].get()
      genOpHelpers.writeCallStackMetaToSlot(vm, slot)
      const procLen: i32 = genOpHelpers.alignedProcLength(params.bufferLength)
      genOpHelpers.writeInputToHistoryRingZero(slot.history)
      const inputPtr: usize = 0
      output = vm.arena.get(procLen)
      const outputPtr: usize = output.dataStart
      const instance: Adsr_default_attack_audio_decay_audio_sustain_audio_release_audio_exponent_scalar_trig_audio = changetype<Adsr_default_attack_audio_decay_audio_sustain_audio_release_audio_exponent_scalar_trig_audio>(slot.instance)
      vm.paramScratch[6] = instance.stage
      vm.paramScratch[7] = instance.env
      const attackAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, attackTagged, procLen)
      const decayAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, decayTagged, procLen)
      const sustainAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, sustainTagged, procLen)
      const releaseAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, releaseTagged, procLen)
      const trigAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, trigTagged, procLen)
      const osFactor: i32 = genOpHelpers.getOversampleFactor(vm)
      if (osFactor > 0) {
        const baseLen: i32 = params.bufferLength / osFactor
        const baseSampleRate: f32 = params.sampleRate / f32(osFactor)
        const baseNyquist: f32 = baseSampleRate * 0.5
        const basePiOverNyquist: f32 = f32(Mathf.PI) / baseNyquist
        const baseSampleCount: f32 = f32(params.sampleCount) / f32(osFactor)
        const baseProcLen: i32 = (baseLen + 15) & ~15
        const baseOut: Float32Array = vm.arena.get(baseProcLen)
        const baseOutPtr: usize = baseOut.dataStart
        let baseInputPtr: usize = inputPtr
        const attackSrc: usize = attackAudioResult.ptr
        const attackBuf: Float32Array = vm.arena.get(baseProcLen)
        const attackPtr: usize = attackBuf.dataStart
        downsample(vm, attackSrc, attackPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(attackPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(attackPtr + (usize(k) << 2), last)
          }
        }
        const decaySrc: usize = decayAudioResult.ptr
        const decayBuf: Float32Array = vm.arena.get(baseProcLen)
        const decayPtr: usize = decayBuf.dataStart
        downsample(vm, decaySrc, decayPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(decayPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(decayPtr + (usize(k) << 2), last)
          }
        }
        const sustainSrc: usize = sustainAudioResult.ptr
        const sustainBuf: Float32Array = vm.arena.get(baseProcLen)
        const sustainPtr: usize = sustainBuf.dataStart
        downsample(vm, sustainSrc, sustainPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(sustainPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(sustainPtr + (usize(k) << 2), last)
          }
        }
        const releaseSrc: usize = releaseAudioResult.ptr
        const releaseBuf: Float32Array = vm.arena.get(baseProcLen)
        const releasePtr: usize = releaseBuf.dataStart
        downsample(vm, releaseSrc, releasePtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(releasePtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(releasePtr + (usize(k) << 2), last)
          }
        }
        const trigSrc: usize = trigAudioResult.ptr
        const trigBuf: Float32Array = vm.arena.get(baseProcLen)
        const trigPtr: usize = trigBuf.dataStart
        downsample(vm, trigSrc, trigPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(trigPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(trigPtr + (usize(k) << 2), last)
          }
        }
        slot.history.write(params.sampleCount / osFactor, vm.paramScratch)
        instance.process(baseLen, params.sampleCount / osFactor, baseSampleRate, baseNyquist, basePiOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, baseInputPtr, baseOutPtr, exponentValue, attackPtr, decayPtr, sustainPtr, releasePtr, trigPtr)
        upsample(vm, baseOutPtr, outputPtr, baseLen, osFactor)
        genOpHelpers.extendBufferWithLastSample(outputPtr, params.bufferLength, genOpHelpers.alignedProcLength(params.bufferLength))
        vm.arena.release(baseOut)
        vm.arena.release(attackBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
        vm.arena.release(decayBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
        vm.arena.release(sustainBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
        vm.arena.release(releaseBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
        vm.arena.release(trigBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      } else {
        slot.history.write(params.sampleCount, vm.paramScratch)
        instance.process(params.bufferLength, params.sampleCount, params.sampleRate, params.nyquist, params.piOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, inputPtr, outputPtr, exponentValue, attackAudioResult.ptr, decayAudioResult.ptr, sustainAudioResult.ptr, releaseAudioResult.ptr, trigAudioResult.ptr)
        genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      }
      genOpHelpers.writeOutputToHistoryRing(slot.history, outputPtr, params.bufferLength)
      genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      break
    }
    case 31: {
      const slot: GenSlot = vm.genPools[503].get()
      genOpHelpers.writeCallStackMetaToSlot(vm, slot)
      const procLen: i32 = genOpHelpers.alignedProcLength(params.bufferLength)
      genOpHelpers.writeInputToHistoryRingZero(slot.history)
      const inputPtr: usize = 0
      output = vm.arena.get(procLen)
      const outputPtr: usize = output.dataStart
      const instance: Adsr_default_attack_audio_decay_audio_sustain_audio_release_audio_exponent_audio_trig_scalar = changetype<Adsr_default_attack_audio_decay_audio_sustain_audio_release_audio_exponent_audio_trig_scalar>(slot.instance)
      vm.paramScratch[6] = instance.stage
      vm.paramScratch[7] = instance.env
      const attackAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, attackTagged, procLen)
      const decayAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, decayTagged, procLen)
      const sustainAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, sustainTagged, procLen)
      const releaseAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, releaseTagged, procLen)
      const exponentAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, exponentTagged, procLen)
      const osFactor: i32 = genOpHelpers.getOversampleFactor(vm)
      if (osFactor > 0) {
        const baseLen: i32 = params.bufferLength / osFactor
        const baseSampleRate: f32 = params.sampleRate / f32(osFactor)
        const baseNyquist: f32 = baseSampleRate * 0.5
        const basePiOverNyquist: f32 = f32(Mathf.PI) / baseNyquist
        const baseSampleCount: f32 = f32(params.sampleCount) / f32(osFactor)
        const baseProcLen: i32 = (baseLen + 15) & ~15
        const baseOut: Float32Array = vm.arena.get(baseProcLen)
        const baseOutPtr: usize = baseOut.dataStart
        let baseInputPtr: usize = inputPtr
        const attackSrc: usize = attackAudioResult.ptr
        const attackBuf: Float32Array = vm.arena.get(baseProcLen)
        const attackPtr: usize = attackBuf.dataStart
        downsample(vm, attackSrc, attackPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(attackPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(attackPtr + (usize(k) << 2), last)
          }
        }
        const decaySrc: usize = decayAudioResult.ptr
        const decayBuf: Float32Array = vm.arena.get(baseProcLen)
        const decayPtr: usize = decayBuf.dataStart
        downsample(vm, decaySrc, decayPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(decayPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(decayPtr + (usize(k) << 2), last)
          }
        }
        const sustainSrc: usize = sustainAudioResult.ptr
        const sustainBuf: Float32Array = vm.arena.get(baseProcLen)
        const sustainPtr: usize = sustainBuf.dataStart
        downsample(vm, sustainSrc, sustainPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(sustainPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(sustainPtr + (usize(k) << 2), last)
          }
        }
        const releaseSrc: usize = releaseAudioResult.ptr
        const releaseBuf: Float32Array = vm.arena.get(baseProcLen)
        const releasePtr: usize = releaseBuf.dataStart
        downsample(vm, releaseSrc, releasePtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(releasePtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(releasePtr + (usize(k) << 2), last)
          }
        }
        const exponentSrc: usize = exponentAudioResult.ptr
        const exponentBuf: Float32Array = vm.arena.get(baseProcLen)
        const exponentPtr: usize = exponentBuf.dataStart
        downsample(vm, exponentSrc, exponentPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(exponentPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(exponentPtr + (usize(k) << 2), last)
          }
        }
        slot.history.write(params.sampleCount / osFactor, vm.paramScratch)
        instance.process(baseLen, params.sampleCount / osFactor, baseSampleRate, baseNyquist, basePiOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, baseInputPtr, baseOutPtr, trigValue, attackPtr, decayPtr, sustainPtr, releasePtr, exponentPtr)
        upsample(vm, baseOutPtr, outputPtr, baseLen, osFactor)
        genOpHelpers.extendBufferWithLastSample(outputPtr, params.bufferLength, genOpHelpers.alignedProcLength(params.bufferLength))
        vm.arena.release(baseOut)
        vm.arena.release(attackBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
        vm.arena.release(decayBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
        vm.arena.release(sustainBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
        vm.arena.release(releaseBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
        vm.arena.release(exponentBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
      } else {
        slot.history.write(params.sampleCount, vm.paramScratch)
        instance.process(params.bufferLength, params.sampleCount, params.sampleRate, params.nyquist, params.piOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, inputPtr, outputPtr, trigValue, attackAudioResult.ptr, decayAudioResult.ptr, sustainAudioResult.ptr, releaseAudioResult.ptr, exponentAudioResult.ptr)
        genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
      }
      genOpHelpers.writeOutputToHistoryRing(slot.history, outputPtr, params.bufferLength)
      genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
      break
    }
    case 63: {
      const slot: GenSlot = vm.genPools[504].get()
      genOpHelpers.writeCallStackMetaToSlot(vm, slot)
      const procLen: i32 = genOpHelpers.alignedProcLength(params.bufferLength)
      genOpHelpers.writeInputToHistoryRingZero(slot.history)
      const inputPtr: usize = 0
      output = vm.arena.get(procLen)
      const outputPtr: usize = output.dataStart
      const instance: Adsr_default_attack_audio_decay_audio_sustain_audio_release_audio_exponent_audio_trig_audio = changetype<Adsr_default_attack_audio_decay_audio_sustain_audio_release_audio_exponent_audio_trig_audio>(slot.instance)
      vm.paramScratch[6] = instance.stage
      vm.paramScratch[7] = instance.env
      const attackAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, attackTagged, procLen)
      const decayAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, decayTagged, procLen)
      const sustainAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, sustainTagged, procLen)
      const releaseAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, releaseTagged, procLen)
      const exponentAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, exponentTagged, procLen)
      const trigAudioResult = genOpHelpers.taggedToAudioParamBuffer(vm, trigTagged, procLen)
      const osFactor: i32 = genOpHelpers.getOversampleFactor(vm)
      if (osFactor > 0) {
        const baseLen: i32 = params.bufferLength / osFactor
        const baseSampleRate: f32 = params.sampleRate / f32(osFactor)
        const baseNyquist: f32 = baseSampleRate * 0.5
        const basePiOverNyquist: f32 = f32(Mathf.PI) / baseNyquist
        const baseSampleCount: f32 = f32(params.sampleCount) / f32(osFactor)
        const baseProcLen: i32 = (baseLen + 15) & ~15
        const baseOut: Float32Array = vm.arena.get(baseProcLen)
        const baseOutPtr: usize = baseOut.dataStart
        let baseInputPtr: usize = inputPtr
        const attackSrc: usize = attackAudioResult.ptr
        const attackBuf: Float32Array = vm.arena.get(baseProcLen)
        const attackPtr: usize = attackBuf.dataStart
        downsample(vm, attackSrc, attackPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(attackPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(attackPtr + (usize(k) << 2), last)
          }
        }
        const decaySrc: usize = decayAudioResult.ptr
        const decayBuf: Float32Array = vm.arena.get(baseProcLen)
        const decayPtr: usize = decayBuf.dataStart
        downsample(vm, decaySrc, decayPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(decayPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(decayPtr + (usize(k) << 2), last)
          }
        }
        const sustainSrc: usize = sustainAudioResult.ptr
        const sustainBuf: Float32Array = vm.arena.get(baseProcLen)
        const sustainPtr: usize = sustainBuf.dataStart
        downsample(vm, sustainSrc, sustainPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(sustainPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(sustainPtr + (usize(k) << 2), last)
          }
        }
        const releaseSrc: usize = releaseAudioResult.ptr
        const releaseBuf: Float32Array = vm.arena.get(baseProcLen)
        const releasePtr: usize = releaseBuf.dataStart
        downsample(vm, releaseSrc, releasePtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(releasePtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(releasePtr + (usize(k) << 2), last)
          }
        }
        const exponentSrc: usize = exponentAudioResult.ptr
        const exponentBuf: Float32Array = vm.arena.get(baseProcLen)
        const exponentPtr: usize = exponentBuf.dataStart
        downsample(vm, exponentSrc, exponentPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(exponentPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(exponentPtr + (usize(k) << 2), last)
          }
        }
        const trigSrc: usize = trigAudioResult.ptr
        const trigBuf: Float32Array = vm.arena.get(baseProcLen)
        const trigPtr: usize = trigBuf.dataStart
        downsample(vm, trigSrc, trigPtr, baseLen, osFactor)
        if (baseProcLen > baseLen && baseLen > 0) {
          const last: f32 = load<f32>(trigPtr + (usize(baseLen - 1) << 2))
          for (let k: i32 = baseLen; k < baseProcLen; k++) {
            store<f32>(trigPtr + (usize(k) << 2), last)
          }
        }
        slot.history.write(params.sampleCount / osFactor, vm.paramScratch)
        instance.process(baseLen, params.sampleCount / osFactor, baseSampleRate, baseNyquist, basePiOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, baseInputPtr, baseOutPtr, attackPtr, decayPtr, sustainPtr, releasePtr, exponentPtr, trigPtr)
        upsample(vm, baseOutPtr, outputPtr, baseLen, osFactor)
        genOpHelpers.extendBufferWithLastSample(outputPtr, params.bufferLength, genOpHelpers.alignedProcLength(params.bufferLength))
        vm.arena.release(baseOut)
        vm.arena.release(attackBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
        vm.arena.release(decayBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
        vm.arena.release(sustainBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
        vm.arena.release(releaseBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
        vm.arena.release(exponentBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
        vm.arena.release(trigBuf)
        genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      } else {
        slot.history.write(params.sampleCount, vm.paramScratch)
        instance.process(params.bufferLength, params.sampleCount, params.sampleRate, params.nyquist, params.piOverNyquist, vm.currentBpm, vm.co, vm.samplesPerBeat, vm.samplesPerBar, inputPtr, outputPtr, attackAudioResult.ptr, decayAudioResult.ptr, sustainAudioResult.ptr, releaseAudioResult.ptr, exponentAudioResult.ptr, trigAudioResult.ptr)
        genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
        genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      }
      genOpHelpers.writeOutputToHistoryRing(slot.history, outputPtr, params.bufferLength)
      genOpHelpers.releaseTaggedAudioParamResult(vm, attackAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, decayAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, sustainAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, releaseAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, exponentAudioResult)
      genOpHelpers.releaseTaggedAudioParamResult(vm, trigAudioResult)
      break
    }
    default: {
      const procLen: i32 = (params.bufferLength + 15) & ~15
      output = vm.arena.get(procLen)
      memory.fill(output.dataStart, 0, usize(procLen) << 2)
      break
    }
  }
  push(vm, encodeAudio(output.dataStart), true)
  if (isAudio(attackTagged)) vm.arena.releaseByPtr(u32(decodeAudio(attackTagged)))
  if (isAudio(decayTagged)) vm.arena.releaseByPtr(u32(decodeAudio(decayTagged)))
  if (isAudio(sustainTagged)) vm.arena.releaseByPtr(u32(decodeAudio(sustainTagged)))
  if (isAudio(releaseTagged)) vm.arena.releaseByPtr(u32(decodeAudio(releaseTagged)))
  if (isAudio(exponentTagged)) vm.arena.releaseByPtr(u32(decodeAudio(exponentTagged)))
  if (isAudio(trigTagged)) vm.arena.releaseByPtr(u32(decodeAudio(trigTagged)))
  if (vm.absolutePCCallStackTop > 0) vm.absolutePCCallStackTop--
  return pc
}

    default: {
      throw new Error(`Unknown gen op: ${op}`)
    }
  }
}
